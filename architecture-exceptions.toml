# Architecture Exceptions
# Every exception MUST have: id, reason, owner, expires, migration_issue.
# Exceptions without expiry are prohibited.

[legacy_deps]
# These are pre-existing dependency edges that violate the target DAG.
# New code must NOT follow these patterns.

"backend-wasm -> ir" = { id = "ARCH-EXC-001", reason = "LoweredProgram type used in legacy emitter (behind legacy-emitter feature gate)", owner = "wgkz", expires = "2026-09-01", migration_issue = "P3: migrate to semantic-ir types" }
"backend-wasm -> runtime-catalog" = { id = "ARCH-EXC-005", reason = "RuntimeFn/SpecOp mapping in legacy emitter (behind legacy-emitter feature gate)", owner = "wgkz", expires = "2026-09-01", migration_issue = "P5: RuntimeFn -> SpecOp migration" }
"compiler -> ir" = { id = "ARCH-EXC-006", reason = "LoweredProgram pipeline in legacy path (behind legacy-pipeline feature gate)", owner = "wgkz", expires = "2026-09-01", migration_issue = "P3: switch to semantic-ir pipeline" }

# RESOLVED exceptions:
# ARCH-EXC-002 (backend-wasm -> syntax): dep removed from Cargo.toml (dead dep)
# ARCH-EXC-003 (backend-wasm -> semantics): dep removed from Cargo.toml (dead dep)
# ARCH-EXC-004 (backend-wasm -> resolve): dep removed from Cargo.toml (dead dep)
# ARCH-EXC-007 (compiler -> runtime-abi): added to allowed_deps
# ARCH-EXC-008 (cli -> ir): added to allowed_deps
# ARCH-EXC-009 (cli -> backend-wasm): added to allowed_deps
# ARCH-EXC-010 (cli -> runtime-abi): added to allowed_deps
# ARCH-EXC-012 (semantic-ir -> source): added to allowed_deps
# ARCH-EXC-014 (runtime-catalog -> spec-kernel): added to allowed_deps
# ARCH-EXC-015 (runtime-catalog -> shared): added to allowed_deps

[legacy_files]
"crates/backend-wasm/src/native_lowered.rs" = { id = "ARCH-EXC-016", allowed_change = "delete|bugfix", reason = "Behind legacy-emitter feature gate — replace with spec_emit", owner = "wgkz", expires = "2026-09-01", migration_issue = "P3: spec backend" }
"crates/backend-wasm/src/runtime/core/typed.rs" = { id = "ARCH-EXC-017", allowed_change = "delete|move", reason = "Behind legacy-emitter feature gate — move to runtime-core", owner = "wgkz", expires = "2026-09-01", migration_issue = "P2: SpecOp builders" }
"crates/backend-wasm/src/native_runtime_embed.rs" = { id = "ARCH-EXC-018", allowed_change = "delete|move|bugfix", reason = "Behind legacy-emitter feature gate — move to runtime-core", owner = "wgkz", expires = "2026-09-01", migration_issue = "P2: SpecOp builders" }
"crates/runtime-catalog/src/runtime_fn.rs" = { id = "ARCH-EXC-019", allowed_change = "bugfix", reason = "Pre-existing catalog maintenance only", owner = "wgkz", expires = "2026-09-01", migration_issue = "P5: RuntimeFn cleanup" }

[legacy_runtimefn]
# Exceptions for RuntimeFn modifications (bugfix only)
# new_variant = { id = "ARCH-EXC-NNN", old_variant = "...", reason = "...", owner = "wgkz", expires = "...", migration_issue = "..." }
