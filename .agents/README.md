# Agent Workspace

This directory contains agent prompts, skills, and historical plans. The current project contract is `AGENTS.md`; the current docs map is `docs/INDEX.md`.

## Contents

| Path | Role |
|---|---|
| `.agents/skills/` | Procedure docs for recurring work |
| `.agents/prompts/` | Worker prompts |
| `.agents/plans/` | Historical plans; not canonical current design |

## Rule

When a skill needs project facts, point to `docs/` paths instead of duplicating large explanations.
