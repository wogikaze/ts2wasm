#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "pre-commit: cargo fmt --all --check"
cargo fmt --all --check

npx markdownlint-cli2 "**/*.md" "#reference" --fix