# fail-fixture

```typescript
// Test fixture: should compile but produce wrong output
// We output WRONG but Node.js will output PASS
console.log("WRONG");

```

**Path:** `fixtures/test-infrastructure/fail-fixture.ts`
