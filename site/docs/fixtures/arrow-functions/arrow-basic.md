# arrow-basic

```typescript
// Test basic arrow function
const add = (x, y) => x + y;
console.log(add(2, 3)); // Should print 5
console.log(0); // Exit marker

```

**Path:** `fixtures/arrow-functions/arrow-basic.ts`
