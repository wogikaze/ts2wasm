# string-key-literal

```typescript
// Test fixture for object literal with string literal keys
const obj = {"name": "Alice", "age": 30};
console.log(obj.name);
console.log(obj.age);

```

**Path:** `fixtures/arrays-objects/string-key-literal.ts`
