# string-indexing

```typescript
// Test fixture for string indexing
const s = "hello";
console.log(s[0]); // Should print 'h' (UTF-16 code unit)
console.log(s[1]); // Should print 'e'
console.log(s[4]); // Should print 'o'
console.log(s[5]); // Should print undefined

```

**Path:** `fixtures/builtins-and-io/string-indexing.ts`
