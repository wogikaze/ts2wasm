# regexp-unsupported-flag

```typescript
// Diagnostic fixture for unsupported RegExp literal flags tracked by issue 202.
console.log(/abc/d);

```

**Path:** `fixtures/core-semantics/regexp-unsupported-flag.ts`
