# ir-test

```typescript
function add(a: number, b: number): number {
  return a + b;
}

const total: number = add(1, 2);

if (total === 3) {
  console.log("ir-ok");
} else {
  console.log("ir-bad");
}

```

**Path:** `fixtures/core-semantics/ir-test.ts`
