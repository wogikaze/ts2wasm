# function-prototype-metadata-unsupported

```typescript
function score(left, right) {
    return left + right;
}

console.log(score.prototype);

```

**Path:** `fixtures/core-semantics/function-prototype-metadata-unsupported.ts`
