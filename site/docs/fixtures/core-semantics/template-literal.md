# template-literal

```typescript
// Test template literal interpolation
let name = "world";
let count = 3;

console.log(`Hello, ${name}!`);
console.log(`${name}`);
console.log(`${name}${count}`);
console.log(`left ${name} middle ${count} right`);
console.log(`tick \`${name}\``);
console.log(0); // exit marker

```

**Path:** `fixtures/core-semantics/template-literal.ts`
