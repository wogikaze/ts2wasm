# arrow-captured-local

```typescript
let base = 4;
const addBase = x => x + base;

console.log(addBase(3));
base = 10;
console.log(addBase(3));

```

**Path:** `fixtures/core-semantics/arrow-captured-local.ts`
