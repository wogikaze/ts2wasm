# for-await-of-unsupported

```typescript
// Diagnostic fixture for unsupported async iteration tracked by issue 230.
for await (var value of values) {
  console.log(value);
}

```

**Path:** `fixtures/core-semantics/for-await-of-unsupported.ts`
