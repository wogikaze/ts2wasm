# regexp-literal

```typescript
// Test supported RegExp literal syntax in the current string-backed subset.
console.log(/abc/i);
console.log(/a*/g);
console.log(/a\/b/);
console.log(/[a/]/);

```

**Path:** `fixtures/core-semantics/regexp-literal.ts`
