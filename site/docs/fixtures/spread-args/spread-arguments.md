# spread-arguments

```typescript
// Test spread arguments in function calls

function add(a, b, c) {
    return a + b + c;
}

add(...[1, 2, 3]);

```

**Path:** `fixtures/spread-args/spread-arguments.ts`
