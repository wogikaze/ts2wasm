# new-expression

```typescript
class Box {
    constructor(v) {
        this.v = v;
    }

    get() {
        return this.v;
    }
}

let b = new Box(42);
console.log(b.get());

```

**Path:** `fixtures/classes-and-inheritance/new-expression.ts`
