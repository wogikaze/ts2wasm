# class-extends

```typescript
class Base {
    value() {
        return 7;
    }
}

class Child extends Base {
}

let c = new Child();
console.log(c.value());

```

**Path:** `fixtures/classes-and-inheritance/class-extends.ts`
