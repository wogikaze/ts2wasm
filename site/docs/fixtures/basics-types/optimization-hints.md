# optimization-hints

```typescript
function typedAdd(a: number, b: number): number {
  return a + b;
}

const left: number = 1;
const right: number = 2;
const total: number = typedAdd(left, right);

console.log(total);

```

**Path:** `fixtures/basics-types/optimization-hints.ts`
