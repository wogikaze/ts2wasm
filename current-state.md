# Current State Pointer

Canonical current state lives in `docs/current-state.md`.

This root-level file exists for older agents and scripts that look for `current-state.md` at repository root. Do not duplicate status tables here; update `docs/current-state.md` instead.
