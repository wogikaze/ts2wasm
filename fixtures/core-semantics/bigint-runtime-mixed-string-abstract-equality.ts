let one = { x: 1n };
let neg = { x: -3n };
let zero = { x: 0n };
let valid = { x: "1" };
let same = "1";
same = same + "";
let different = "2";
different = different + "";
let invalid = "abc";
invalid = invalid + "";
let fractional = "1.0";
fractional = fractional + "";
let empty = "";
empty = empty + "";
let spaces = "   ";
spaces = spaces + "";
let signed = "-3";
signed = signed + "";

console.log(one.x == valid.x);
console.log(valid.x == one.x);
console.log(one.x == same);
console.log(same == one.x);
console.log(one.x != different);
console.log(different != one.x);
console.log(zero.x == empty);
console.log(spaces == zero.x);
console.log(neg.x == signed);
console.log(signed == neg.x);
console.log(one.x == invalid);
console.log(invalid != one.x);
console.log(one.x == fractional);
console.log(fractional != one.x);
