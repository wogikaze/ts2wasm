// Object literal creation
// Exercises: object literal syntax, basic property read

let obj = { x: 42 };
console.log(obj.x);

let point = { x: 10, y: 20 };
console.log(point.x);
console.log(point.y);

let empty = {};
console.log(empty.x);

let person = { name: "Alice", age: 30 };
console.log(person.name);
console.log(person.age);
