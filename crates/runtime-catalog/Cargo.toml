[package]
name = "ts2wasm-runtime-catalog"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ts2wasm-runtime-abi = { path = "../runtime-abi" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
ts2wasm-shared = { path = "../shared" }
