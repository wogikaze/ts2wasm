Id: I-20260513-G84CR3
Status: done
Priority: P1
Labels: type:feature area:runtime area:ir
Owner:
BlockedReason:
Created: 2026-05-13T20:48:09+09:00
Updated: 2026-05-13T20:55:43+09:00
Title: Runtime: bound constructor function objects and new.target preservation
Summary: Add a real function object model for bound constructors, construct calls, and new.target preservation through Function.prototype.bind.
Next:
---

## Acceptance

- Add a Node differential fixture covering `const Bound = SomeClass.bind(null); new Bound()` with `new.target` observable in the original constructor.
- Add a Node differential fixture covering reused bound constructors with partial arguments.
- `cargo nextest run -p ts2wasm-cli --test m2_node_diff bound_constructor_function_objects_match_node_output`
- `cargo test -p ts2wasm-ir -p ts2wasm-compiler`

## Notes

- 2026-05-13 codex: Added source-visible bound class constructor tracking for `ClassName.bind(thisArg, ...boundArgs)` locals. `new BoundLocal(...args)` now reconstructs the original class constructor path with bound args prepended, ignores the bound `thisArg` during construct calls, and preserves existing `new.target === ClassName` behavior through the original class prototype reference.
- Current scope is statically visible class-constructor binding. Fully dynamic first-class function object reflection/identity remains a broader runtime object-model topic outside the validated fixture.

## Evidence
- bound constructor new.target fixture validated with TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff bound_constructor_function_objects_match_node_output
- RED: `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff bound_constructor_function_objects_match_node_output` failed with `unresolved name: BoundBox`.
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff bound_constructor_function_objects_match_node_output` => pass.
- `cargo nextest run -p ts2wasm-cli --test m2_node_diff bound_constructor_function_objects_match_node_output` => pass.
- `cargo test -p ts2wasm-ir -p ts2wasm-compiler` => pass.
- `mise run clippy` => pass.
- `mise run issue-lint` => pass.
- `git diff --check` => pass.
