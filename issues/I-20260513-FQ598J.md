Id: I-20260513-FQ598J
LegacyId: 481
Status: doing
Priority: P2
Labels: type:feature area:runtime
Owner: agent/loop
BlockedReason: 
Created: 2026-05-13T10:03:15+09:00
Updated: 2026-05-14T08:07:19+09:00
Title: Runtime: NativeError types (EvalError, RangeError, ReferenceError, SyntaxError, TypeError, URIError) with prototype methods
Summary: Implement all NativeError constructors with proper prototype chain (NativeError.prototype.name/constructor/stack/message). Every Error subclass has sp
Next: Add $native_error_create for all 6 Error subtypes with prototype
---

## Acceptance
- [x] cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_native_error_types (PASS)
- [x] All 7 Error types (Error, EvalError, RangeError, ReferenceError, SyntaxError, TypeError, URIError) compile and run with correct message/stack/instanceof

## Notes

### Implementation (2026-05-14)

Added EvalError and URIError support to complete all 7 NativeError types:

**`crates/ir/src/lowered/types.rs`**:
- Added `EvalError` and `URIError` variants to `BuiltinErrorConstructor` enum
- Added `from_name()` entries for "EvalError" and "URIError"
- Updated `parent()` to return `Some(Self::Error)` for both (same as other subtypes)

**`crates/backend-wasm/src/emitter/mod.rs`**:
- Added `error_proto_eval_error` and `error_proto_uri_error` to `builtin_error_prototype_global()`
- Added `"EvalError: "` and `"URIError: "` to `builtin_error_stack_prefix()`

**No other changes needed** — prototype globals, prototype initializers, GC root marking, ErrorNew emission, BuiltinErrorPrototype emission, and instanceof handling all use the enum generically via `builtin_error_prototype_global()` / `constructor.parent()`.

**Test fixture**: `fixtures/builtins-and-io/native-error-types.ts` covers all 7 types with message and instanceof checks.

**Validation**: cargo nextest run => 2318/2319 pass (1 pre-existing failure).
