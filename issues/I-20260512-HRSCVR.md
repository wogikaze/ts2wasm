Id: I-20260512-HRSCVR
Status: done
Priority: P2
Labels: P16 ir hir docs test
Owner:
BlockedReason:
Created: 2026-05-12T15:00:00+09:00
Updated: 2026-05-13T22:04:07+09:00
Title: Map HIR supported slice and UnsupportedSyntax boundary to semantic fixtures
Summary: Turn the implemented HIR slice and explicit unsupported forms into a tracked support matrix with tests.
Next:
---

## Notes

### Goal

Make the current HIR boundary measurable. The important result is not that every JavaScript feature lowers to HIR, but that every supported and unsupported case is deliberate.

### Acceptance

- Document supported HIR statements and expressions against the feature matrix.
- Add fixtures for supported `let`, assignment, expression, `if`, `while`, `return`, constants, locals, property/index, builtin/user/method calls.
- Add rejection fixtures for nested function, ternary, assignment expression, and dynamic calls where HIR intentionally rejects them.
- Ensure unsupported forms produce stable diagnostics rather than silent fallback in HIR-only validation.
- Record commands and any unavailable tooling caveat.

### Non-goals

- No broad HIR feature expansion in this slice.
- No native MIR model changes.

## Evidence
- HIR support matrix and fixture-backed UnsupportedSyntax boundary: fmt/check/fixture-catalog/architecture/clippy/nextest pass


- Added `fixtures/hir-support/` supported fixtures for HIR statements, expressions, constants, local/builtin loads, property/index access, builtin calls, direct user calls, and method calls.
- Added `fixtures/hir-support/` unsupported fixtures for nested function declarations, ternary expressions, assignment expressions, and dynamic calls.
- Added `docs/27-ir-layer-completion.md` HIR support matrix mapping HIR boundaries to feature labels and fixture evidence.
- `cargo test -p ts2wasm-ir hir_` => pass.
- `mise run check fixture-catalog` => pass.
- `mise run fmt` => pass.
- `mise run check` => pass.
- `mise run check architecture` => pass with existing warnings.
- `mise run check scripts` => pass.
- `mise run clippy` => pass.
- `mise run nextest` => pass (2246 passed, 10 skipped).
- `mise run issue-lint` => pass.
- `git diff --check` => pass.
