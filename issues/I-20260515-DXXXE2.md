Id: I-20260515-DXXXE2
Status: done
Priority: P0
Labels: area:backend-wasm,type:perf
Owner: 
BlockedReason: 
Created: 2026-05-15T22:41:46+09:00
Updated: 2026-05-16T20:28:12+09:00
Title: Remove per-case wat2wasm process from semantic coverage
Summary: Replace per-case wat2wasm subprocess with direct binary emission or wasm-encoder backend
Next: 
---

## Notes

### 2026-05-16: Implemented

**Changes:**
1. Removed `wasm-encoder-backend` feature gate; `wasm-encoder` is now a regular (non-optional) dependency
2. Removed all `#[cfg(feature = "wasm-encoder-backend")]` gates from test files (`wasm_encoder_parity.rs`, `wat_writer/tests.rs`)
3. Added `emit_wasm_binary(program: &Validated<LoweredProgram>) -> Result<Vec<u8>, Diagnostic>` — high-level function that emits WAT text then converts to binary via pure-Rust `wat` crate (no subprocess)
4. Fixed `CustomSection` API usage for wasm-encoder v0.248 (uses struct fields, not `.new()`)
5. `emit_wasm_module_binary` and `WasmEncoderBackendExt` are now unconditionally available in the public API

**Evidence:**
- `cargo nextest run -p ts2wasm-backend-wasm`: 138 passed (including previously feature-gated parity tests)
- `cargo nextest run -p ts2wasm-compiler`: 88 passed
- Commit: `ef9844724`

## Test-Requirements
- `cargo nextest run -p ts2wasm-backend-wasm` → 138 passed
- `cargo nextest run -p ts2wasm-compiler` → 88 passed

## Acceptance
- [x] Per-case wat2wasm subprocess removed from semantic coverage path
- [x] wasm-encoder backend always available (no feature gate)
- [x] All existing tests pass (138 backend-wasm, 88 compiler)

## Evidence
- commit ef9844724: removed feature gate, added emit_wasm_binary; cargo nextest run -p ts2wasm-backend-wasm: 138 passed; cargo nextest run -p ts2wasm-compiler: 88 passed
