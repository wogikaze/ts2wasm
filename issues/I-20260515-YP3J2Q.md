Id: I-20260515-YP3J2Q
Status: open
Priority: P1
Labels: area:runtime feature:symbol
Owner: 
BlockedReason: 
Created: 2026-05-15T01:31:00+09:00
Updated: 2026-05-15T01:31:00+09:00
Title: Runtime symbol-key object properties need identity semantics
Summary: Object property storage stringifies dynamic symbol keys, so same-description symbols can collide and descriptor lookup loses symbol identity.
Next: Add runtime object-property key storage/lookup that preserves raw symbol identity for get/set/defineProperty/descriptor paths, then add node-diff fixtures with two Symbol("slot") keys.

---

## Notes

- Discovered while closing `I-20260514-67ZV8S` symbol-key accessor parity.
- Repro shape:
  - `const a = Symbol("slot"); const b = Symbol("slot");`
  - Define object properties/accessors with `[a]` and `[b]`.
  - Node treats `a` and `b` as distinct keys; current dynamic object runtime paths stringify symbol values and can collapse both to `Symbol(slot)`.
- `I-20260514-67ZV8S` now has a static accessor-dispatch workaround for statically visible symbol locals, but that does not fix general runtime symbol-key storage, descriptor lookup, enumeration, or dynamic aliasing.
