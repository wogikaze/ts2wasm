Id: I-20260515-VC2Z7R
Status: open
Priority: P1
Labels: type:architecture area:backend area:abi
Owner: 
BlockedReason: 
Created: 2026-05-15T05:32:26+09:00
Updated: 2026-05-15T05:32:26+09:00
Title: Architecture wave: typed WasmIR/wasm-encoder parity fixtures
Summary: Add bounded typed WasmIR fixtures proving WAT/wasm-encoder parity for imports, memory, globals, data, functions, exports, and ABI custom section.
Next: Implement typed WasmIR parity fixtures and ABI custom section parity test.

---

## Acceptance
- Add fixtures covering imports, memory, globals, data, functions, exports, custom ABI section through typed WasmIR (`WasmInstr` / `WasmModule`).
- Emit each fixture through `WatWriter::emit_module` and feature-gated `wasm-encoder` backend.
- Validate WAT with `wasm-tools validate` when present.
- Validate binary output with `wasm-tools validate` when wasm-encoder feature is enabled.
- Record unavailable tools as `blocked` in test output with reason `tool-unavailable:<tool-or-feature>`, not as `pass`.
- ABI custom section parity (REQ-ABI-001) is included in at least one fixture.
- Run: cargo test -p ts2wasm-backend-core -p ts2wasm-backend-wasm wasm_ir
- Run: python3 scripts/manager.py check wasm

## Notes
- Source: plans/next-architecture-design (2).md REQ-ABI-003, TASK-ABI-002 (partial).
- Suggested order: after generated WASM ABI metadata (I-20260515-ZMKV6V).
- Existing related: I-20260515-E24P8A (canonical target profiles) covers REQ-ABI-002; this issue covers the typed WasmIR parity slice of TASK-ABI-002.
- Related existing: I-20260512-WAENCD (wasm-encoder parity fixtures).
- Do not switch the default backend to wasm-encoder.
- Do not replace all raw WAT emission.
