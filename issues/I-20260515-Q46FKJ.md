Id: I-20260515-Q46FKJ
Status: done
Priority: P2
Labels: type:feature area:runtime
Owner: 
BlockedReason: 
Created: 2026-05-15T21:11:08+09:00
Updated: 2026-05-17T01:51:12+09:00
Title: Runtime: BigInt arithmetic and BigInt typed arrays
Summary: Implement BigInt arithmetic, BigInt typed arrays, BigInt string operations
Next: 
---

## Acceptance
- [x] iwasm test: `new BigInt64Array(1)` + `arr[0] = 8n` + `console.log(arr[0])` → "8n"
- [x] cargo nextest run -p ts2wasm-cli --test m2_node_diff typedarray_constructors

## Test-Requirements
- Test type: differential
- Fixture path(s): fixtures/builtins-and-io/typedarray-constructors.ts (existing)
- Test command(s): cargo nextest run -p ts2wasm-cli --test m2_node_diff typedarray_constructors
- Expected result: pass

## Evidence
- cargo nextest run -p ts2wasm-cli --test m2_node_diff typedarray_constructors: exit 0; iwasm BigInt64Array(1) element access returns correct value
 (2026-05-17)
- Fix: Added `ResolvedExpr::New { class_name, .. } if is_typed_array_class(class_name)` to `resolved_expr_produces_dense_array` in `crates/ir/src/lowered/resolver/expr/facts.rs`
- Root cause: `$property_get` WAT function only handles OBJECT tag (7). When typed array constructor results were not tracked in `array_locals`, static index access like `arr[0]` was lowered as `PropertyGet` (static string key → `$property_get`) instead of `PropertyGetDynamic` (`$index`). The `$index` function correctly handles ARRAY tag (5) via `$array_get`.
- Verification: `iwasm` output shows `8n` and `1` instead of `undefined` and `1`
- All 516 compiler/IR tests pass
- All 6 typed array node_diff tests pass
- All build_smoke tests pass (pre-existing failures unchanged)

## Notes
