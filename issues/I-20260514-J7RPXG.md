Id: I-20260514-J7RPXG
Status: done
Priority: P2
Labels: type:feature area:runtime
Owner:
BlockedReason:
Created: 2026-05-14T04:18:08+09:00
Updated: 2026-05-14T09:31:55+09:00
Title: Runtime: Number.prototype formatting methods (toExponential, toFixed, toPrecision)
Summary: Implement Number.prototype formatting methods after static Number methods landed in I-20260513-7XN478.
Next:
---

## Notes

Static Number methods (`isNaN`, `isFinite`, `isInteger`, `isSafeInteger`) are done in I-20260513-7XN478. Remaining scope includes unique spec symbols, digit precision formatting, and radix handling follow-ups for `Number.parseInt` / `Number.parseFloat`.

## Evidence

- number-formatting small-integer Node/iwasm fixture pass; decimal rounding parity split to I-20260514-D6YC6Z
- Commit `0d6895015` adds stack_effect fix and static literal optimization

Implemented the runtime path for the current small-integer number representation:

- `toFixed`, `toExponential`, and `toPrecision` now lower to distinct runtime functions instead of all re-emitting `$value_to_string_into`.
- Omitted precision arguments lower as `undefined`; explicit precision arguments are passed to the runtime.
- Added `fixtures/builtins-and-io/number-formatting.ts` and a Node/iwasm differential assertion.

Complete changes across both commits (`f093ee578` + `0d6895015`):
- WAT implementations in `runtime_strings.rs` with proper 2-param signatures
- Spec symbols point to `$number_to_fixed`/`$number_to_exponential`/`$number_to_precision`
- Stack effect updated to 2 params, 1 result for all three methods
- `#[allow(dead_code)]` removed from RuntimeFn variants and emit methods
- Static compile-time format evaluation via `static_number_format_method` (literal values)
- `number_literal_locals` tracking for variable-based static evaluation
- Decimal fixture `number-formatting-decimal.ts` with non-integer values

Validation:

- `env TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli number_formatting_matches_node` => pass
- `cargo nextest run number_formatting --no-fail-fast` => 2/2 passed
- `cargo nextest run build_smoke --no-fail-fast` => 308/308 passed
- `cargo fmt --all --check` => pass
- `mise run gate-fast` => pass
