Id: I-20260513-T27KTA
LegacyId: 458
Status: open
Priority: P1
Labels: type:feature area:runtime
Owner:
BlockedReason:
Created: 2026-05-13T10:03:15+09:00
Updated: 2026-05-13T17:13:01+09:00
Title: Runtime: Function.prototype bind, call, apply
Summary: Implement Function.prototype.bind (with partial application and new-target preservation), Function.prototype.call (thisArg, args), Function.prototype.
Next: Add runtime-backed function objects for escaped bind/call/apply values and bound function reuse.
---

## Acceptance
- cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_function_bind_call_apply
- cargo nextest run -p ts2wasm-cli --test m2_node_diff function_bind_call_apply_matches_node_output

## Notes

- 2026-05-13: Added static lowering for direct function declaration `fn.call(thisArg, ...)`, `fn.apply(thisArg, arrayLiteralOrDenseArrayLocal)`, and immediate `fn.bind(thisArg, ...bound)(...)` calls. Receiver-aware functions now receive the supplied thisArg through the existing function signature path.
- 2026-05-13: Added static local tracking for escaped bound functions (`const f = fn.bind(...); f(...)`) when the bound target is a known function declaration. Calls expand the captured receiver and partial arguments at the call site.
- Remaining: generic `Function.prototype.call/apply/bind` values, constructor/new-target preservation, and reusable runtime-backed bound function objects still need function object support.

## Evidence

- `cargo fmt --all --check` => pass
- `cargo check -p ts2wasm-ir -p ts2wasm-cli` => pass
- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_function_bind_call_apply` => pass
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff function_bind_call_apply_matches_node_output` => pass
- `mise run fmt && mise run clippy && mise run check && mise run issue-lint && git diff --check && mise run nextest` => pass (`2044` passed, `10` skipped)
- RED: `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_function_bind_call_apply` failed after adding escaped `bind` calls because `sum3.bind(...)` lowered as an unknown receiver method.
- RED: `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff function_bind_call_apply_matches_node_output` failed with the same build diagnostic before static bound-local tracking.
- `cargo check -p ts2wasm-ir -p ts2wasm-cli` => pass.
- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_function_bind_call_apply` => pass.
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff function_bind_call_apply_matches_node_output` => pass.
- `rg --no-ignore -n 'function-bind-call-apply|bound_function_locals|BoundFunction|boundSum|boundBase' fixtures crates scripts docs README.md AGENTS.md artifacts issues` => pass.
- `sg run --lang rust -p 'assert_fixture_matches_node($$$)' crates/cli/tests` => pass.
- `mise run fmt` => pass.
- `mise run check` => pass.
- `python scripts/issue-lint.py` => pass.
- `git diff --check` => pass after fixing `issue-status` trailing whitespace in the empty `BlockedReason` header.
- `mise run clippy` => pass.
- `mise run nextest` => pass (`2080` passed, `10` skipped).
