Id: I-20260513-ZWBXQE
Status: done
Priority: P2
Labels: area:ir type:feature
Owner:
BlockedReason:
Created: 2026-05-13T13:30:17+09:00
Updated: 2026-05-13T15:12:11+09:00
Title: Phase 2 (PR 2): Function self-reference in body
Summary: function assert(x){ return assert; } fails with unresolved name. Bind function name to scope before body lowering.
Next:
---

## Acceptance
- cargo test -p ts2wasm-ir -p ts2wasm-compiler
- cargo nextest run
- `function assert(x){ return assert; }` が build pass
- `function assert(x){ return assert._toString(x); }` が build pass
- 既存 resolver/semantic tests が全 regression なし

## Notes
- Part of plans/457-harness-compiler-gaps.md (Epic v2)
- PR 2 in the 6-PR sequence (Phase 2)
- Must preserve shadowing: `function f(f) { return f; }` → parameter wins
- Primary: crates/ir/src/lowered/resolver/expr/control.rs
- Also: crates/ir/src/semantic.rs if HIR phase also needs fix
- Target: function name pre-bound to scope before body lowering
- If FunctionRef IR unavailable, document as technical debt
- Not just Undefined fallback: actual scope binding preferred
- 2026-05-13: Added top-level function self binding during lowered function body construction, preserving parameter shadowing. `return assert;` now builds; `assert._toString(x)` remains blocked because function object property method dispatch is resolved while the function body is lowered, before later `assert._toString = function ...` assignments can populate `object_function_props`.
- 2026-05-13: Preindexed top-level `functionName.property = function(...) { ... }` assignments before function body lowering and attached the method ids to the function self-closure local. This lets self-body calls such as `assert._toString(x)` lower to direct user function calls without falling back to an unknown receiver diagnostic.

## Evidence
- cargo test -p ts2wasm-ir -p ts2wasm-compiler: pass; cargo nextest run: pass


- RED: `cargo run -q -p ts2wasm-cli -- build /tmp/self.ts -o /tmp/self.wasm` for `function assert(x){ return assert; }` failed with `UnresolvedName` before self binding.
- RED/blocker: `cargo run -q -p ts2wasm-cli -- build /tmp/self_method.ts -o /tmp/self_method.wasm` for `function assert(x){ return assert._toString(x); }` still fails with issue-211 unknown receiver class for `_toString`; next work needs delayed function property method resolution or pre-indexed function object properties.
- RED before fix: `cargo run -q -p ts2wasm-cli -- build /tmp/self_method.ts -o /tmp/self_method.wasm` for `function assert(x){ return assert._toString(x); }` with later `assert._toString = function(x){ return x; };` failed with issue-211 unknown receiver class for `_toString`.
- `cargo nextest run -p ts2wasm-cli --test ir_lowering lowering_binds_function_name_inside_own_body lowering_preserves_parameter_shadowing_over_function_self_name` => pass.
- `cargo run -q -p ts2wasm-cli -- build /tmp/self.ts -o /tmp/self.wasm` for `function assert(x){ return assert; }` => pass.
- `cargo nextest run -p ts2wasm-cli --test ir_lowering lowering_preindexes_function_self_property_method_assignment` => pass.
- `cargo run -q -p ts2wasm-cli -- build /tmp/self_method.ts -o /tmp/self_method.wasm` for `function assert(x){ return assert._toString(x); }` with later `assert._toString = function(x){ return x; };` => pass.
- `cargo nextest run -p ts2wasm-cli --test ir_lowering lowering_binds_function_name_inside_own_body lowering_preserves_parameter_shadowing_over_function_self_name lowering_hoists_direct_eval_block_function_to_enclosing_function_scope` => pass.
- `cargo test -p ts2wasm-ir -p ts2wasm-compiler && mise run fmt && mise run clippy && mise run check && python scripts/issue-lint.py && git diff --check && mise run nextest` => pass.
