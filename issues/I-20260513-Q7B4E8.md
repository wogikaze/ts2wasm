Id: I-20260513-Q7B4E8
LegacyId: 441
Status: open
Priority: P2
Labels: type:feature area:runtime
Owner: 
BlockedReason: 
Created: 2026-05-13T02:35:42+09:00
Updated: 2026-05-13T02:35:42+09:00
Title: Runtime: RegExp advanced features (named groups, lookahead, lookbehind, Unicode, s/dotAll, u/unicode, y/sticky, flags)
Summary: Implement advanced RegExp: named capture groups, lookahead/lookbehind assertions, Unicode property escapes, dotAll flag, sticky flag, index/indices wi
Next: Add named groups capture table and lookahead/lookbehind NFA simulation

---

## Acceptance
- [x] cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_regexp_advanced
- [ ] cargo nextest run -p ts2wasm-cli --test m2_node_diff regexp_advanced_matches_node_output

## Progress (2026-05-15)
### Completed
- Flag validation in `validate_regexp_plain_literal` (`crates/ir/src/lowered/program_builtins.rs`) updated to accept all lexer-supported flags (d, g, i, m, s, u, y) instead of only g/i
- Created `fixtures/builtins-and-io/regexp-advanced.ts` with dotAll (s), unicode (u), sticky (y), and multi-flag (gim) .test() calls
- Added `build_smoke_regexp_advanced` test — passes (PASS [0.016s])
- Validated: 347 test suite passes, no regressions
- Commits: 244f2a5ef

### Remaining for done
- WAT runtime semantics for s (dotAll): modify regexp match loop to skip \n exclusion when s flag is active
- WAT runtime semantics for u (unicode): UTF-8/UTF-16 code point handling
- WAT runtime semantics for y (sticky): lastIndex tracking for anchored matching
- WAT runtime semantics for m (multiline): line-boundary ^/$ anchors
- Named capture groups (?<name>...): capture table and backreference support
- Lookahead/lookbehind assertions: NFA simulation or backtracking
- Unicode property escapes (\p{...}): Unicode character database tables
- node_diff test (`regexp_advanced_matches_node_output`) for runtime-correct output

### Implementation notes
- The WAT regex engine (`crates/backend-wasm/src/runtime/regexp/emit.rs`) treats patterns as byte strings and doesn't parse flags. Each flag requires distinct WAT changes.
- Named groups and lookahead/lookbehind require fundamental changes to the matching algorithm (NFA simulation) since the current engine is a simple backtracking matcher without group capture state.
- Unicode support requires multi-byte character handling which conflicts with the byte-level string model.
