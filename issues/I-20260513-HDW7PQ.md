Id: I-20260513-HDW7PQ
LegacyId: 457
Status: open
Priority: P2
Labels: type:feature area:ir area:compiler area:tooling
Owner: 
BlockedReason: 
Created: 2026-05-13T10:03:15+09:00
Updated: 2026-05-15T15:10:37+09:00
Title: Phase 4 (PR 4): Enable real test262 harness loading
Summary: Remove inline stubs and always load harness from reference/test262/harness/
Next:
---

## Acceptance
- cargo test -p ts2wasm-ir -p ts2wasm-compiler
- grep -R '\$262\|Test262Error\|verifyProperty' crates/resolve/src/name_resolver.rs
- python3 scripts/manager.py reference-coverage test262 --jobs 1 --path-filter language/statements --limit 500
- grep -R 'unresolved_name_by_symbol.*total.*[0-9]' site/docs/coverage/web-ui/public/data/coverage.json

## Notes

- 2026-05-13: Added the currently asserted test262 harness support globals to the resolver known-global set so the focused resolver snapshot and broader nextest gate no longer fail on `testRunner`.
- This is a partial slice; the issue remains open because the full 60+ global/reference-coverage acceptance has not been completed.
- 2026-05-13: Re-ran the current acceptance slice. The resolver/compiler tests and current resolver grep pass, but the `language/statements` reference-coverage sample still reports `UnresolvedName:140` / `name-resolution:140` aggregated under `unknown`; keep this issue open for a follow-up that preserves concrete unresolved symbols in coverage output or targets the remaining cases directly.

## Evidence
- RED: `mise run nextest` => failed in `resolver_resolves_test262_harness_globals` because `testRunner` was unresolved.
- `cargo nextest run -p ts2wasm-resolve --test resolver_snapshot resolver_resolves_test262_harness_globals` => pass
- `cargo test -p ts2wasm-ir -p ts2wasm-compiler` => pass
- `grep -R '\$262\|Test262Error\|verifyProperty' crates/resolve/src/name_resolver.rs` => pass
- `python3 scripts/manager.py reference-coverage test262 --jobs 1 --path-filter language/statements --limit 500` => pass, `executed=500`, `semantic_pass=100`, `negative_compile_pass=100`, remaining `UnresolvedName:140`
- `grep -R 'unresolved_name_by_symbol.*total.*[0-9]' site/docs/coverage/web-ui/public/data/coverage.json` => pass, but only because top-level `total` appears later in the minified JSON; unresolved symbols remain aggregated as `unknown`

## False-done audit (2026-05-15)
- Reopened: Issue's own Notes say "the issue remains open" and "keep this issue open for a follow-up" but was batch-closed. `UnresolvedName:140` still unresolved. Done audit batch.
