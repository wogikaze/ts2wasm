Id: I-20260520-EA5MRA
Status: open
Priority: P2
Labels: area:infra
Owner: 
BlockedReason: 
Created: 2026-05-20T18:34:59+09:00
Updated: 2026-05-20T18:35:55+09:00
Title: Eliminate all architecture check violations without allowlists
Summary: architecture check: fix remaining source violations (function/file length, public API, match arms, push_str, span:None)
Next: mise run check architecture で ERROR/WARN ゼロを確認
---

## Notes

`mise run check architecture` が ERROR/WARN ゼロで通らない。ALLOWLIST による回避ではなく、ソースコード自体を修正する必要がある。

### 1. manifest 登録漏れ ✅ 対応済み
- 14 variants を `runtime-catalog/src/runtime/manifest/all.rs` に追加
- コミット: `d6d03729d`

### 2. 関数長超過 (200行超 ERROR 14件, 150行超 WARN ~36件)

**backend-wasm:**
- `runtime/link_plan.rs:239` — `collect_required_runtime_expr` (382行)
- `runtime/link_plan.rs:699` — `bigint_runtime_arithmetic_selects_helper_deps` (291行)

**compiler:**
- `stages/eval_expand.rs` — 6関数 (248-395行)
- `stages/lower.rs:1509` — `collect_literal_named_exports` (201行)

**ir:**
- `lowered/program.rs:4882` — `lower_function_with_resolved_params` (269行)
- `lowered/program_direct_eval.rs` — 2関数 (210-213行)
- `lowered/resolver/expr/mod.rs:474` — `lower_eval_completion_steps_into` (562行)
- `lowered/resolver/function.rs:533` — `lower_nested_function_with_receiver` (221行)

**対処**: 関数分割。内部ヘルパー抽出で200行以下に。

### 3. ファイル長超過 (1200行超 ERROR ~13件)

**backend-wasm (5ファイル):**
- `runtime/date/emit.rs` (1768行), `runtime/link_plan.rs` (1254行), `runtime/object/emit.rs` (1224行), `runtime/regexp/emit.rs` (1204行), `runtime/typed_arrays.rs` (1425行)

**compiler (2):** `stages/eval_expand.rs` (3325行), `tests.rs` (1967行)

**ir (4):** `builtin_resolved.rs` (2089行), `program_direct_eval.rs` (1693行), `resolver/expr/mod.rs` (2065行), `name_resolver_tests.rs` (1472行)

**cli/tests (2):** `host_deny.rs` (3076行), `node_shim_host.rs` (2571行)

**対処**: ファイル分割。ドメイン別に submodule 化。

### 4. public API 超過 (30超 ERROR 4件)
- `backend-wasm/src/runtime/date/emit.rs` (40個) — `pub(crate)` に変更可能
- `backend-wasm/src/runtime/typed_arrays.rs` (31個) — 同上
- `ir/src/builtin_resolved.rs` (64個) — 内部型の visibility 調整 (他 crate から参照されているため注意)
- `ir/src/lowered/facts.rs` (31個) — 同上

**対処**: 非公開でよいものは `pub(crate)` または private に。外部から必要なものは re-export で対応。

### 5. large match arms (50超 ERROR 8件, 30超 WARN ~18件)

**backend-wasm dispatch (6):**
- `dispatch_array.rs` (94臂), `dispatch_collections.rs` (60臂), `dispatch_date.rs` (92臂), `dispatch_core.rs` (75臂→分割済み), `link_plan.rs` (68臂)

**runtime-catalog (3):** `capability.rs` (59), `host_import.rs` (74+74), `link_plan.rs` (51)

**ir (1):** `resolver/expr/mod.rs` (51)

**対処**: サブドメイン別 dispatch 関数に分割。既存パターン: `dispatch_core.rs` の 75臂→9臂分割を参照。

### 6. push_str 850件 WARN 🚨 最大
`check_runtime_push_str()` が `sg` (ast-grep) で検出。全45ファイルに分散。WAT 文字列生成を構造化ビルダー (`WasmFunction` + `WatWriter`) に移行する必要がある。P4 プロジェクト相当。

**対処**: `runtime/core/typed.rs` に移行パターンの参考実装あり。各 emit 関数を `WasmFunction` ビルダーに順次置き換え。

### 7. span: None WARN (51件)
24ファイルに分散。`Diagnostic { span: None, ... }` パターン。内部エラー(InvariantViolation)は許容されるが、ソース位置情報が必要な箇所では span を追加する必要あり。

**対処**: 各 Diagnostic 生成箇所に適切な `Span` を追加。または `DiagCode::InvariantViolation` を使う。

### 8. その他 WARN
- `use super::*`  outside test (6件, 既知の legacy)
- `pub fn emit_wat` / `emit_wasm_binary_mvp` が `Validated<` 未使用 (2件, legacy)
- 巨大 issue ファイル (1件: `issues/I-20260514-67ZV8S.md`, 1581行)

## Acceptance

- [ ] `mise run check architecture` が ERROR 0、WARN 0、exit code 0 で完了する
- [ ] すべての修正は ALLOWLIST 追加ではなくコード自体の修正による
- [ ] `cargo build -p ts2wasm-backend-wasm` が通る

## Test-Requirements

- `mise run check architecture`
- `cargo build -p ts2wasm-backend-wasm -p ts2wasm-ir -p ts2wasm-compiler`
