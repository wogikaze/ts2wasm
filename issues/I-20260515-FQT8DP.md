Id: I-20260515-FQT8DP
Status: doing
Priority: P0
Labels: abi manifest backend
DependsOn: I-20260515-T8S7JT
Owner: claude
BlockedReason: 
Created: 2026-05-15T05:30:14+09:00
Updated: 2026-05-16T15:17:23+09:00
Title: Runtime ABI and Target Metadata in Artifacts
Summary: Add RUNTIME_ABI constants, emit ABI metadata in manifest and WASM custom section
Next:
---

## Problem

Coverage and manifests need to identify the runtime ABI and canonical target that produced a WASM module. Currently `crates/runtime-abi` has constants/layout contracts and compat snapshots, but generated manifest JSON and WASM do not expose ABI version or canonical target ID. Target strings are inconsistent (`wasm32-wasi`, `wasm32-wasi-p1`, `wasm32-wasi+node-host`, `wasm32-wasi-p1+node-shim`).

## Steps

1. Add `RUNTIME_ABI_NAME = "ts2wasm-runtime-abi"` and `RUNTIME_ABI_VERSION: u32` constants in `crates/runtime-abi`
2. Emit manifest fields `runtime_abi_name`, `runtime_abi_version`, `target_id`, `target_aliases`
3. Emit WASM custom section `ts2wasm.abi` with canonical JSON matching manifest metadata
4. Define canonical target IDs `wasm32-wasi-p1` and `wasm32-wasi-p1+node-shim`; preserve legacy target strings as aliases
5. Add tests comparing manifest and WASM metadata

## Acceptance

- [x] `RUNTIME_ABI_NAME` / `RUNTIME_ABI_VERSION` constants defined
- [x] Manifest includes `runtime_abi_name`, `runtime_abi_version`, `target_id`, `target_aliases`
- [x] WASM custom section `ts2wasm.abi` emitted matching manifest metadata
- [x] Canonical target IDs `wasm32-wasi-p1` and `wasm32-wasi-p1+node-shim` defined
- [x] Legacy aliases accepted
- [x] Existing manifest compatibility preserved through additive fields

```bash
cargo test -p ts2wasm-runtime-abi
cargo test -p ts2wasm-backend-wasm --test runtime_signature
cargo test -p ts2wasm-compiler --test manifest_snapshot
cargo test -p ts2wasm-shared capability
```

## Test-Requirements
- Test type: red_pending (open issue)
- Script: scripts/check/test-issue-fqt8dp.py
- Test command: python3 scripts/check/test-issue-fqt8dp.py
- Expected result: pass (RUNTIME_ABI_NAME/RUNTIME_ABI_VERSION implemented)
- Default gate: no (RED test)

## Non-goals

- Do not add WASI preview2, Component Model, or new memory layout
- Do not change existing manifest JSON structure

## Risk

WAT and encoder paths may diverge; centralize metadata emission and test both paths if both emit binaries.

## Source

Plan: `plans/next-architecture-design.md` REQ-ABI-001 + TASK-ABI-001 (P0)
Depends on: REQ-CAP-001
Files: `crates/runtime-abi/src/lib.rs`, `crates/shared/src/{abi,capability}.rs`, `crates/backend-wasm/src/{wasm_binary,wasm_encoder_backend,capability_manifest}.rs`, `crates/runtime-catalog/src/link_plan.rs`, `crates/backend-wasm/tests/runtime_signature.rs`, `crates/compiler/tests/manifest_snapshot.rs`, `docs/02-execution-model-and-targets.md`, `docs/14-runtime-abi.md`

## Notes

### 2026-05-16: Implementation complete

All 5 steps implemented and verified with 342 tests passing.

**Step 1: ABI constants**
- Added `RUNTIME_ABI_NAME = "ts2wasm-runtime-abi"` and `RUNTIME_ABI_VERSION = 2` in `crates/runtime-abi/src/lib.rs`

**Step 2: Manifest ABI fields**
- Added `runtime_abi_name`, `runtime_abi_version`, `target_id`, `target_aliases` to `CapabilityManifest` in `crates/shared/src/capability.rs`
- Added `RUNTIME_ABI_NAME` / `RUNTIME_ABI_VERSION` constants to `crates/shared/src/capability.rs`
- Populated fields in `new_wasi()` and `require_node_host()`
- Updated `canonical_manifest_from_link_plan` in `crates/backend-wasm/src/capability_manifest.rs`
- Existing `target` field preserved unchanged for backward compatibility

**Step 3: WASM custom section**
- Added `append_custom_section()` function in `crates/backend-wasm/src/wasm_binary.rs`
- Modified `write_output.rs` to accept optional `AbiMetadata` and append `ts2wasm.abi` custom section
- Threaded `ExecutionTarget` through `build_file_impl` to produce `AbiMetadata`
- Custom section contains JSON: `{"schema_version":1,"runtime_abi_version":2,"target":"wasm32-wasi-p1","target_profile":"wasi-p1","features":["wasi-preview1","standalone"],"generator":"ts2wasm"}`

**Step 4: Canonical target IDs (pre-existing)**
- `ExecutionTarget` in `crates/shared/src/abi.rs` already defines `manifest_target()`, `aliases()`, `target_profile()`, `features()`, `from_string()`

**Step 5: Tests**
- `wasm_binary_has_ts2wasm_abi_custom_section` — verifies custom section exists
- `abi_custom_section_matches_manifest` — verifies manifest and custom section metadata match
- `manifest_has_abi_fields` — verifies all new manifest fields
- `manifest_target_field_unchanged_for_backward_compat` — verifies backward compat preserved

### Files changed
- `crates/runtime-abi/src/lib.rs`: Added `RUNTIME_ABI_NAME`, `RUNTIME_ABI_VERSION` constants
- `crates/shared/src/capability.rs`: Added ABI fields to `CapabilityManifest`, constants
- `crates/backend-wasm/src/wasm_binary.rs`: Added `append_custom_section()`, made `encode_name_raw` public
- `crates/backend-wasm/src/capability_manifest.rs`: Populate `target_id`/`target_aliases` for node-shim
- `crates/backend-wasm/src/lib.rs`: Re-export `append_custom_section`
- `crates/compiler/src/io/write_output.rs`: Added `write_wasm_from_wat_with_abi()`
- `crates/compiler/src/pipeline.rs`: Threaded target → `AbiMetadata` → custom section
- `crates/compiler/tests/manifest_snapshot.rs`: Added 4 new tests

## Evidence
- cargo test -p ts2wasm-shared -p ts2wasm-runtime-abi -p ts2wasm-backend-wasm -p ts2wasm-compiler: 342 passed, 0 failed
- cargo fmt --all --check: pass
