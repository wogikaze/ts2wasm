Id: I-20260515-YW9CY9
Status: done
Priority: P0
Labels: type:architecture area:coverage area:scripts
Owner: 
BlockedReason: 
Created: 2026-05-15T03:44:01+09:00
Updated: 2026-05-15T06:40:06+09:00
Title: Architecture wave: coverage outcome schema v2
Summary: Unify coverage outcomes/statuses into schema v2 while preserving legacy status readers.
Next: 
---

## Acceptance
- Add a shared coverage outcome model with schema_version 2 fields: status, outcome, phase, semantic_checked, build_pass, tracking, reason, corpus evidence, selection hash, ABI version, and target ID.
- Keep legacy status readers working while ensuring mismatch, runtime_error, and oracle_skipped are outcomes rather than top-level status values.
- Validate schema v1 fallback and schema v2 strict examples for mismatch, runtime error, oracle skip, and negative compile cases.
- **Runtime/GC failure mode classification (REQ-RT-003)**:
  - Map iwasm trap, wasm validation failure after successful compile, and runtime ABI assertion messages to `outcome=runtime_error` unless the compiler itself panics (which is `internal_failure`).
  - Add `phase=runtime` for iwasm trap and `phase=abi` for metadata/layout mismatch if detected by ABI metadata gate.
  - Include first stderr line in triage sample but cap to 200 chars.
  - Add synthetic JSONL classification self-test for iwasm trap and wasm validation failure.
- Run: python -m py_compile scripts/lib/coverage_outcome.py scripts/run/reference-coverage.py scripts/lib/test262_harness.py scripts/check/test-records-schema.py
- Run: python scripts/check/test-records-schema.py --self-test
- Run: python scripts/lib/coverage_outcome.py --self-test
- Run: python scripts/report/coverage_triage.py --self-test
- Run: mise run reference-coverage -- test262 --jsonl --sample 1 --jobs 1 --no-semantic --no-dashboard-data
- Run: python scripts/check/test-records-schema.py artifacts/coverage/results/test262-results.jsonl

## Notes
- Source: plans/next-architecture-design.md REQ-COV-001; plans/next-architecture-design (1).md REQ-COV-001; plans/next-architecture-design (2).md REQ-COV-001; plans/next-architecture-design (3).md REQ-COV-001; plans/next-architecture-design (4).md REQ-COV-001.
- Suggested order: do this before deterministic triage reports and semantic coverage gates.

## Evidence
- coverage outcome schema v2 arch done
