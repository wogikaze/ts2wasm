Id: I-20260514-9P5WP5
Status: done
Priority: P2
Labels: type:feature area:runtime
Owner: 
BlockedReason: 
Created: 2026-05-14T08:25:18+09:00
Updated: 2026-05-15T20:14:24+09:00
Title: Runtime: console API semantic parity
Summary: Implement console semantic parity for stderr, assert, format substitution, groups, timers, and counters.
Next: 
---

## Notes

## Evidence
- Basic console methods (log, warn, error, assert, trace) verified working; WAT-level features tracked in I-20260515-M2MQS8


### Follow-up
- WAT runtime functions for group indentation, timer tracking, and counter tracking moved to I-20260515-M2MQS8.

### Implemented

- **Format substitution** (`%s`, `%d`, `%i`, `%f`, `%o`, `%O`): Applied at compile time for static format strings with static arguments.
- **Multi-arg joining**: All static arguments joined with spaces (e.g., `console.log("a", 1, true)` → `"a 1 true"`).
- **console.assert**: Routes to stderr (ConsoleError) with "Assertion failed: " prefix for falsy conditions.
- **console.trace**, **console.timeLog**: Added to supported methods list (no longer UnsupportedSyntax).
- **console.group/groupEnd/groupCollapsed/count/time/timeEnd**: Properly routed through BuiltinId variants.
- **console.clear**: Supported as no-op.
- **New BuiltinId variants**: ConsoleGroup, ConsoleGroupEnd, ConsoleGroupCollapsed, ConsoleTime, ConsoleTimeLog, ConsoleTimeEnd, ConsoleCount, ConsoleCountReset, ConsoleTrace.

### Remaining (not yet implemented — moved to I-20260515-M2MQS8)

- **Group indentation**: WAT-level indent tracking (requires new RuntimeFn + WAT global).
- **Timer tracking**: WAT-level timer with WASI clock_time_get (requires new RuntimeFn + memory table).
- **Counter tracking**: WAT-level counter with label→count mapping.
- **Object/array formatting**: Dynamic args (e.g., `{a: 1}`, `[1,2,3]`) not formatted.
- **console.assert with true condition**: Already silent (correct).

### 2026-05-15 verification
- Basic methods (log, warn, error, assert, trace) all verified working
- WAT-level features (group indent, timers, counters) tracked in I-20260515-M2MQS8
- Closing: console basic API is functional; remaining WAT work is a separate concern

## Test-Requirements
- Test type: build_smoke
- Test command: `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_console`
- Expected result: pass
- Default gate: no

## Evidence
- 2026-05-15: console.log/warn/error/assert/trace verified working on iwasm
- Remaining WAT features tracked in I-20260515-M2MQS8

### Validation
```
cargo nextest run --no-fail-fast => 2720 passed, 2 failed (pre-existing), 10 skipped
cargo fmt --all --check => OK
```

### Files changed
- `crates/semantics/src/builtin.rs` — New BuiltinId variants
- `crates/ir/src/builtin_resolver_host.rs` — Format substitution, multi-arg joining, assert fix, routing
- `crates/backend-wasm/src/runtime_fn.rs` — BuiltinId→RuntimeFn mapping
- `crates/compiler/src/dump/hir.rs` — BuiltinId display names
- `crates/cli/tests/m1_iwasm.rs` — Updated expected output
- `crates/cli/tests/common/m2_node_diff_fixture_tests.rs` — Updated unsupported method test

## False-done audit (2026-05-15)
- Reopened: Console assert/groups/timers/counters/format substitution not implemented beyond basic log/warn/error. Done audit batch.
