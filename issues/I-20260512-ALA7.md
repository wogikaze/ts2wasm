Id: I-20260512-ALA7
OldId: 366
Status: done
Priority: P1
Labels: test cross P14-final-test-strategy
Created: 2026-05-12
Updated: 2026-05-12
Title: Add boundary test layers for parser, resolver, builtin, HIR, MIR, link plan, manifest, and differential execution
Summary: Ensure every architecture boundary has focused tests, not only end-to-end differential tests.
Next: Ensure every architecture boundary has focused tests, not only end-to-end differential tests.

---

## Acceptance
- cargo test --workspace
- grep -R "parser_snapshot" crates/frontend/tests tests
- grep -R "resolver_snapshot" crates/resolve crates/ir/tests tests
- grep -R "builtin.*contract" crates/semantics crates/ir/tests tests
- grep -R "hir.*snapshot" crates/ir/tests tests
- grep -R "mir.*snapshot" crates/ir/tests tests
- grep -R "runtime_link_plan" crates/runtime-catalog crates/backend-wasm/tests crates/compiler/tests
- grep -R "manifest.*snapshot" crates/compiler/tests tests
- grep -R "differential" tests crates/cli/tests

## Non-goals
- No broad fixture rewrite.
- No new JS feature support.

## Notes
This makes final architecture refactors safe without reading the whole system.

## Evidence
- Commit: `5418d95bb`
- `cargo test -p ts2wasm-resolve -p ts2wasm-semantics 2>&1 | tail -10` → exit 0
- `rg --no-ignore "parser_snapshot" crates/frontend/tests/ tests/` → exit 0
- `rg --no-ignore "resolver_snapshot" crates/resolve/ crates/ir/tests/ tests/` → exit 0
- `rg --no-ignore "builtin.*contract" crates/semantics/ crates/ir/tests/ tests/` → exit 0
- `rg --no-ignore "hir.*snapshot" crates/ir/tests/ tests/` → exit 0
- `rg --no-ignore "mir.*snapshot" crates/ir/tests/ tests/` → exit 0
- `rg --no-ignore "runtime_link_plan" crates/runtime-catalog/ crates/backend-wasm/tests/ crates/compiler/tests/` → exit 0
- `rg --no-ignore "manifest.*snapshot" crates/compiler/tests/ tests/` → exit 0
- `rg --no-ignore "differential" tests/ crates/cli/tests/` → exit 0
- `git diff --check` → exit 0
