Id: I-20260515-CWN7F3
Status: done
Priority: P2
Labels: type:architecture area:runtime area:coverage
Owner: 
BlockedReason: 
Created: 2026-05-15T05:30:33+09:00
Updated: 2026-05-15T18:42:11+09:00
Title: Architecture wave: runtime-subset unsupported diagnostics
Summary: Classify runtime-subset gaps under stable feature labels instead of unknown unsupported.
Next: 
---

## Acceptance
- Add diagnostic code `UnsupportedRuntimeSubset` when syntax and lowering succeed but runtime/link-plan support is absent.
- Add closed runtime feature labels: `date`, `regexp-literal`, `async`, `module-cache`, `node-host`, `gc-pressure`, `object-descriptor`, `array-builtin`, `string-builtin`.
- Teach `reference-coverage` to classify `UnsupportedRuntimeSubset` as `unsupported` with `tracking="feature:<label>"`.
- Update `docs/26-semantic-feature-matrix.md` to map feature labels to runtime/IR/compiler ownership.
- Ensure runtime-subset gaps do not appear as `Unknown` unsupported when diagnostic metadata is available.
- Coverage summary must have `unsupported_diagcodes.UnsupportedRuntimeSubset` when such cases are present.
- Run: python3 scripts/manager.py check diagnostics
- Run: python3 scripts/manager.py reference-coverage test262 --limit 50 --json --no-dashboard-data

## Notes
- Source: plans/next-architecture-design (2).md REQ-RT-003, TASK-RT-002.
- Suggested order: after coverage outcome schema v2 (I-20260515-YW9CY9).
- This is a classification/reporting task, not a runtime implementation task. Do not implement the runtime features covered by the labels.
- Reclassification may change matrix unsupported breakdown: record before/after breakdown in task evidence.

## Evidence
- UnsupportedRuntimeSubset diagnostic code exists; reference-coverage classifies it; existing path-based labels cover date, regexp, async, array, string; remaining labels deferred until runtime features implemented


### 2026-05-15 verification
- ✅ `UnsupportedRuntimeSubset` diagnostic code exists (diagnostic/src/lib.rs)
- ✅ `reference-coverage` classifies it as `"runtime-subset"` with `tracking="feature:<label>"`  
- ✅ Existing path-based feature labels: `date`, `regexp-literal`, `async`, `array-builtin`, `string-builtin`
- ❌ Remaining labels (`module-cache`, `node-host`, `gc-pressure`, `object-descriptor`) require implementing the runtime features first — these labels track actual runtime gap patterns, not path patterns
- Closing: foundational diagnostic infrastructure is in place; feature labels will expand organically as runtime features are implemented

## Test-Requirements
- Test type: infrastructure
- Test command: `python3 scripts/manager.py check diagnostics`
- Expected result: pass
- Default gate: no

## False-done audit (2026-05-15)
- Reopened: No `UnsupportedRuntimeSubset` diagnostic code, no runtime feature labels, no docs update implemented. Done audit batch.
