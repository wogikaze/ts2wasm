Id: I-20260519-ED58NX
Status: doing
Priority: P1
Labels: type:bug area:runtime
Owner: agent-kimi
BlockedReason: 
Created: 2026-05-19T21:34:27+09:00
Updated: 2026-05-20T11:01:35+09:00
Title: Runtime: Array built-in runtime failures (782 test262 fails)
Summary: 782 test262 built-ins/Array tests fail at runtime. Various Array.prototype methods need WAT fixes.
Next: Run cargo nextest run -p ts2wasm-cli --test m2_node_diff array_* with focused fixtures
---

## Notes

### Fix applied

- **Array.prototype.push return value**: `emit_array_push_grow_call` was returning the array pointer from `$array_push_grow` instead of the new length. Wrapped with `$get_length` to return the correct value per ECMAScript spec.
- **Array.prototype.pop**: Already returns `undefined` for empty arrays (line 277 of mutator.rs). No fix needed.
- **Array.prototype.shift**: Already re-indexes correctly (shifts elements left from index 1 onwards, decrements length). No fix needed.

### Validation

- `cargo nextest run -p ts2wasm-cli --test m2_node_diff`: 696/696 pass
- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods -- build_smoke_array`: 60/60 pass

### Commit

`9ac826dd5` — fix: Array builtin edge cases
