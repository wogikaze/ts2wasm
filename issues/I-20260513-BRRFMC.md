Id: I-20260513-BRRFMC
Status: open
Priority: P1
Labels: area:runtime type:epic coverage:builtin-api
Owner:
BlockedReason:
Created: 2026-05-13T00:26:24+09:00
Updated: 2026-05-13T08:16:06+09:00
Title: Epic 1: Builtin API Coverage Expansion
Summary: Expand builtin functions: Math, JSON, Object, Array, String. Target: test262 semantic +100 pass.
Next: Continue builtin API coverage with the next missing runtime-backed fixture or focused test262 failure.
---

## Notes

- 2026-05-13: Added project fixture coverage for existing `Object.hasOwn` runtime lowering.
- Fixture: `fixtures/builtins-and-io/object-has-own.ts`.
- Wired build smoke and Node differential tests so static `Object.hasOwn` coverage is tracked beside `hasOwnProperty`.

## Evidence

- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_has_own` => pass
- `cargo nextest run -p ts2wasm-cli --test m2_node_diff object_has_own_matches_node` => pass
- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods` => pass
- `cargo nextest run -p ts2wasm-cli --test m2_node_diff object_builtin_method_fixtures_match_node_output_under_iwasm` => pass
- `mise run clippy && mise run check && mise run issue-lint && git diff --check && mise run nextest` => pass
