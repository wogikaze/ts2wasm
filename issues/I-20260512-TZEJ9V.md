Id: I-20260512-TZEJ9V
LegacyId: 354
Status: done
Priority: P1
Labels: refactor ir P14-ir-contracts
DependsOn: I-20260512-4ZT9NT
Owner: 
BlockedReason: 
Created: 2026-05-12T12:00:00+09:00
Updated: 2026-05-12T23:38:39+09:00
Title: Complete HIR to MIR lowering for the currently supported feature surface
Summary: Make HIR/MIR operational for the feature surface already supported by Lowered IR: constants, locals, primitive JS operations, property operations, met
Next: 
---

## Acceptance
- cargo test -p ts2wasm-ir -p ts2wasm-backend-wasm -p ts2wasm-compiler
- grep -R "lower_hir_to_mir" crates/ir/src crates/compiler/src
- grep -R "RuntimeCall" crates/ir/src/lowered/types.rs → found (variant defined with RuntimeFn field)
- grep -R "RuntimeFn" crates/ir/src/lowered/hir_to_mir.rs → found (used in RuntimeCall lowering)
- mise run check architecture

## Non-goals
- No full ECMAScript feature migration.
- No removal of LoweredProgram in this issue.

## Plan files
- `crates/ir/src/lowered/hir.rs`
- `crates/ir/src/lowered/mir.rs`
- `crates/ir/src/lowered/hir_to_mir.rs`
- `crates/ir/src/lowered/lower.rs`
- `crates/compiler/src/stages/lower.rs`
- `crates/ir/tests/hir_to_mir.rs`

## Notes
This makes HIR/MIR a real phase boundary instead of a design-only skeleton.

## Evidence
- cargo test -p ts2wasm-ir -p ts2wasm-backend-wasm -p ts2wasm-compiler -p ts2wasm-cli: all pass. grep RuntimeCall + RuntimeFn + lower_hir_to_mir: found. mise run check architecture: OK. All 6 HirStmt + 15 HirExpr variants lowered.

- cargo test -p ts2wasm-ir -p ts2wasm-backend-wasm -p ts2wasm-compiler: all 15 tests pass
- lower_hir_to_mir call chain: lower.rs -> hir_to_mir.rs -> re-exported from lowered.rs -> lib.rs
- RuntimeCall variant defined in types.rs with RuntimeFn field
- RuntimeFn used in hir_to_mir.rs (PropertyGet, TruthyBool lowerings)
- mise run check architecture: OK (warnings only for pre-existing push_str patterns)
- All 6 HirStmt variants and all 15 HirExpr variants are lowered in hir_to_mir.rs
