Id: I-20260513-V832BD
LegacyId: 460
Status: done
Priority: P1
Labels: type:feature area:runtime
Owner:
BlockedReason:
Created: 2026-05-13T10:03:15+09:00
Updated: 2026-05-13T17:34:47+09:00
Title: Runtime: String.prototype supplementary methods (normalize, localeCompare, matchAll, replaceAll advanced, isWellFormed, toWellFormed, trimStart, trimEnd, padStart, padEnd, repeat)
Summary: Implement remaining String.prototype methods: Unicode normalization, locale-sensitive comparison, String.prototype.matchAll (with regexp[@@matchAll]),
Next:
---

## Acceptance
- cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_string_supplementary
- cargo nextest run -p ts2wasm-cli --test m2_node_diff string_supplementary_matches_node_output

## Notes

- 2026-05-13: Added acceptance aggregate tests for supplementary string fixtures and a focused ASCII `String.prototype.normalize` fixture. Static ASCII receivers now lower `normalize()` / `normalize("NFC"|"NFD"|"NFKC"|"NFKD")` as identity, which is spec-correct for ASCII. Non-ASCII Unicode normalization was split to `I-20260513-DCRMZJ` because it needs a dedicated table/runtime strategy.

## Evidence

- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_string_supplementary` => pass.
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff string_supplementary_matches_node_output` => pass.
- `mise run nextest` => pass.
- RED: `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_string_supplementary` failed before lowering support because `String.prototype.normalize` was unsupported.
- RED: `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff string_supplementary_matches_node_output` failed before lowering support because `string-normalize.ts` could not build.
- `cargo fmt --all` => pass.
- `cargo check -p ts2wasm-ir -p ts2wasm-cli` => pass.
- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_string_supplementary build_smoke_string_normalize_method` => pass.
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff string_supplementary_matches_node_output string_builtin_fixtures_match_node_output_under_iwasm` => pass.
- `mise run fmt` => pass.
- `mise run check` => pass.
- `python scripts/issue-lint.py` => pass.
- `rg --no-ignore -n 'string-normalize|String\.prototype\.normalize|build_smoke_string_supplementary|string_supplementary_matches_node_output' fixtures crates scripts docs README.md AGENTS.md artifacts issues` => pass.
- `sg run --lang rust -p 'assert_fixture_matches_node($$$)' crates/cli/tests` => pass.
- `sg run --lang rust -p 'TestRecord { $$$ }' crates/cli/tests crates/shared/src` => pass.
- `git diff --check` => pass.
- `mise run clippy` => pass after removing a needless `Ok(...?)` wrapper.
- `mise run nextest` => pass (`2083` passed, `10` skipped).
