Id: I-20260515-66P4M5
Status: done
Priority: P1
Labels: type:architecture area:coverage area:gate
Owner: 
BlockedReason: 
Created: 2026-05-15T03:44:01+09:00
Updated: 2026-05-15T07:58:12+09:00
Title: Architecture wave: semantic coverage accounting gates
Summary: Make gates enforce semantic/build-only accounting, schema version, evidence, and runner parity.
Next: 
---

## Acceptance
- Add outcome-aware counters so build_pass/build_only/oracle_skipped never increment semantic_pass.
- Add coverage gate checks for schema version, unknown unsupported growth, required evidence keys, semantic regression, and fail-count growth.
- Preserve existing coverage matrix delta and shard regression modes.
- Add a deterministic comparison path for server-mode and legacy subprocess outcome classification where applicable.
- **Runner parity checker (REQ-COV-003)**:
  - Add deterministic paths file `scripts/data/coverage-parity-test262.txt` with 20 cases covering semantic match, unsupported parser, unsupported name resolution, build-only, verified negative compile, blocked-like metadata, and runtime mismatch if available.
  - Add `scripts/check/coverage-runner-parity.py` that runs server and legacy modes with the same paths file.
  - Compare `suite`, `case`, `status`, `outcome`, `diagnostic_code`, `feature`, and `tracking`; ignore timing/log fields.
  - Print first 10 mismatches with field-level diff and exit nonzero.
  - Add manager alias `check coverage-parity`.
  - Use REQ-REF-001 prerequisite checker for missing reference corpus.
  - Add self-test with synthetic JSONL mismatch.
- Run: mise run reference-coverage -- test262 --jsonl --sample 1 --jobs 1 --no-semantic --no-dashboard-data
- Run: python scripts/gate/coverage.py --shards --jsonl-file artifacts/coverage/results/test262-results.jsonl --schema-version 2 --max-unknown-unsupported 999999
- Run: python scripts/gate/coverage.py --check-regression --jsonl-file artifacts/coverage/results/test262-results.jsonl --schema-version 2
- Run: mise run update-coverage-matrix -- --check
- Run: `python scripts/check/coverage-runner-parity.py --self-test`
- Run: `python scripts/check/coverage-runner-parity.py --suite test262 --paths-file scripts/data/coverage-parity-test262.txt`

## Notes
- Source: plans/next-architecture-design.md REQ-COV-003; plans/next-architecture-design (1).md REQ-COV-003; plans/next-architecture-design (2).md REQ-COV-003; plans/next-architecture-design (4).md REQ-COV-003.
- Suggested order: after coverage outcome schema v2, triage reports, and reference selection evidence.

## Evidence
- Outcome-aware counters in compute_shard_metrics. Coverage gate with --schema-version, --max-unknown-unsupported, --require-evidence-key flags. scripts/check/coverage-runner-parity.py created with self-test (3 checks pass). scripts/data/coverage-parity-test262.txt created with 20 cases. Manager has check coverage-parity alias. Parity checker suite verification run in progress.

- Outcome-aware counters in compute_shard_metrics (gate/coverage.py). Coverage gate with --schema-version, --max-unknown-unsupported, --require-evidence-key flags. scripts/check/coverage-runner-parity.py created with self-test. scripts/data/coverage-parity-test262.txt created with 20 cases. Manager has check coverage-parity alias. Parity checker needs actual suite verification run.
