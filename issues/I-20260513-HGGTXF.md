Id: I-20260513-HGGTXF
Status: open
Priority: P2
Labels: type:techdebt area:architecture gate
Owner: codex
BlockedReason:
Created: 2026-05-13T21:16:45+09:00
Updated: 2026-05-14T21:50:28+09:00
Title: Architecture: split size-gate allowlisted modules
Summary: Refactor or shard the modules and manifests allowlisted by I-20260513-JMR4HH so architecture size gates can tighten without blocking feature work.
Next: Split fixtures/catalog.yaml and the named oversized Rust modules into domain shards, then remove their allowlist entries.
---

## Notes

- Created from `I-20260513-JMR4HH` to make each architecture allowlist entry removable.
- Current split targets:
  - `fixtures/catalog.yaml`: shard fixture metadata by domain while preserving `mise run check fixtures`.
  - `crates/ir/src/lowered/mir/`: continue moving bridge conversion dispatch toward the native MIR model issues `I-20260512-MRDATA` / `I-20260512-MRVADT`.
  - `crates/ir/src/lowered/resolver/call/method.rs::lower_mcall_array_runtime`: split array method cases into helpers or an array-method submodule.
  - `crates/backend-wasm/src/runtime/object/emit.rs`: split object runtime emission by descriptor/prototype/reflection responsibilities.
  - `crates/compiler/src/dump/`: split remaining dump/unparse dispatch into smaller AST/HIR/MIR domain helpers.
  - `crates/ir/src/lowered/resolver/mod.rs`: reduce facade exports after resolver domain modules own their APIs.
  - `crates/ir/src/lowered/resolver/expr/facts.rs`: move static fact helpers behind a smaller fact-context API.
  - `crates/backend-wasm/src/wat_writer.rs`: split WAT writer helpers by instruction family.
  - `crates/backend-wasm/src/wasm_encoder_backend.rs::build_single_function`: extract section builders.

### 2026-05-14 allowlist sync

Replaced stale architecture allowlist entries for removed split roots (`crates/compiler/src/dump.rs`, `crates/ir/src/lowered/mir.rs`) with current paths and narrower follow-up reasons. Also documented newly surfaced current baselines for `wat_writer.rs`, `wasm_encoder_backend.rs::build_single_function`, and `resolver/call/method.rs` helpers.

### 2026-05-14 encoder function split

Extracted `local_groups_for_function` from `crates/backend-wasm/src/wasm_encoder_backend.rs::build_single_function`, bringing `build_single_function` below the hard function-length gate and removing its temporary allowlist entry.

Validation:
- `mise run check architecture`
- `python3 -m py_compile scripts/check/architecture-rules.py`
- `mise run check scripts`
- `mise run check`
- `python3 scripts/issue-lint.py`

### 2026-05-14 WAT writer test split

Moved the inline `crates/backend-wasm/src/wat_writer.rs` test module into `crates/backend-wasm/src/wat_writer/tests.rs`, bringing the WAT writer source below the hard file-size gate and removing its temporary file-size allowlist entry. Remaining WAT writer allowlists now track public wrapper count and instruction dispatch shape rather than file size.

Validation:
- `mise run check architecture`
- `cargo fmt --all --check`
- `cargo nextest run -p ts2wasm-backend-wasm wat_writer`
- `cargo clippy -p ts2wasm-backend-wasm --tests -- -D warnings`
- `mise run check scripts`
- `python3 scripts/issue-lint.py`

### 2026-05-14 dump unparser split

Wired the existing `crates/compiler/src/dump/ast.rs` and `crates/compiler/src/dump/hir.rs` modules into `crates/compiler/src/dump/mod.rs` and removed the duplicate inline unparser implementations from the dispatcher. This brings `dump/mod.rs` below the hard file-size gate and removes its temporary file-size allowlist entry.

Validation:
- `mise run check architecture`
- `cargo fmt --all --check`
- `cargo check -p ts2wasm-compiler`
- `mise run check scripts`
- `python3 scripts/issue-lint.py`

### 2026-05-14 object runtime emitter split

Wired the existing `crates/backend-wasm/src/runtime/object/descriptor.rs` and `crates/backend-wasm/src/runtime/object/integrity.rs` modules into `crates/backend-wasm/src/runtime/object/mod.rs`, then removed the duplicate descriptor and integrity emitters from `crates/backend-wasm/src/runtime/object/emit.rs`. This brings `object/emit.rs` below the hard file-size gate and removes its file-size allowlist entries.

Validation:
- `mise run check architecture`
- `cargo fmt --all --check`
- `cargo check -p ts2wasm-backend-wasm`
- `mise run check scripts`
- `python3 scripts/issue-lint.py`

## Acceptance

- Each allowlist entry added by `I-20260513-JMR4HH` is removed or replaced by a narrower documented entry.
- `mise run check architecture` passes after removals.
- Focused tests for touched domains pass.
