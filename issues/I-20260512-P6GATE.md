Id: I-20260512-P6GATE
Status: done
Priority: P1
Labels: P16 semantic gate coverage ir
DependsOn: I-20260512-FNCSEM I-20260512-HRSCVR
Owner:
BlockedReason:
Created: 2026-05-12T15:00:00+09:00
Updated: 2026-05-13T22:11:56+09:00
Title: Establish P16 semantic correctness gate for default HIR to MIR pipeline
Summary: Freeze semantic baselines and blockers required before the default pipeline can use HIR to MIR emission.
Next:
---

## Notes

### Goal

Create the explicit go/no-go gate for making HIR -> MIR -> emit the default path.

### Acceptance

- Semantic canary suite passes or has documented blockers.
- Function/call semantic suite from `I-20260512-FNCSEM` passes or has documented blockers.
- HIR support matrix from `I-20260512-HRSCVR` is linked from docs.
- Reference coverage comparison records `build_pass`, `semantic_pass`, mismatch, runtime_error, and build_only deltas.
- Gate result is written to docs/current-state or the project tracking file used by the repo.

### Non-goals

- No native MIR emitter work.
- No default path switch.

## Evidence

- P16 gate snapshot recorded in docs/current-state.md; focused checks and repo gates completed

2026-05-13:

- Gate result recorded in `docs/current-state.md`: no-go for making HIR -> MIR -> emit the default path.
- Semantic canary: `mise run check fixture-differential -- --catalog /tmp/ts2wasm-semantic-canary-catalog.yaml` => fail, 19 pass / 1 stdout mismatch / 0 unsupported / 0 blocked. Blocker: `fixtures/builtins-and-io/global-names-promise-symbol-reflect-proxy.ts`.
- Function/call diagnostics-only suite: `cargo test -p ts2wasm-cli --test m2_node_diff -- fncsem` => pass, 20 passed.
- Function/call runtime differential suite: `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff -- fncsem` => fail, 16 passed / 4 failed. Blockers: diagnostic-code suffix expectation mismatch and `fixtures/core-semantics/builtin-call-hir.ts` stdout mismatch.
- HIR support matrix linked from `docs/27-ir-layer-completion.md#hir-support-matrix`; `cargo test -p ts2wasm-ir hir_` => pass, 85 passed.
- Reference coverage sample: `mise run reference-coverage -- test262 --jsonl --sample 50 --jobs 4 --no-dashboard-data` => pass, 299 executed, `build_pass=18`, `semantic_pass=18`, `mismatch=0`, `runtime_error=0`, `build_only=0`, `fail=32`, `unsupported=250`, `blocked=0`.
