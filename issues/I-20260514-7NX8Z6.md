Id: I-20260514-7NX8Z6
Status: done
Priority: P2
Labels: type:feature area:runtime
Owner:
BlockedReason:
Created: 2026-05-14T22:30:17+09:00
Updated: 2026-05-15T00:29:14+09:00
Title: Runtime: Date setter methods and remaining prototype completion
Summary: Implement remaining Date setter and complete Date prototype semantics split from I-20260513-P494YQ after Date.parse/UTC acceptance passed
Next:
---

## Notes

- Split from I-20260513-P494YQ after Date string methods, Date.parse, and Date.UTC acceptance passed.
- Cover Date setter methods such as setTime, setUTCFullYear, setUTCMonth, setUTCDate, setUTCHours, setUTCMinutes, setUTCSeconds, setUTCMilliseconds, and local-time setter counterparts.
- Add focused fixtures for build smoke and semantic behavior where host shims/runtime support make comparison possible.

### 2026-05-14 Date.prototype.setTime slice

- Added RuntimeFn/manifest/spec coverage for DateSetTime and WAT lowering that updates the Date epoch slot and returns the new timestamp.
- Lowered Date.prototype.setTime(value) for Date receivers.
- Added `fixtures/builtins-and-io/date-set-time.ts` with build-smoke and Node/iwasm differential coverage.
- Validation:
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_date_set_time`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff date_set_time_fixture_matches_node_output_under_iwasm`
  - `cargo check -p ts2wasm-runtime-catalog -p ts2wasm-backend-wasm -p ts2wasm-ir -p ts2wasm-cli`
  - `cargo nextest run -p ts2wasm-runtime-catalog`
  - `mise run check architecture`
  - `mise run check`
  - `mise run clippy`
### 2026-05-14 Date.setTime verification and next slice

- Verified Date.setTime is fully implemented: commit 254ede318
- All 15 Date build smoke tests pass, formatting clean
- Pushed feat/date-setters-7NX8Z6
- Remaining: setUTC*/set* component setters (setUTCFullYear, setUTCMonth, etc.) require C-level host shim functions in iwasm before RuntimeFn/HostImport/Capability chain can be wired. Suggest splitting into per-setter issues.

### 2026-05-14 Date.prototype.setUTCFullYear slice

- Added `RuntimeFn::DateSetUTCFullYear` and WAT lowering that rebuilds the UTC timestamp through existing `Date.UTC`/UTC getter runtime functions, stores the new epoch slot, and returns the new timestamp.
- Lowered `Date.prototype.setUTCFullYear(year[, month[, date]])` for Date receivers, including missing month/date defaults.
- Added `fixtures/builtins-and-io/date-set-utc-full-year.ts` with build-smoke and Node/iwasm differential coverage.
- Validation:
  - `cargo fmt --all --check`
  - `cargo check -p ts2wasm-runtime-catalog -p ts2wasm-backend-wasm -p ts2wasm-ir -p ts2wasm-cli`
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_date_set_utc_full_year`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff date_set_utc_full_year_fixture_matches_node_output_under_iwasm`
  - `mise run check architecture`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: setUTCMonth, setUTCDate, setUTCHours, setUTCMinutes, setUTCSeconds, setUTCMilliseconds, and local-time setter counterparts.

### 2026-05-14 Date.prototype UTC component setters slice

- Added `RuntimeFn`/manifest/spec coverage for `setUTCMonth`, `setUTCDate`, `setUTCHours`, `setUTCMinutes`, `setUTCSeconds`, and `setUTCMilliseconds`.
- Added WAT lowering that rebuilds the UTC timestamp through existing `Date.UTC`/UTC getter runtime functions, stores the Date epoch slot, and returns the new timestamp.
- Lowered the six Date receiver methods with JavaScript-compatible optional component defaults.
- Added `fixtures/builtins-and-io/date-set-utc-components.ts` with build-smoke and Node/iwasm differential coverage.
- Validation:
  - `cargo fmt --all --check`
  - `cargo check -p ts2wasm-runtime-catalog -p ts2wasm-backend-wasm -p ts2wasm-ir -p ts2wasm-cli`
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_date_set_utc_components`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff date_set_utc_components_fixture_matches_node_output_under_iwasm`
  - `mise run check architecture`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: local-time setter counterparts.

### 2026-05-14 Date.prototype UTC setter default-preservation fixture

- Registered `fixtures/builtins-and-io/date-set-utc-methods.ts` to cover the single-argument forms of `setUTCMonth`, `setUTCHours`, `setUTCMinutes`, and `setUTCSeconds`.
- The fixture checks that omitted trailing UTC components default from the receiver date rather than resetting to zero.
- Validation:
  - `cargo fmt --all --check`
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_date_set_utc_methods_defaults`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff date_set_utc_methods_defaults_fixture_matches_node_output_under_iwasm`
  - `cargo check -p ts2wasm-runtime-catalog -p ts2wasm-backend-wasm -p ts2wasm-ir -p ts2wasm-cli`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: local-time setter counterparts.

### 2026-05-15 Date.prototype local setter runtime catalog wiring

- Completed runtime catalog/spec/manifest coverage for local-time setter variants (`setFullYear`, `setMonth`, `setDate`, `setHours`, `setMinutes`, `setSeconds`, `setMilliseconds`) already added to the backend Date runtime.
- Validation:
  - `cargo check -p ts2wasm-runtime-catalog -p ts2wasm-backend-wasm`
- Remaining: lower Date receiver methods in IR and add fixture coverage for local setter behavior.

### 2026-05-15 Date.prototype local component setters slice

- Lowered Date receiver methods for `setFullYear`, `setMonth`, `setDate`, `setHours`, `setMinutes`, `setSeconds`, and `setMilliseconds`.
- Added `fixtures/builtins-and-io/date-set-local-components.ts` with build-smoke and Node/iwasm differential coverage.
- Validation:
  - `cargo test -p ts2wasm-cli build_smoke_date_set_local_components -- --exact`
  - `cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::date_set_local_components_fixture_matches_node_output_under_iwasm -- --exact`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: none for Date setter methods tracked by this issue.

## Evidence
- cargo test -p ts2wasm-cli build_smoke_date_set_local_components -- --exact; cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::date_set_local_components_fixture_matches_node_output_under_iwasm -- --exact; mise run check; mise run clippy; mise run nextest

- 15 Date build smoke pass, Date.setTime committed
- Date.setUTCFullYear build smoke and Node/iwasm differential pass.
- Date UTC component setter build smoke and Node/iwasm differential pass.
- Commit 749ff5ba4: all 17 Date build smoke, 3 Date node_diff, and 34 runtime-catalog tests pass.
- Date UTC setter default-preservation build smoke and Node/iwasm differential pass.
- Date local setter runtime catalog wiring compile-checks for runtime-catalog and backend-wasm.
- Date local component setter build smoke and Node/iwasm differential pass; `mise run nextest` passed.
