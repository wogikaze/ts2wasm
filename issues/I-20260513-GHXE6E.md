Id: I-20260513-GHXE6E
LegacyId: 482
Status: open
Priority: P1
Labels: type:feature area:runtime
Owner:
BlockedReason:
Created: 2026-05-13T10:03:15+09:00
Updated: 2026-05-13T11:41:10+09:00
Title: Runtime: Global object properties (globalThis, Infinity, NaN, undefined, isNaN, isFinite, parseFloat, parseInt, encodeURI, decodeURI, encodeURIComponent, decodeURIComponent, escape, unescape)
Summary: Implement all global object properties and functions. globalThis resolves to the global scope. URI encoding/decoding functions need proper UTF-8 handl
Next: Extend URI/escape helpers beyond current ASCII-scope behavior.
---

## Acceptance
- cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_global_properties
- cargo nextest run -p ts2wasm-cli --test m2_node_diff global_properties_matches_node_output

## Notes

- 2026-05-13: Replaced the host-import shims for ASCII-scope `encodeURI`, `decodeURI`, `escape`, and `unescape` with pure WAT runtime implementations.
- 2026-05-13: Added aggregate `build_smoke_global_properties` and `global_properties_matches_node_output` acceptance tests covering global numeric calls, zero-arg calls, function metadata, and URI/escape fixtures.
- 2026-05-13: Split `encodeURIComponent` / `decodeURIComponent` into distinct builtin and runtime functions so URI component helpers encode/decode reserved punctuation independently from `encodeURI` / `decodeURI`.
- 2026-05-13: Lowered `globalThis` to a runtime singleton object backed by a declared runtime global and GC root marking, so repeated reads preserve object identity instead of returning `undefined`.
- Remaining: URI/escape helpers are currently ASCII-scope and still need full UTF-8 / ECMAScript edge-case coverage.

## Evidence

- RED: `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_global_properties` => failed before this slice because the acceptance test did not exist.
- RED: `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff global_properties_matches_node_output` => failed before this slice because the acceptance test did not exist.
- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_global_properties` => pass
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff global_properties_matches_node_output` => pass
- `cargo nextest run -p ts2wasm-cli --test m11_host_deny standalone_global_uri_and_escape` => pass
- `mise run fmt && mise run clippy && mise run check && mise run issue-lint && git diff --check && mise run nextest` => pass (`2040` passed, `10` skipped)
- RED: `encodeURIComponent` / `decodeURIComponent` resolved to the `encodeURI` / `decodeURI` builtin IDs before this slice, so reserved punctuation component semantics were not represented.
- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_global_properties` => pass with `global-uri-component.ts`
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff global_uri_component_matches_node_under_iwasm global_properties_matches_node_output` => pass
- `cargo nextest run -p ts2wasm-cli --test m11_host_deny standalone_global_uri_and_escape` => pass with URI component fixture
- `mise run fmt && mise run clippy && mise run check && mise run issue-lint && git diff --check && mise run nextest` => pass (`2041` passed, `10` skipped)
- `cargo check -p ts2wasm-runtime-catalog -p ts2wasm-ir -p ts2wasm-backend-wasm -p ts2wasm-cli` => pass
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff global_this_matches_node_under_iwasm -- --nocapture` => pass
- `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_global_properties -- --nocapture` => pass
- `cargo test -p ts2wasm-runtime-catalog --lib emission_order -- --nocapture` => pass
- `mise run fmt && mise run clippy && mise run check && mise run issue-lint && git diff --check && mise run nextest` => pass (`2042` passed, `10` skipped)
