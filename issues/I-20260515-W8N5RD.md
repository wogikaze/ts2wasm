Id: I-20260515-W8N5RD
Status: open
Priority: P0
Labels: area:coverage area:pipeline type:infra
DependsOn: I-20260515-YV65RX I-20260515-ENB7EJ
Owner: 
BlockedReason: 
Created: 2026-05-15T16:16:26+09:00
Updated: 2026-05-15T16:16:26+09:00
Title: Enable semantic checking in test262 coverage pipeline
Summary: Semantic diff comparison is on by default but the parity run uses --no-semantic. Remove that flag to re-enable it.
Next: Drop --no-semantic from the parity coverage invocation and verify semantic_pass > 0

---

## Notes

### 2026-05-15 creation evidence

- From `artifacts/coverage/results/test262-summary.json`:
  - `semantic_enabled: false` — differential semantic comparison is completely disabled
  - `semantic_pass: 10`, `build_pass: 10`, `differential_pass: 10` (identical = no semantic check active)
  - Current invocation: `--no-semantic --no-server`
- Semantic checking is ON by default; `--no-semantic` disables it. The issue is simply that the parity invocation uses `--no-semantic`.
- Once enabled, all 18 current cases would need semantic pass to maintain parity.

## Acceptance
- [ ] `mise run reference-coverage -- test262 --paths-file scripts/data/coverage-parity-test262.txt --no-dashboard-data` (no `--no-semantic`) shows `semantic_enabled: true` and `semantic_pass > 0`
- [ ] `build_pass == semantic_pass` (all cases that build also pass semantic check)
- [ ] `differential_pass == semantic_pass` (all semantically checked cases match Node.js oracle)

## Test-Requirements
- Test type: command-contract
- Fixture path(s): scripts/data/coverage-parity-test262.txt
- Test command(s): mise run reference-coverage -- test262 --paths-file scripts/data/coverage-parity-test262.txt --no-dashboard-data
- Expected result: pass
- Default gate: yes

