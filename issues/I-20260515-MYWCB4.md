Id: I-20260515-MYWCB4
Status: open
Priority: P1
Labels: area:compiler feature:regexp feature:test262
DependsOn: I-20260514-67ZV8S
Owner:
BlockedReason:
Created: 2026-05-15T09:47:30+09:00
Updated: 2026-05-15T09:51:37+09:00
Title: Intl late RegExp constructor dynamic pattern gap
Summary: Intl taint harness now advances past captured NumberFormat.format and stops later on a dynamic RegExp constructor pattern.
Next: Inspect the late testIntl.js RegExp constructor source around locale validation and extend static pattern resolution or add a focused unsupported split.
---

## Notes

- Reproducer after `I-20260515-RTSHVA`: `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-triage -- test262 reference/test262/test/intl402/NumberFormat/taint-Object-prototype.js`.
- Current result: `[UnsupportedRegExp/lowering] issue-051: RegExp constructor pattern must be a string literal in this subset`.
- Representative triage reports visible symbols around the late `testIntl.js` locale / duration-format helper area, after captured `Intl.NumberFormat` method calls now lower.
- Investigation note: the original case only calls `taintProperties(...)` and `isCanonicalizedStructurallyValidLanguageTag(locale)`, but the Python test262 wrapper compiles the full `testIntl.js` helper. After `I-20260515-RTSHVA`, lowering advances into unused helper bodies and hits dynamic `new RegExp(...)` construction such as `new RegExp(oneoneRE)` in `testNumberFormat`.
- A blanket fallback that lowers dynamic RegExp patterns to `//` would turn previously unsupported dynamic regex semantics into silently wrong runtime behavior. Next action should choose between a targeted `testIntl.js` harness shim/pruning strategy or real dynamic RegExp constructor support.
