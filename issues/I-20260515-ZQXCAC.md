Id: I-20260515-ZQXCAC
Status: done
Priority: P1
Labels: feature:generators,feature:object-literal,area:semantics
DependsOn: I-20260514-67ZV8S
Owner:
BlockedReason:
Created: 2026-05-15T05:20:19+09:00
Updated: 2026-05-15T05:33:47+09:00
Title: Semantic fail: computed-yield object accessors need generator resume values
Summary: accessor-name-computed-yield-expr.js builds, but semantic execution fails on computed accessor resume values.
Next:
---

## Notes

- Follow-up from `I-20260515-VWP4WJ`.
- The focused case now builds, so the old capture/lowering blocker is gone.
- Added a specialized static resume plan for generator bodies that assign an object literal with direct computed-key `yield` expressions.
- Sequential `.next(value)` calls on statically tracked generator iterator locals now keep literal resume arguments long enough to rebuild the object literal with the resumed computed keys.
- The resumed object assignment reuses ordinary object-literal lowering, so static accessor metadata is available for following `obj.first` and `obj.second = ...` dispatch.
- `language/expressions/object/accessor-name-computed-yield-expr.js` now passes semantic reference-coverage.

## Evidence
- reference-coverage accessor-name-computed-yield-expr.js: Pass 1 Fail 0 Unsupported 0


- RED: `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_generator_computed_accessor_yield_resume_values_use_static_steps -- --exact --nocapture` => failed because `GeneratorNext` runtime calls were still emitted
- `cargo fmt --all`
- `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_generator_computed_accessor_yield_resume_values_use_static_steps -- --exact --nocapture` => pass
- `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli` => pass
- `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --detail --path-filter language/expressions/object/accessor-name-computed-yield-expr.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0
