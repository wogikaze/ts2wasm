Id: I-20260517-WE8P5A
Status: doing
Priority: P1
Labels: area:runtime type:feature feature:eval
Owner: codex
BlockedReason:
Created: 2026-05-17T09:03:56+09:00
Updated: 2026-05-18T12:47:49+09:00
Title: Implement eval and Function constructor support
Summary: Complete static AOT and audited host-lane eval/Function support; remaining work is canonical eval/function lowering and broader semantics.
Next: Implement the next canonical `EvalFragmentPlan` / eval-code environment slice with focused Node parity tests.
---

## Notes

- 2026-05-18 codex: Reopened as the active eval/Function implementation tracker. Dynamic eval and dynamic Function constructor are no longer classified as intentionally unsupported as a whole: supported runtime-source slices now lower to audited Node host lanes with exact `host.eval.*` / `host.function.*` manifest entries and host-deny checks. Remaining work is the canonical `EvalFragmentPlan` / `FunctionConstructorPlan` migration, direct-eval declaration landing, full TDZ modeling, runtime-wide host external object contracts, and broader test262/realm classification.
- 2026-05-18 codex: Static direct eval declaration landing now covers expression completion, eval-created `var` direct reads later in the same function body, eval-defined function reads of eval-defined vars, and host-lane dynamic declaration persistence for later direct eval calls. Remaining declaration work is broader lexical environment modeling and the canonical eval-code environment path.
- 2026-05-18 codex: `$262.evalScript(source)` now lowers through `Dollar262Eval` to the audited `host.eval.indirect` lane with focused Node-shim, manifest, and host-deny guards. Broader realm / cross-realm classification remains open.
- 2026-05-18 codex: Static literal `Function("...items", ...)` now lowers through the AOT Function constructor path, executes rest parameter calls under Node differential coverage, reports Node-compatible `.length === 0`, and remains host-deny standalone.
- 2026-05-18 codex: Static literal Function constructor parameter grammar now has Node differential coverage for defaults, destructuring, and `new Function` rest parameters with Node-compatible `.length` metadata.
- 2026-05-18 codex: Static literal Function constructor calls now preserve explicit object returns from `new F()` while primitive constructor returns still fall back to the allocated receiver, matching Node differential coverage.
- 2026-05-18 codex: Static literal Function constructor functions containing `this` now lower as receiver-aware functions so sloppy calls see `globalThis` and strict calls see `undefined`, matching focused Node differential coverage.
- 2026-05-18 codex: Static literal Function constructor functions containing `new.target` now receive a synthetic construct-only target token, so ordinary calls see `undefined` and `new F()` sees `typeof new.target === "function"` plus Node-compatible `.name` / `.length` metadata under Node differential coverage. Static Function constructor `arguments` object reads are also guarded.
- 2026-05-18 codex: Intrinsic `Function(...)` / `new Function(...)` builtin resolution now uses an explicit `ResolvedExpr::FunctionConstructor` IR node for both static AOT expansion and dynamic host-lane fallback instead of preserving magic identifier call/new shapes after builtin resolution.
- 2026-05-18 codex: `ResolvedExpr::Eval` now carries `EvalFragmentPlan` as an explicit plan object for kind/source/strict/span metadata, preparing the later scope/declaration/completion record migration without changing current AOT or host-lane behavior.
- 2026-05-18 codex: Removed parser-side direct-eval statement splicing and the `possible_eval_shadowing` heuristic. Static direct eval statements now preserve AST call shape through parser/name resolver and run through compiler `EvalCompletionStep` expansion; build/server/dump paths all run `expand_static_eval_fragments`.
- 2026-05-18 codex: Recursive static eval expansion now happens inside eval fragments, and EvalCompletion function declarations feed mutable-capture/env-cell planning so the guarded direct-eval block-function fixtures remain Node-differential green without parser splicing.
- 2026-05-18 codex: Static direct eval expansion now propagates strict caller context through nested function/class scopes. Strict caller eval-code `var` declarations stay eval-local under Node-shim, Node differential, and host-deny standalone guards.
- 2026-05-18 codex: Builtin resolution now annotates `EvalFragmentPlan.caller_is_strict` across script, function, class, arrow, and nested expression scopes so static expansion and dynamic host-lane lowering receive the same strict caller metadata.
- 2026-05-18 codex: Dynamic direct eval env descriptors now carry resolver-owned strict-caller metadata into the Node shim. Runtime-source direct eval in a strict caller keeps eval-created `var` declarations eval-local under focused Node-shim and host-deny guards.
- 2026-05-18 codex: Added a focused strict-caller runtime-source direct eval syntax guard for `delete Identifier`, which now bridges Node's catchable `SyntaxError` through the env-descriptor host lane.
- 2026-05-18 codex: Extended the strict-caller `delete Identifier` guard to reserved direct-eval-visible bindings such as `arguments`, avoiding the sloppy wrapper fallback when strict source references compatibility env bindings.
- 2026-05-18 codex: Strict-caller runtime-source direct eval now rejects source that binds `arguments` or `eval` before the Node shim falls back to compatibility env bindings, with focused `var arguments` coverage.
- 2026-05-18 codex: Added focused `function eval() {}` coverage for strict-caller runtime-source direct eval restricted bindings.
- 2026-05-18 codex: Extended strict-caller runtime-source direct eval restricted binding coverage to destructuring `var [arguments] = ...` and explicit async function declarations before compatibility fallback.
- 2026-05-18 codex: Extended the same strict-caller restricted binding guard to object destructuring value bindings such as `const {value: eval} = ...`, preserving allowed alias patterns for future coverage.
- 2026-05-18 codex: Dynamic direct eval results assigned into env-cell locals now retain host external object / host function handle facts, so function-valued properties can be called through the shared host.function call/method bridge.
- 2026-05-18 codex: Dynamic direct eval host-lane declaration persistence now extracts binding names from `var` destructuring patterns, so new object-pattern `var` bindings remain visible to later direct eval calls for the same caller env.
- 2026-05-18 codex: Added focused array-pattern coverage for the same runtime-source direct eval declaration persistence path, including rest bindings reused by a later direct eval call.
- 2026-05-18 codex: Extended dynamic direct eval host-lane function declaration persistence to generator declarations (`function* created() { ... }`), with later direct eval calls reusing the created binding.
- 2026-05-18 codex: Added async function declaration persistence coverage for runtime-source direct eval, verifying later direct eval calls can observe the created async function binding metadata.
- 2026-05-18 codex: Tightened dynamic direct eval host-lane function declaration collection so named function expressions are not persisted as eval-created bindings and do not shadow sloppy unbound global writes.
- 2026-05-18 codex: Added async/generator function expression name coverage for the same direct eval host-lane declaration collector boundary, keeping those expression-local names out of persisted eval bindings.
- 2026-05-18 codex: Replaced strict-caller runtime-source direct eval restricted-binding/delete regex scans with a code keyword scanner that skips string literals and comments, avoiding false SyntaxError results for text such as `"var arguments"` while preserving focused strict binding/delete guards.
- 2026-05-18 codex: Updated the reference test262 runner Node oracle prelude so `$262.evalScript(source)` performs indirect global eval instead of throwing an unsupported-stub error, aligning the runner with the audited `Dollar262Eval` / `host.eval.indirect` lowering slice.
- 2026-05-18 codex: Fixed JSONL reference coverage parity for `--no-server --no-semantic`: the legacy JSONL path now emits compile-only `build_pass` records instead of running Node oracle semantics, so `$262.evalScript` eval-code cases classify the same as server check mode after rebuilding the selected `ts2wasm` binary.
- 2026-05-18 codex: Updated the Node test262 oracle to execute non-module prepared sources with `vm.runInThisContext`, and routed `$262.evalScript` through the same script context helper. The focused Annex B `$262.evalScript` eval-code case now passes strict Node/iwasm semantic comparison.
- 2026-05-18 codex: Static direct eval completion now preserves multiple flattened Annex B block function declarations in execution order instead of dropping post-prefix declarations. `func-block-decl-eval-func-existing-block-fn-update.js` now passes strict Node/iwasm semantic comparison; the Annex B direct eval-code subset moved from 135 to 136 strict passes.
- 2026-05-18 codex: Refreshed `plans/eval-new-function-implementation-plan.md` to the archive15 support matrix and phase split. The plan now treats dynamic eval / dynamic Function as focused supported host-lane slices, keeps parser semantic rewrite removal as a regression guard, and identifies `EvalFragmentPlan` canonicalization as the next immediate implementation target.
- 2026-05-18 codex: Synchronized `current-state.md`, JavaScript feature docs, semantic feature matrix, and targeted unsupported eval fixture comments with the archive15 matrix. Also fixed the shadowed-eval fixture test expectation to assert the actual resolver contract: shadowed `eval` remains an ordinary call, so the later block-function name is unresolved rather than AOT-created.
- 2026-05-18 codex: Started PR B canonicalization by extending `EvalFragmentPlan` with explicit `scope_mode` and `host_policy`. Direct eval now records caller scope mode, indirect eval records global scope mode, static eval records AOT-only policy, and runtime-source eval records the exact direct/indirect host policy while preserving existing lowering behavior.
- 2026-05-18 codex: Lowering now consumes `EvalFragmentPlan.host_policy` instead of inferring host imports directly from `EvalKind`. Static AOT-only eval fragments that reach lowering without `expand_static_eval_fragments` now produce an `UnsupportedEval` diagnostic rather than silently falling into the runtime host eval lane.
- 2026-05-18 codex: Wrapped static eval completion steps in `EvalCompletionPlan`, so static eval expansion now returns a plan object instead of a bare `Vec<EvalCompletionStep>`. This preserves existing lowering/capture behavior while preparing declaration/completion metadata to move into plan-owned structures.
- 2026-05-18 codex: Added `EvalDeclarationPlan` metadata to `EvalCompletionPlan`. Static eval expansion now records eval `var` names and function hoists on the completion plan while retaining the existing `Hoist*` step compatibility path for current lowering/traversal code.
- 2026-05-18 codex: Moved top-level static eval declaration lowering to consume `EvalCompletionPlan.declarations` directly. The legacy `HoistVars` / `HoistFunctions` completion steps remain as a compatibility lowering path, but current static expansion no longer emits them as top-level sentinels.
- 2026-05-18 codex: Removed the legacy `HoistVars` / `HoistFunctions` completion-step variants after moving static eval declarations into `EvalDeclarationPlan`. Eval declaration hoisting now has a single plan-owned representation, while executable completion statements remain in `EvalCompletionStep`.
- 2026-05-18 codex: Added `FunctionConstructorPlan` with explicit call/new kind, args, AOT-vs-host policy, and span. Resolver, static expansion, lowering, and host-fact detection now consume the plan instead of destructuring loose `FunctionConstructor` fields.
- 2026-05-18 codex: Extended `FunctionConstructorPlan` with static Function constructor source metadata (`params` + `body`). Literal-only `Function` / `new Function` expansion now consumes plan-owned source splitting, while runtime-source constructors remain on the host-compile policy.
- 2026-05-18 codex: Added plan-owned Function constructor parse-goal and generated-function metadata. Static expansion now builds the synthetic anonymous function source from `StaticFunctionConstructorSource`, keeping FormalParameters / FunctionBody and generated `anonymous` function facts on the plan.
- 2026-05-18 codex: Propagated Function constructor generated-function metadata onto the expanded `FunctionExpr` and into lowering. Metadata name / capture suppression / Function-constructor detection now prefer plan-derived metadata instead of relying only on `FunctionExprOrigin::FunctionConstructor` hardcoding.
- 2026-05-18 codex: Reclassified static eval source `return` validation from `UnsupportedEval` to `UnsupportedSyntax`. The diagnostic display classifier now preserves this precise eval-code syntax error instead of bucketizing it as generic unsupported eval.
- 2026-05-18 codex: Extended `EvalScopeMode::Global` with explicit current-realm metadata. Indirect and optional eval plans now record global scope as `Global { realm: Current }`, preparing the global eval environment model instead of carrying a bare global marker.
- 2026-05-18 codex: Started the host external object/function contract cleanup by adding `HostExternalKind` and `StaticFacts` helpers for marking/querying host function handles and host external object locals. Dynamic Function call/construct/method lowering now uses the helper API instead of directly reaching into scattered fact sets.
- 2026-05-18 codex: Introduced a typed `DirectEvalEnvDescriptor` lowering layer for dynamic direct eval. Caller strict metadata and initialized env-cell bindings are collected as descriptor fields before lowering to the existing host ABI array, preparing descriptor v2 / mutation ledger work.
- 2026-05-18 codex: Started PR H burn-down classification by splitting `UnsupportedEval` coverage labels into `eval-direct-tdz`, `eval-static-aot`, `eval-code`, `function-constructor`, and `eval-unsupported` buckets across the test262 harness, reference coverage runner, legacy feature-label shell helper, and dashboard priority map.
- 2026-05-18 codex: Verified the PR H classification path with a small eval-code test262 JSONL run (`reference-coverage test262 --jsonl --path-filter language/eval-code --sample 3 --jobs 1 --no-semantic --no-dashboard-data`) and direct classifier checks for `eval-direct-tdz`, `eval-static-aot`, `eval-code`, and `function-constructor`.
- 2026-05-18 codex: Split the PR H test262 harness gaps further so `$262.evalScript` and cross-realm / `$262.createRealm` eval failures can classify as `test262-evalscript` and `test262-cross-realm` instead of being folded into generic eval unsupported.
- 2026-05-18 codex: Split the focused Node shim's dynamic direct eval write-back into an internal mutation ledger (`result`, `writes`, `createdBindings`) plus an explicit apply step. The host ABI still returns the current raw result, but the shim now mirrors the descriptor v2 / mutation-ledger shape needed by PR G.
- 2026-05-18 codex: Extended PR E static `Function` constructor metadata coverage to `Function.prototype.toString()`. Static constructor expansion now preserves the generated anonymous function source text through nested lowering, and the metadata fixture remains host-deny standalone.
- 2026-05-18 codex: Extended PR F dynamic host function handle metadata coverage to `f.toString()`. Host function handles now expose a bounded function-valued `toString` property in the Node shim, and lowering routes host function handle `toString()` through `host.function.callMethod` instead of Object.prototype fallback.
- 2026-05-18 codex: Added a PR D resolver regression guard for shadowed optional eval. Unshadowed `eval?.(source)` still classifies as indirect/global eval, while user-bound `eval?.(...)` remains an ordinary optional call and does not enter the intrinsic eval lane.
- 2026-05-18 codex: Added a PR C resolver guard and fix for static direct eval `var` declaration landing. Eval-created `var` declarations still predeclare into the caller when allowed, but a same-scope caller lexical binding now rejects the eval `var` as `DuplicateLocal` instead of silently bypassing the lexical collision.
- 2026-05-18 codex: Promoted the PR D shadowed optional eval guard to fixture coverage. `optional-eval-shadowed-ordinary-call.ts` now runs under Node differential comparison and host-deny standalone checks, proving user-bound `eval?.(...)` neither captures the intrinsic optional eval lane nor emits host eval capabilities.
- 2026-05-18 codex: Added PR C compiler regression coverage for static eval-source lexical/var declaration conflicts. `eval("let value = 1; var value = 2")` and `eval("var value = 1; let value = 2")` now stay guarded as `DuplicateLocal` during AOT eval expansion.
- 2026-05-18 codex: Advanced PR B by carrying eval completion context on `EvalCompletionPlan`. AOT static eval completion now records direct-caller vs global scope mode plus caller/eval strict flags, preserves that context through indirect-eval global collision rewrites, and lowering rejects any global eval completion plan that would land declarations in the caller scope.
- 2026-05-18 codex: Added PR C fixture coverage for eval lexical environment shadowing. `direct-eval-lexical-shadows-caller.ts` verifies static direct eval `let` / `const` declarations can shadow caller `let` / `var` bindings without mutating them, while remaining Node-differential and host-deny standalone.
- 2026-05-19 codex: Advanced PR D by giving static indirect eval `var` declarations an explicit `GlobalVarLet` completion step. AOT indirect eval no longer records these as caller declaration landings; lowering writes them through `globalThis.<name>`, and `indirect-eval-static-var-global.ts` guards Node parity plus no host-eval capabilities.
- 2026-05-19 codex: Extended PR D static indirect eval global landing to function declarations. AOT indirect eval now emits `GlobalFunctionDecl`, writes the generated function object to `globalThis.<name>`, tracks that global function-valued property for standalone `globalThis.fn()` calls, and guards the behavior with `indirect-eval-static-function-global.ts` under Node differential and host-deny checks.
- 2026-05-19 codex: Tightened the same PR D fixture/compiler guard so a static indirect eval-created global function body resolves caller-name collisions through `globalThis` instead of capturing the caller local.
- 2026-05-19 codex: Added PR D lexical-boundary coverage for static indirect eval. `indirect-eval-static-lexical-local.ts` verifies `let` / `const` declarations inside static indirect eval are eval-local, can shadow caller locals, do not leak to `globalThis`, and still require no host eval capability.
- 2026-05-19 codex: Added PR D global `var` hoist support for static indirect eval. AOT expansion now prepends global `var` hoist steps with `undefined` before executable completion steps, so branch-skipped `var` declarations still create global object bindings; `indirect-eval-static-var-hoist-global.ts` guards Node parity and host-deny standalone behavior.
- 2026-05-19 codex: Added PR D global function hoist coverage for static indirect eval. `indirect-eval-static-function-hoist-global.ts` verifies an eval-created global function can be called before its declaration and remains host-deny standalone.
- 2026-05-19 codex: Added PR E static `new Function(...)` prototype identity coverage. `function-constructor-new-static-prototype.ts` verifies the generated function object's `.prototype.constructor` points back to the function and constructed instances satisfy `instanceof`, while remaining host-deny standalone.
- 2026-05-19 codex: Tightened PR E static Function constructor host-deny coverage by adding the existing call-static, static construct, and zero-argument static Function fixtures to the standalone no-host manifest guard.
- 2026-05-19 codex: Extended PR E static Function constructor source classification beyond string literals to compile-time primitive literals. `Function(null)` and `new Function(true)` now apply plan-owned ToString source coercion, expand through the AOT lane, match Node output including `toString()`, and remain host-deny standalone.
- 2026-05-19 codex: Added PR B/D static non-string eval classification. `eval(1)`, `(0, eval)(true)`, `eval(null)`, and `eval(undefined)` now use `EvalSource::NonStringStatic` and return the argument value without host eval imports, matching Node's non-string eval semantics under Node differential and host-deny guards.
- 2026-05-19 codex: Extended the same non-string eval classification to optional eval. `eval?.(1)` now resolves as indirect/global AOT eval with `EvalSource::NonStringStatic`, matches Node's value-return behavior, and remains host-deny standalone.
- 2026-05-19 codex: Extended the static non-string eval fixtures to no-argument calls. `eval()`, `(0, eval)()`, and `eval?.()` now have Node differential and host-deny guards for the `undefined` result path.
- 2026-05-19 codex: Guarded the PR E Function constructor primitive-parameter path. `Function(1, "return 1")` now stays on the AOT parse-goal path and reports a static Function-constructor syntax diagnostic instead of falling back to `host.function.compile`.
- 2026-05-19 codex: Added the existing `new-function-constructor-static.ts` fixture to the standalone host-deny guard so static `new Function("return 1")` remains covered as AOT/no-host in both Node differential and manifest checks.
- 2026-05-19 codex: Added PR F host function handle propagation coverage for array elements returned by dynamic `Function(...)`. `function-constructor-dynamic-array-function-element-node-shim.ts` verifies callbacks loaded from a host external array remain callable through `host.function.call` with exact `host.function.compile` + `host.function.call` manifest imports.
- 2026-05-19 codex: Added PR F/G coverage for host function handles returned inside arrays from dynamic direct eval. `direct-eval-dynamic-array-function-element-node-shim.ts` verifies callbacks loaded from a host external eval result remain callable through `host.function.call`, with exact `host.eval.direct` + `host.function.call` manifest imports. The direct eval env descriptor guard is now source-aware for statically known runtime-source strings: it still rejects sources that reference a later TDZ binding, but no longer rejects merely because unrelated caller bindings are declared after the eval call.
- 2026-05-19 codex: Added a direct PR G regression for the source-aware guard. `direct-eval-dynamic-let-initializer-node-shim.ts` verifies `let result = eval(source)` compiles and runs when the statically known runtime source does not reference later caller bindings, while requiring only `host.eval.direct`.
- 2026-05-19 codex: Tightened the source-aware direct eval TDZ guard so identifier names inside single/double quoted source strings and comments do not count as caller binding references. `direct-eval-dynamic-tdz-name-in-string-node-shim.ts` guards the false-positive case while the existing TDZ conflict fixture still rejects a real later binding reference.
- 2026-05-19 codex: Extended the same source-aware TDZ scan to template literals. Raw template text such as `` `later` `` no longer triggers a false TDZ rejection, while template expressions such as `` `${later}` `` still reject as an UnsupportedEval descriptor-v2 boundary.
- 2026-05-19 codex: Advanced PR G normal-code visibility for runtime-source direct eval-created `var` bindings when the source value is statically known. Resolver/lowering now predeclare simple eval-created `var`/function names as caller env cells, so `let source = "var created = 7; created"; eval(source); created` reads through the direct-eval mutation ledger; `direct-eval-dynamic-new-var-normal-code-node-shim.ts` guards the focused path with host-deny coverage.
- 2026-05-19 codex: Extended the same PR G normal-code visibility path to statically known runtime-source direct eval function declarations. Eval-created function names now retain host function handle facts on the predeclared env cell, so `created()` lowers through exact `host.eval.direct` + `host.function.call`; `direct-eval-dynamic-new-function-normal-code-node-shim.ts` guards Node-shim stdout, host-deny rejection, and exact manifest imports.
- 2026-05-19 codex: Extended PR G normal-code visibility to statically known runtime-source direct eval `var` destructuring declarations. Resolver/lowering now extract object and array binding names from eval source `var` patterns, predeclare them as caller env cells, and guard normal-code reads with focused node-shim and host-deny fixtures.
- 2026-05-19 codex: Centralized the lightweight statically known eval-source declaration scanner in `ts2wasm-resolve::direct_eval_source`. Resolver predeclaration and IR lowering now consume the same scanner for eval-created `var` destructuring and function declaration names, reducing descriptor-v2 drift while the canonical `EvalDeclarationPlan` migration remains open.
- 2026-05-19 codex: Extended PR F dynamic Function host handle metadata coverage to prototype identity. The Node shim now gives host function handle prototype objects an enumerable `constructor` link back to the same host function handle and handles cyclic host object refresh without recursive re-entry; `function-constructor-dynamic-prototype-identity-node-shim.ts` guards `F.prototype.constructor === F` with exact `host.function.compile` manifest coverage and host-deny rejection.
- 2026-05-19 codex: Extended PR F receiver-preserving host function bridge coverage through dynamic Function prototype objects. Non-identifier host external object receivers such as `F.prototype.constructor(...)` now lower through `host.function.callMethod`, preserving the prototype receiver and exact `host.function.compile` + `host.function.callMethod` manifest imports; `function-constructor-dynamic-prototype-constructor-call-node-shim.ts` guards the path.
- 2026-05-19 codex: Extended the same PR F host external method bridge to computed property calls. Dynamic Function results such as `obj["cb"](...)` now lower through `host.function.callMethod` with the host external object receiver preserved, while the surrounding dynamic Function handle call keeps the exact `host.function.compile` + `host.function.call` + `host.function.callMethod` manifest set; `function-constructor-dynamic-computed-function-property-call-node-shim.ts` guards the path.
- 2026-05-19 codex: Extended the PR F/G computed host external method bridge to dynamic direct eval results. `obj["cb"](...)` where `obj` is returned by runtime-source direct eval now preserves the host external object receiver through `host.function.callMethod`, with exact `host.eval.direct` + `host.function.callMethod` manifest coverage and host-deny rejection.
- 2026-05-19 codex: Extended PR F host function handle method bridging to computed calls on the handle itself. Dynamic Function handles now lower `f["toString"]()` through `host.function.callMethod` with the handle as receiver, preserving exact `host.function.compile` + `host.function.callMethod` manifest coverage and host-deny rejection.
- 2026-05-19 codex: Extended the computed host-function method bridge to direct eval-created function bindings that are visible to normal code. `created["toString"]()` now uses the eval-created function env-cell host handle fact and lowers through exact `host.eval.direct` + `host.function.callMethod`; `direct-eval-dynamic-new-function-computed-tostring-node-shim.ts` guards the path.
- 2026-05-19 codex: Advanced PR G direct eval descriptor v2 by adding an explicit `__ts2wasm_eval_descriptor_v2` header to lowered dynamic direct eval env descriptors. The Node shim now parses that typed header before caller strict metadata while retaining compatibility with the previous caller-strict-first shape.
- 2026-05-19 codex: Extended the direct eval descriptor v2 shape with an explicit `__ts2wasm_eval_bindings` section containing nested `[name, envCell]` binding entries. The Node shim now consumes that typed binding section while preserving fallback parsing for the previous flat binding-pair descriptor tail.
- 2026-05-19 codex: Added a binding kind field to direct eval descriptor v2 entries. Initialized caller env-cell bindings now emit as `[name, envCell, "readwrite"]`, and the Node shim validates that kind while keeping compatibility with older two-field binding entries.
- 2026-05-19 codex: Advanced PR G TDZ handling for dynamic direct eval descriptors. The descriptor can now carry `"tdz"` binding entries, and the Node shim turns focused single-identifier / template-expression TDZ references into catchable `ReferenceError` results instead of build-time `UnsupportedEval`; the former TDZ unsupported fixtures moved to node-shim coverage with host-deny and exact manifest guards.
- 2026-05-19 codex: Tightened the PR G direct eval mutation ledger shape in the focused Node shim. Ledgers now carry an explicit version, `abrupt` slot, and validated `writes` / `createdBindings` arrays before applying env-cell writes or persisted eval-created bindings.
- 2026-05-19 codex: Started using the direct eval mutation ledger `abrupt` slot for thrown eval-code completions. The focused Node shim now captures thrown eval errors in the ledger, applies env-cell writes / created bindings, then rethrows the recorded error; `direct-eval-dynamic-throw-writeback-node-shim.ts` guards write-back-before-throw behavior.
- 2026-05-19 codex: Extended abrupt-ledger coverage to eval-created bindings. `direct-eval-dynamic-throw-created-binding-node-shim.ts` verifies a `var` binding created before a thrown dynamic direct eval completion is persisted through `createdBindings` and remains visible to a later runtime-source direct eval call.
- 2026-05-19 codex: Extended the same abrupt-ledger persistence guard to eval-created function declarations. `direct-eval-dynamic-throw-created-function-node-shim.ts` verifies a function declaration created before a thrown dynamic direct eval completion remains callable from a later runtime-source direct eval call.
- 2026-05-19 codex: Extended abrupt-ledger eval-created function persistence to normal wasm code. `direct-eval-dynamic-throw-created-function-normal-code-node-shim.ts` verifies a function declared before a thrown dynamic direct eval completion remains callable from subsequent compiled code with exact `host.eval.direct` + `host.function.call` manifest coverage.
- 2026-05-19 codex: Extended the PR F/G host external bridge through dynamic direct eval catch bindings. Plain objects thrown from runtime-source direct eval now preserve their function-valued properties in the focused Node shim exception bridge, and catch bindings for dynamic direct eval lower host object method calls through exact `host.eval.direct` + `host.function.callMethod`; `direct-eval-dynamic-throw-object-method-node-shim.ts` guards the path.
- 2026-05-19 codex: Extended the same catch-binding host external object bridge to dynamic Function host calls. Try blocks that may throw host external objects from host eval/function lanes now mark catch bindings before lowering the catch body, allowing `error.cb(...)` from a dynamic Function-thrown plain object to lower through exact `host.function.compile` + `host.function.call` + `host.function.callMethod`; `function-constructor-dynamic-throw-object-method-node-shim.ts` guards the path.
- 2026-05-19 codex: Added dynamic indirect eval coverage for the same host external catch bridge. A runtime-source indirect eval that throws a plain object with a function-valued property now lets compiled catch code call `error.cb(...)` through exact `host.eval.indirect` + `host.function.callMethod`; `indirect-eval-dynamic-throw-object-method-node-shim.ts` guards the path.
- 2026-05-19 codex: Extended the host external catch bridge to the `$262.evalScript` harness hook without reclassifying it as language direct eval. `$262.evalScript` thrown plain objects can now preserve function-valued properties through catch and lower `error.cb(...)` through exact `host.eval.indirect` + `host.function.callMethod`; `test262-eval-script-throw-object-method-node-shim.ts` guards the path.
- 2026-05-19 codex: Advanced PR B plan ownership by routing static eval completion construction through `EvalFragmentPlan::completion_expr`. The AOT expansion stage now derives completion scope mode from the fragment plan instead of passing ad-hoc scope/caller fields into `extract_completion_value`, with existing direct/indirect eval completion tests guarding parity.
- 2026-05-19 codex: Extended the same PR B migration by giving `EvalFragmentPlan` optional `declaration_plan` and `completion_plan` fields. Static AOT expansion now materializes those fields through `with_completion_plan` before emitting the existing `ResolvedExpr::EvalCompletion`, preserving current lowering behavior while making declaration/completion data plan-owned.
- 2026-05-19 codex: Advanced PR E source representation ownership for static Function constructors. `StaticFunctionConstructorSource` now provides the generated function source text used by expanded `FunctionExpr` metadata/toString output, instead of relying on parser-returned synthetic function source text as an incidental side effect; the static Function metadata node diff guards parity.
- 2026-05-19 codex: Advanced PR E parse-goal ownership for static Function constructors. AOT expansion now validates the `StaticFunctionConstructorSource` parse-goal contract (`FormalParameters` + `FunctionBody`) before using the synthetic parse wrapper, and compiler tests assert that literal Function constructor plans carry those parse goals before expansion.
- 2026-05-19 codex: Advanced PR E generated function metadata ownership by adding optional `.length` metadata to `FunctionConstructorGeneratedFunction`. Static Function constructor expansion now computes the ECMAScript length from parsed parameters and lowering consumes the plan-owned value before falling back to parameter inference; compiler and Node differential tests guard default/rest/destructuring parameter parity.
- 2026-05-19 codex: Wired PR E constructability metadata through lowering. Static Function constructor locals now use `FunctionConstructorGeneratedFunction.constructable` when deciding whether to expose generated `.prototype` / `new f()` behavior, with legacy FunctionConstructor-origin fallback retained for older IR shapes; the static new Function prototype differential guard remains passing.
- 2026-05-19 codex: Extended PR G dynamic direct eval TDZ focused scanning for parenthesized identifiers and `typeof` references. Descriptor v2 now emits TDZ candidates for `(later)` / `typeof later`, and the focused Node shim mirrors that classifier so both forms become catchable `ReferenceError` completions with exact `host.eval.direct` manifest coverage while string/template raw-text false positives remain guarded.
- 2026-05-19 codex: Extended the same PR G TDZ focused scanner to simple member references. Runtime-source direct eval of `later.value` now emits a TDZ descriptor candidate for the base binding and the focused Node shim rejects it as a catchable `ReferenceError`; `direct-eval-dynamic-tdz-member-node-shim.ts` guards the path.
- 2026-05-19 codex: Tightened the shared statically-known direct eval source declaration scanner so named function expressions are not predeclared as eval-created normal-code bindings. `function hiddenProbe(){}` declarations and async/generator declarations remain collected, while `let holder = function hiddenProbe(){}` now leaves `hiddenProbe` unresolved in normal compiled code instead of fabricating an `undefined` binding. The focused TDZ member scanner also now excludes known ambient globals such as `globalThis`, preserving `globalThis.hiddenProbe` direct-eval reads without disabling real caller TDZ member guards.
- 2026-05-19 codex: Tightened the same direct eval source declaration scanner across nested function/class bodies. Statically-known runtime-source direct eval now keeps top-level eval-created `var` / function declarations visible, but no longer predeclares `var` / function declarations that are local to a nested function body; `direct-eval-dynamic-nested-function-var-normal-code-unsupported.ts` guards the normal-code boundary.
- 2026-05-19 codex: Extended the direct eval source declaration scanner boundary to arrow function block bodies. Statically-known runtime-source direct eval now skips `=> { ... }` bodies when collecting eval-created names, so arrow-local `var` declarations are not fabricated as caller bindings; `direct-eval-dynamic-arrow-body-var-normal-code-unsupported.ts` guards the boundary while top-level eval-created `var` / function fixtures remain passing.
- 2026-05-19 codex: Extended the same declaration scanner boundary to object literal method bodies. Method-local `var` / function declarations inside `method() { ... }` are no longer predeclared as caller bindings, while block-level eval `var` declarations such as `if (...) { var x }` remain collected; `direct-eval-dynamic-object-method-var-normal-code-unsupported.ts` guards the normal-code boundary.
- 2026-05-19 codex: Tightened PR E static Function constructor parse-goal validation against parameter-wrapper injection. Literal AOT `Function("a) { ... } function injected(", "body")` now rejects as a static Function-constructor syntax error instead of parsing multiple top-level synthetic functions and expanding the first one; compiler and fixture-level guards cover the boundary.
- 2026-05-19 codex: Extended PR B/D static non-string eval classification beyond primitive literals. Direct/indirect eval with statically non-string object, array, function expression, arrow, or Function-constructor arguments now returns the argument value through the AOT lane without requesting `host.eval.*`; `eval-static-non-string-object.ts` guards Node parity and host-deny standalone behavior.
- 2026-05-19 codex: Moved the shared statically-known direct eval source declaration collector toward the PR G parsed-plan target. Frontend-parseable source strings now collect eval-created `var` / function declarations from AST statements while naturally skipping nested function/class/arrow/object-method bodies, with the lexical scanner retained for parser-unsupported runtime source strings. The same slice tightened strict-caller direct eval boundaries so eval-created `var` / function names are not predeclared into caller normal code, and focused TDZ descriptor entries are only synthesized for known caller bindings.
- 2026-05-19 codex: Closed a PR G regression in the parsed direct eval source declaration collector for `for (var ... in/of ...)` heads. Statically-known runtime-source direct eval now predeclares loop-head `var` bindings for normal-code visibility while leaving `let` / `const` heads eval-local; the focused Node shim declaration collector mirrors the same for-head boundary so mutation-ledger created bindings do not fabricate `in` / `of` / `for` keyword bindings.
- 2026-05-19 codex: Added PR G coverage for direct eval `for` head `var` destructuring declarations. The parsed source collector now has an explicit regression for object/array binding patterns in `for (var ... of ...)`, and `direct-eval-dynamic-for-head-var-destructuring-normal-code-node-shim.ts` verifies the mutation ledger keeps those created bindings visible to later compiled code with host-deny coverage.
- 2026-05-19 codex: Extended PR B/C static direct eval declaration landing to `for (var ... in/of ...)` loop heads. AOT eval source resolution and completion lowering now classify loop-head `var` bindings as caller/global/local landing slots instead of always creating eval-local loop variables, while `let` / `const` loop heads stay eval-local; `direct-eval-for-head-var-lands-in-caller.ts` guards Node parity and no-host manifest behavior.
- 2026-05-19 codex: Added PR D coverage for static indirect eval `for (var ... in/of ...)` loop-head global landing. `indirect-eval-static-for-head-var-global.ts` verifies AOT indirect eval keeps caller locals untouched, writes loop-head `var` bindings to `globalThis`, and remains standalone with no `host.eval.*` imports.
- 2026-05-19 codex: Extended PR C static direct eval loop-head declaration landing to destructuring patterns. `EvalCompletionStep::ForIn` / `ForOf` now carry the parsed `var` binding pattern and lowering writes each iteration value into caller/global declaration slots before the loop body, so `for (var { item } of ...)` and `for (var [first, ...rest] of ...)` remain wasm-native and visible after eval; `direct-eval-for-head-var-destructuring-lands-in-caller.ts` guards Node parity plus no-host manifest behavior.
- 2026-05-19 codex: Added PR D coverage for static indirect eval destructuring loop-head global landing. `indirect-eval-static-for-head-var-destructuring-global.ts` verifies `for (var { item } of ...)` and `for (var [first, ...rest] of ...)` leave caller locals untouched, write destructured values to `globalThis`, and remain in the standalone AOT lane.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to compile-time expression values. `StaticFunctionConstructorSource::from_args` now evaluates supported static `+` and unary source expressions before applying Function-constructor ToString, so string concatenation, template-literal add chains, numeric addition, `void`, and `!` sources stay in the AOT/no-host lane; `function-constructor-static-expression-source.ts` and `function-constructor-static-unary-source.ts` guard Node parity and host-deny standalone behavior.
- 2026-05-19 codex: Extended PR C static direct eval `for (var ... of/in ...)` destructuring defaults beyond literal-only values. Eval for-head destructuring now reuses the existing binding-default lowering path, so defaults can read caller bindings while remaining in the AOT/no-host lane; `direct-eval-for-head-var-destructuring-default-caller.ts` guards Node-shim stdout and host-deny standalone behavior.
- 2026-05-19 codex: Extended PR C static direct eval `for (var ... of/in ...)` object destructuring to computed string keys. Eval for-head object patterns now lower computed keys through `PropertyGetDynamic` and reuse the shared computed binding-key helper, while object rest remains the explicit unsupported boundary; `direct-eval-for-head-var-destructuring-computed-key.ts` guards Node-shim stdout and host-deny standalone behavior.
- 2026-05-19 codex: Tightened the PR C computed-key slice so static direct eval `for` head destructuring can read caller bindings from computed keys like `{ [key]: item }`. The shared direct-eval declaration scanners now skip computed and quoted property keys when collecting eval-created binding names, avoiding false `DuplicateLocal` predeclarations while preserving the actual target binding.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to compile-time ternary expressions. Supported static ternary conditions now select a static source branch before Function-constructor ToString, keeping conditional string and numeric expression bodies in the AOT/no-host lane; `function-constructor-static-ternary-source.ts` guards Node parity and standalone manifest behavior.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to compile-time logical/nullish expressions. Supported static `&&`, `||`, and `??` sources now preserve JavaScript selected-operand semantics before Function-constructor ToString, keeping short-circuit string/number/boolean/nullish sources in the AOT/no-host lane; `function-constructor-static-logical-source.ts` guards Node parity and standalone manifest behavior.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to side-effect-free array literals. `Function(["return 7"])`, array join source bodies, and `Function([])` now apply ECMAScript array ToString at compile time and stay in the AOT/no-host lane; `function-constructor-static-array-source.ts` guards Node-shim stdout, compiler expansion, and host-deny standalone behavior.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to side-effect-free array spread arguments. `Function(...["return 11"])` and mixed static arguments such as `Function("left", ...["right", "return left + right"])` now flatten the compile-time array source list and remain AOT/no-host; `function-constructor-static-spread-array-source.ts` guards Node-shim stdout, compiler expansion, and host-deny standalone behavior.
- 2026-05-19 codex: Tightened PR G direct eval descriptor TDZ source scanning so identifier text inside regular expression literals is not treated as a caller TDZ reference. The focused Node shim now skips regex literals alongside strings, comments, and template raw text; `direct-eval-dynamic-tdz-name-in-regexp-node-shim.ts` guards Node-shim stdout, exact `host.eval.direct` manifest imports, and host-deny rejection.
- 2026-05-19 codex: Added a strict-caller guard for the same PR G regex-literal scanner boundary. `direct-eval-dynamic-strict-caller-regexp-restricted-words-node-shim.ts` verifies regex text like `/var arguments/` does not trigger strict direct-eval restricted-binding syntax rejection while still requiring the dynamic direct eval host lane.
- 2026-05-19 codex: Extended the PR G regex-literal scanner to expression-start keywords. Strict-caller runtime-source direct eval now skips regex literals after keywords such as `void`, so `void /var arguments/.source` does not produce a false restricted-binding `SyntaxError`; `direct-eval-dynamic-strict-caller-regexp-after-keyword-node-shim.ts` guards the boundary with host-deny coverage.
- 2026-05-19 codex: Mirrored the regex-literal scanner boundary in the shared `ts2wasm-resolve::direct_eval_source` collector. The statically-known direct eval source declaration scanner now skips `var` / `function` text inside regex literals, including keyword-start regex forms, reducing resolver/lowering declaration-plan drift while the canonical `EvalDeclarationPlan` migration remains open.
- 2026-05-19 codex: Added PR D coverage for computed keys in static indirect eval `for (var ... of ...)` destructuring. Global-landing computed keys such as `{ [key]: item }` now resolve the key expression through `globalThis.key` instead of capturing a caller lexical `key`, leaving caller locals untouched while writing the loop-head `var` result to the global object; `indirect-eval-static-for-head-var-computed-global.ts` guards Node parity and standalone/no-host behavior.
- 2026-05-19 codex: Extended PR C/D static eval `for` head destructuring to object rest patterns. Eval completion now strips the parser-inserted for-head destructuring shim when a plan-owned `var_pattern` is present, and lowering builds rest objects with `Object.assign` plus excluded-key deletes before caller/global landing; `direct-eval-for-head-var-object-rest-caller.ts` and `indirect-eval-static-for-head-var-object-rest-global.ts` guard Node parity and no-host manifests.
- 2026-05-19 codex: Extended PR C/D static eval `for` head object-rest destructuring to computed excluded keys. Direct eval now reads caller computed-key bindings for `{ [key]: removed, ...rest }`, and indirect eval resolves the computed key through `globalThis.key` while leaving caller locals untouched. The slice also fixed a runtime object root cause: `$object_create` now allocates append headroom like object literals, so `globalThis` properties created before later eval/object-rest allocations are not overwritten. `direct-eval-for-head-var-object-rest-computed-key.ts` and `indirect-eval-static-for-head-var-object-rest-computed-global.ts` guard Node parity plus standalone/no-host behavior.
- 2026-05-19 codex: Added PR F dynamic Function constructor coverage for runtime spread-array arguments. `function-constructor-dynamic-spread-array-node-shim.ts` verifies `Function(...parts)` flattens the runtime array before `host.function.compile`, calls the returned handle through `host.function.call`, records exact compile+call capabilities, and is rejected by host-deny.
- 2026-05-19 codex: Extended PR C/D static eval declaration landing to traditional `for` init `var` declarations. Static direct eval now collects `var` declarations from `for` init before AOT expansion so caller normal code can read eval-created bindings, and static indirect eval keeps the same form on `globalThis` without capturing caller locals; `direct-eval-for-init-var-lands-in-caller.ts` and `indirect-eval-static-for-init-var-global.ts` guard Node parity plus standalone/no-host behavior.
- 2026-05-19 codex: Extended PR C/D static eval `var` declaration landing to destructuring declarations outside `for-in/of` heads. AOT eval now collects binding names from object/array `var` patterns, carries destructuring `var` completion steps with caller/global landing metadata, and lowers them through the same single-evaluation pattern write path used by `for` heads; `direct-eval-var-destructuring-lands-in-caller.ts`, `direct-eval-for-init-var-destructuring-caller.ts`, `indirect-eval-static-var-destructuring-global.ts`, and `indirect-eval-static-for-init-var-destructuring-global.ts` guard Node parity plus standalone/no-host behavior.
- 2026-05-19 codex: Added PR C/D computed-key and object-rest coverage for static eval `var` destructuring declarations outside `for-in/of` heads. The existing destructuring-var landing path now has focused direct/indirect guards for `{ [key]: removed, ...rest }` both as a top-level eval `var` declaration and as a traditional `for` init declaration, preserving caller computed-key reads for direct eval and `globalThis` landing for indirect eval without host imports.
- 2026-05-19 codex: Added PR C/D hoist coverage for static eval `var` destructuring declarations in skipped branches. `direct-eval-var-destructuring-hoist-caller.ts` and `indirect-eval-static-var-destructuring-hoist-global.ts` verify the plan-owned declaration list still creates caller/global `undefined` bindings even when the destructuring initializer never executes, and both remain standalone/no-host.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to unary decimal sources. `Function(-1.5)` and `new Function(+2.5)` now apply compile-time ToString source coercion and stay in the AOT/no-host lane; `function-constructor-static-decimal-unary-source.ts` guards Node parity, plan-owned source text, and standalone host-deny behavior.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to decimal `+` expressions. `Function(1.5 + 2.5)` and string concatenation with decimal sums now fold through compile-time JavaScript number ToString, stay in the AOT/no-host lane, and are guarded by `function-constructor-static-decimal-expression-source.ts`.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to string numeric unary coercion. `Function(+"2.5")`, `Function(-"1.5")`, and empty-string unary plus now apply a focused compile-time ToNumber + ToString path, remain AOT/no-host, and are guarded by `function-constructor-static-string-unary-source.ts`.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to finite numeric binary expressions. `Function(2 * 3)`, `Function(5 - 2)`, `Function(5 / 2)`, `Function(7 % 4)`, `Function(2 ** 3)`, and focused string/bool/null ToNumber operands now fold before Function-constructor ToString, remain AOT/no-host, and are guarded by `function-constructor-static-numeric-binary-source.ts`.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to focused compile-time comparison expressions. Static relational and equality results such as `Function(1 < 2)`, `Function("2" == 2)`, and `Function(null != undefined)` now fold to boolean source bodies before Function-constructor ToString, remain AOT/no-host, and are guarded by `function-constructor-static-comparison-source.ts`.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to focused bitwise and shift expressions. `Function(5 & 3)`, `Function(1 << 3)`, `Function(-1 >>> 1)`, `Function(~5)`, and primitive ToInt32/ToUint32 operands now fold before Function-constructor ToString, remain AOT/no-host, and are guarded by `function-constructor-static-bitwise-source.ts`.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to focused `typeof` source expressions. Primitive, null, array, and BigInt `typeof` results now fold before Function-constructor ToString, compose through static string concatenation, remain AOT/no-host, and are guarded by `function-constructor-static-typeof-source.ts`.
- 2026-05-19 codex: Extended PR E static Function constructor source classification to side-effect-free sequence expressions. Static comma sources such as `Function((0, "return 9"))` and sequence expressions in parameter/body source positions now evaluate the final static operand before Function-constructor ToString, remain AOT/no-host, and are guarded by `function-constructor-static-sequence-source.ts`.
- 2026-05-19 codex: Advanced PR B `EvalFragmentPlan` canonicalization by adding `eval_source_is_strict` to the fragment plan. Static eval expansion now records the parsed eval-source strictness on the plan alongside the existing completion plan, giving later declaration/completion lowering a plan-owned strictness fact instead of relying only on local expansion variables.
- 2026-05-19 codex: Tightened PR B eval host-policy plan validation. `EvalFragmentPlan` now derives its expected `EvalHostPolicy` from `kind` + `source`, exposes a consistency check, and lowering rejects mismatched eval plans before selecting a runtime host lane, so malformed direct/indirect/static/runtime combinations are diagnosed from plan data instead of falling through ad-hoc source matching.
- 2026-05-19 codex: Tightened PR E Function-constructor host-policy plan validation. `FunctionConstructorPlan` now derives expected host policy from `static_source`, AOT expansion rejects mismatched static/dynamic policy before fallback, and lowering rejects static AOT Function constructors that reach the dynamic host-compile lane without expansion.
- 2026-05-19 codex: Tightened PR B eval scope-mode plan validation. `EvalFragmentPlan` now derives expected scope mode from `EvalKind`, and both static AOT expansion and runtime lowering reject direct/indirect plans whose caller/global scope mode does not match their kind.
- 2026-05-19 codex: Tightened PR B embedded completion-plan validation. `EvalFragmentPlan` now rejects declaration/completion state drift, including missing declaration plans, strictness mismatches, scope mismatches, and runtime-host fragments that incorrectly carry AOT completion plans; static expansion and runtime lowering both check the plan-owned contract.
- 2026-05-19 codex: Moved eval completion landing validation into the PR B plan model. `EvalCompletionPlan` now detects caller/global landing-step drift recursively, and lowering rejects malformed completion plans via the plan API instead of a local caller-landing helper.
- 2026-05-19 codex: Connected completion landing validation back into `EvalFragmentPlan` so malformed embedded completion plans with caller/global landing drift are rejected by fragment validation before static expansion or runtime lowering can consume them.
- 2026-05-19 codex: Tightened the PR E static Function constructor sequence-source boundary. Sequence expressions now remain AOT only when every discarded prefix operand is also statically classifiable; effectful prefixes such as assignments no longer get dropped by the source classifier and instead stay on the dynamic host-compile path.
- 2026-05-19 codex: Added PR E dynamic host-lane coverage for effectful Function-constructor sequence sources. `function-constructor-dynamic-sequence-prefix-node-shim.ts` verifies `Function((side = 1, "return 7"))` preserves the assignment side effect, compiles through the dynamic Function host lane, and is rejected by host-deny.
- 2026-05-19 codex: Tightened the same dynamic sequence-source boundary with exact manifest coverage. The effectful sequence fixture now asserts `standalone: false`, `node_host.required: true`, and the exact `host.function.compile` + `host.function.call` import set with auditable capability reasons.
- Static direct eval("expr") is implemented via compile-time expansion (8fe6e97fa) and has since gained caller-local reads, guarded side-effect completion, static indirect eval AOT slices, dynamic host-lane slices, and `new eval(...)` TypeError parity guards.
