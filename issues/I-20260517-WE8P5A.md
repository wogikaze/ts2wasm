Id: I-20260517-WE8P5A
Status: doing
Priority: P1
Labels: area:runtime type:feature feature:eval
Owner: codex
BlockedReason:
Created: 2026-05-17T09:03:56+09:00
Updated: 2026-05-18T12:47:49+09:00
Title: Implement eval and Function constructor support
Summary: Complete static AOT and audited host-lane eval/Function support; remaining work is canonical eval/function lowering and broader semantics.
Next: Implement the next canonical `EvalFragmentPlan` / eval-code environment slice with focused Node parity tests.
---

## Notes

- 2026-05-18 codex: Reopened as the active eval/Function implementation tracker. Dynamic eval and dynamic Function constructor are no longer classified as intentionally unsupported as a whole: supported runtime-source slices now lower to audited Node host lanes with exact `host.eval.*` / `host.function.*` manifest entries and host-deny checks. Remaining work is the canonical `EvalFragmentPlan` / `FunctionConstructorPlan` migration, direct-eval declaration landing, full TDZ modeling, runtime-wide host external object contracts, and broader test262/realm classification.
- 2026-05-18 codex: Static direct eval declaration landing now covers expression completion, eval-created `var` direct reads later in the same function body, eval-defined function reads of eval-defined vars, and host-lane dynamic declaration persistence for later direct eval calls. Remaining declaration work is broader lexical environment modeling and the canonical eval-code environment path.
- 2026-05-18 codex: `$262.evalScript(source)` now lowers through `Dollar262Eval` to the audited `host.eval.indirect` lane with focused Node-shim, manifest, and host-deny guards. Broader realm / cross-realm classification remains open.
- 2026-05-18 codex: Static literal `Function("...items", ...)` now lowers through the AOT Function constructor path, executes rest parameter calls under Node differential coverage, reports Node-compatible `.length === 0`, and remains host-deny standalone.
- 2026-05-18 codex: Static literal Function constructor parameter grammar now has Node differential coverage for defaults, destructuring, and `new Function` rest parameters with Node-compatible `.length` metadata.
- 2026-05-18 codex: Static literal Function constructor calls now preserve explicit object returns from `new F()` while primitive constructor returns still fall back to the allocated receiver, matching Node differential coverage.
- 2026-05-18 codex: Static literal Function constructor functions containing `this` now lower as receiver-aware functions so sloppy calls see `globalThis` and strict calls see `undefined`, matching focused Node differential coverage.
- 2026-05-18 codex: Static literal Function constructor functions containing `new.target` now receive a synthetic construct-only target token, so ordinary calls see `undefined` and `new F()` sees `typeof new.target === "function"` plus Node-compatible `.name` / `.length` metadata under Node differential coverage. Static Function constructor `arguments` object reads are also guarded.
- 2026-05-18 codex: Intrinsic `Function(...)` / `new Function(...)` builtin resolution now uses an explicit `ResolvedExpr::FunctionConstructor` IR node for both static AOT expansion and dynamic host-lane fallback instead of preserving magic identifier call/new shapes after builtin resolution.
- 2026-05-18 codex: `ResolvedExpr::Eval` now carries `EvalFragmentPlan` as an explicit plan object for kind/source/strict/span metadata, preparing the later scope/declaration/completion record migration without changing current AOT or host-lane behavior.
- 2026-05-18 codex: Removed parser-side direct-eval statement splicing and the `possible_eval_shadowing` heuristic. Static direct eval statements now preserve AST call shape through parser/name resolver and run through compiler `EvalCompletionStep` expansion; build/server/dump paths all run `expand_static_eval_fragments`.
- 2026-05-18 codex: Recursive static eval expansion now happens inside eval fragments, and EvalCompletion function declarations feed mutable-capture/env-cell planning so the guarded direct-eval block-function fixtures remain Node-differential green without parser splicing.
- 2026-05-18 codex: Static direct eval expansion now propagates strict caller context through nested function/class scopes. Strict caller eval-code `var` declarations stay eval-local under Node-shim, Node differential, and host-deny standalone guards.
- 2026-05-18 codex: Builtin resolution now annotates `EvalFragmentPlan.caller_is_strict` across script, function, class, arrow, and nested expression scopes so static expansion and dynamic host-lane lowering receive the same strict caller metadata.
- 2026-05-18 codex: Dynamic direct eval env descriptors now carry resolver-owned strict-caller metadata into the Node shim. Runtime-source direct eval in a strict caller keeps eval-created `var` declarations eval-local under focused Node-shim and host-deny guards.
- 2026-05-18 codex: Added a focused strict-caller runtime-source direct eval syntax guard for `delete Identifier`, which now bridges Node's catchable `SyntaxError` through the env-descriptor host lane.
- 2026-05-18 codex: Extended the strict-caller `delete Identifier` guard to reserved direct-eval-visible bindings such as `arguments`, avoiding the sloppy wrapper fallback when strict source references compatibility env bindings.
- 2026-05-18 codex: Strict-caller runtime-source direct eval now rejects source that binds `arguments` or `eval` before the Node shim falls back to compatibility env bindings, with focused `var arguments` coverage.
- 2026-05-18 codex: Added focused `function eval() {}` coverage for strict-caller runtime-source direct eval restricted bindings.
- 2026-05-18 codex: Extended strict-caller runtime-source direct eval restricted binding coverage to destructuring `var [arguments] = ...` and explicit async function declarations before compatibility fallback.
- 2026-05-18 codex: Extended the same strict-caller restricted binding guard to object destructuring value bindings such as `const {value: eval} = ...`, preserving allowed alias patterns for future coverage.
- 2026-05-18 codex: Dynamic direct eval results assigned into env-cell locals now retain host external object / host function handle facts, so function-valued properties can be called through the shared host.function call/method bridge.
- 2026-05-18 codex: Dynamic direct eval host-lane declaration persistence now extracts binding names from `var` destructuring patterns, so new object-pattern `var` bindings remain visible to later direct eval calls for the same caller env.
- 2026-05-18 codex: Added focused array-pattern coverage for the same runtime-source direct eval declaration persistence path, including rest bindings reused by a later direct eval call.
- 2026-05-18 codex: Extended dynamic direct eval host-lane function declaration persistence to generator declarations (`function* created() { ... }`), with later direct eval calls reusing the created binding.
- 2026-05-18 codex: Added async function declaration persistence coverage for runtime-source direct eval, verifying later direct eval calls can observe the created async function binding metadata.
- 2026-05-18 codex: Tightened dynamic direct eval host-lane function declaration collection so named function expressions are not persisted as eval-created bindings and do not shadow sloppy unbound global writes.
- 2026-05-18 codex: Added async/generator function expression name coverage for the same direct eval host-lane declaration collector boundary, keeping those expression-local names out of persisted eval bindings.
- 2026-05-18 codex: Replaced strict-caller runtime-source direct eval restricted-binding/delete regex scans with a code keyword scanner that skips string literals and comments, avoiding false SyntaxError results for text such as `"var arguments"` while preserving focused strict binding/delete guards.
- 2026-05-18 codex: Updated the reference test262 runner Node oracle prelude so `$262.evalScript(source)` performs indirect global eval instead of throwing an unsupported-stub error, aligning the runner with the audited `Dollar262Eval` / `host.eval.indirect` lowering slice.
- 2026-05-18 codex: Fixed JSONL reference coverage parity for `--no-server --no-semantic`: the legacy JSONL path now emits compile-only `build_pass` records instead of running Node oracle semantics, so `$262.evalScript` eval-code cases classify the same as server check mode after rebuilding the selected `ts2wasm` binary.
- 2026-05-18 codex: Updated the Node test262 oracle to execute non-module prepared sources with `vm.runInThisContext`, and routed `$262.evalScript` through the same script context helper. The focused Annex B `$262.evalScript` eval-code case now passes strict Node/iwasm semantic comparison.
- 2026-05-18 codex: Static direct eval completion now preserves multiple flattened Annex B block function declarations in execution order instead of dropping post-prefix declarations. `func-block-decl-eval-func-existing-block-fn-update.js` now passes strict Node/iwasm semantic comparison; the Annex B direct eval-code subset moved from 135 to 136 strict passes.
- 2026-05-18 codex: Refreshed `plans/eval-new-function-implementation-plan.md` to the archive15 support matrix and phase split. The plan now treats dynamic eval / dynamic Function as focused supported host-lane slices, keeps parser semantic rewrite removal as a regression guard, and identifies `EvalFragmentPlan` canonicalization as the next immediate implementation target.
- 2026-05-18 codex: Synchronized `current-state.md`, JavaScript feature docs, semantic feature matrix, and targeted unsupported eval fixture comments with the archive15 matrix. Also fixed the shadowed-eval fixture test expectation to assert the actual resolver contract: shadowed `eval` remains an ordinary call, so the later block-function name is unresolved rather than AOT-created.
- 2026-05-18 codex: Started PR B canonicalization by extending `EvalFragmentPlan` with explicit `scope_mode` and `host_policy`. Direct eval now records caller scope mode, indirect eval records global scope mode, static eval records AOT-only policy, and runtime-source eval records the exact direct/indirect host policy while preserving existing lowering behavior.
- 2026-05-18 codex: Lowering now consumes `EvalFragmentPlan.host_policy` instead of inferring host imports directly from `EvalKind`. Static AOT-only eval fragments that reach lowering without `expand_static_eval_fragments` now produce an `UnsupportedEval` diagnostic rather than silently falling into the runtime host eval lane.
- 2026-05-18 codex: Wrapped static eval completion steps in `EvalCompletionPlan`, so static eval expansion now returns a plan object instead of a bare `Vec<EvalCompletionStep>`. This preserves existing lowering/capture behavior while preparing declaration/completion metadata to move into plan-owned structures.
- 2026-05-18 codex: Added `EvalDeclarationPlan` metadata to `EvalCompletionPlan`. Static eval expansion now records eval `var` names and function hoists on the completion plan while retaining the existing `Hoist*` step compatibility path for current lowering/traversal code.
- 2026-05-18 codex: Moved top-level static eval declaration lowering to consume `EvalCompletionPlan.declarations` directly. The legacy `HoistVars` / `HoistFunctions` completion steps remain as a compatibility lowering path, but current static expansion no longer emits them as top-level sentinels.
- 2026-05-18 codex: Removed the legacy `HoistVars` / `HoistFunctions` completion-step variants after moving static eval declarations into `EvalDeclarationPlan`. Eval declaration hoisting now has a single plan-owned representation, while executable completion statements remain in `EvalCompletionStep`.
- 2026-05-18 codex: Added `FunctionConstructorPlan` with explicit call/new kind, args, AOT-vs-host policy, and span. Resolver, static expansion, lowering, and host-fact detection now consume the plan instead of destructuring loose `FunctionConstructor` fields.
- 2026-05-18 codex: Extended `FunctionConstructorPlan` with static Function constructor source metadata (`params` + `body`). Literal-only `Function` / `new Function` expansion now consumes plan-owned source splitting, while runtime-source constructors remain on the host-compile policy.
- 2026-05-18 codex: Added plan-owned Function constructor parse-goal and generated-function metadata. Static expansion now builds the synthetic anonymous function source from `StaticFunctionConstructorSource`, keeping FormalParameters / FunctionBody and generated `anonymous` function facts on the plan.
- 2026-05-18 codex: Propagated Function constructor generated-function metadata onto the expanded `FunctionExpr` and into lowering. Metadata name / capture suppression / Function-constructor detection now prefer plan-derived metadata instead of relying only on `FunctionExprOrigin::FunctionConstructor` hardcoding.
- 2026-05-18 codex: Reclassified static eval source `return` validation from `UnsupportedEval` to `UnsupportedSyntax`. The diagnostic display classifier now preserves this precise eval-code syntax error instead of bucketizing it as generic unsupported eval.
- 2026-05-18 codex: Extended `EvalScopeMode::Global` with explicit current-realm metadata. Indirect and optional eval plans now record global scope as `Global { realm: Current }`, preparing the global eval environment model instead of carrying a bare global marker.
- 2026-05-18 codex: Started the host external object/function contract cleanup by adding `HostExternalKind` and `StaticFacts` helpers for marking/querying host function handles and host external object locals. Dynamic Function call/construct/method lowering now uses the helper API instead of directly reaching into scattered fact sets.
- 2026-05-18 codex: Introduced a typed `DirectEvalEnvDescriptor` lowering layer for dynamic direct eval. Caller strict metadata and initialized env-cell bindings are collected as descriptor fields before lowering to the existing host ABI array, preparing descriptor v2 / mutation ledger work.
- 2026-05-18 codex: Started PR H burn-down classification by splitting `UnsupportedEval` coverage labels into `eval-direct-tdz`, `eval-static-aot`, `eval-code`, `function-constructor`, and `eval-unsupported` buckets across the test262 harness, reference coverage runner, legacy feature-label shell helper, and dashboard priority map.
- 2026-05-18 codex: Verified the PR H classification path with a small eval-code test262 JSONL run (`reference-coverage test262 --jsonl --path-filter language/eval-code --sample 3 --jobs 1 --no-semantic --no-dashboard-data`) and direct classifier checks for `eval-direct-tdz`, `eval-static-aot`, `eval-code`, and `function-constructor`.
- 2026-05-18 codex: Split the PR H test262 harness gaps further so `$262.evalScript` and cross-realm / `$262.createRealm` eval failures can classify as `test262-evalscript` and `test262-cross-realm` instead of being folded into generic eval unsupported.
- 2026-05-18 codex: Split the focused Node shim's dynamic direct eval write-back into an internal mutation ledger (`result`, `writes`, `createdBindings`) plus an explicit apply step. The host ABI still returns the current raw result, but the shim now mirrors the descriptor v2 / mutation-ledger shape needed by PR G.
- 2026-05-18 codex: Extended PR E static `Function` constructor metadata coverage to `Function.prototype.toString()`. Static constructor expansion now preserves the generated anonymous function source text through nested lowering, and the metadata fixture remains host-deny standalone.
- 2026-05-18 codex: Extended PR F dynamic host function handle metadata coverage to `f.toString()`. Host function handles now expose a bounded function-valued `toString` property in the Node shim, and lowering routes host function handle `toString()` through `host.function.callMethod` instead of Object.prototype fallback.
- 2026-05-18 codex: Added a PR D resolver regression guard for shadowed optional eval. Unshadowed `eval?.(source)` still classifies as indirect/global eval, while user-bound `eval?.(...)` remains an ordinary optional call and does not enter the intrinsic eval lane.
- 2026-05-18 codex: Added a PR C resolver guard and fix for static direct eval `var` declaration landing. Eval-created `var` declarations still predeclare into the caller when allowed, but a same-scope caller lexical binding now rejects the eval `var` as `DuplicateLocal` instead of silently bypassing the lexical collision.
- 2026-05-18 codex: Promoted the PR D shadowed optional eval guard to fixture coverage. `optional-eval-shadowed-ordinary-call.ts` now runs under Node differential comparison and host-deny standalone checks, proving user-bound `eval?.(...)` neither captures the intrinsic optional eval lane nor emits host eval capabilities.
- 2026-05-18 codex: Added PR C compiler regression coverage for static eval-source lexical/var declaration conflicts. `eval("let value = 1; var value = 2")` and `eval("var value = 1; let value = 2")` now stay guarded as `DuplicateLocal` during AOT eval expansion.
- 2026-05-18 codex: Advanced PR B by carrying eval completion context on `EvalCompletionPlan`. AOT static eval completion now records direct-caller vs global scope mode plus caller/eval strict flags, preserves that context through indirect-eval global collision rewrites, and lowering rejects any global eval completion plan that would land declarations in the caller scope.
- 2026-05-18 codex: Added PR C fixture coverage for eval lexical environment shadowing. `direct-eval-lexical-shadows-caller.ts` verifies static direct eval `let` / `const` declarations can shadow caller `let` / `var` bindings without mutating them, while remaining Node-differential and host-deny standalone.
- 2026-05-19 codex: Advanced PR D by giving static indirect eval `var` declarations an explicit `GlobalVarLet` completion step. AOT indirect eval no longer records these as caller declaration landings; lowering writes them through `globalThis.<name>`, and `indirect-eval-static-var-global.ts` guards Node parity plus no host-eval capabilities.
- 2026-05-19 codex: Extended PR D static indirect eval global landing to function declarations. AOT indirect eval now emits `GlobalFunctionDecl`, writes the generated function object to `globalThis.<name>`, tracks that global function-valued property for standalone `globalThis.fn()` calls, and guards the behavior with `indirect-eval-static-function-global.ts` under Node differential and host-deny checks.
- 2026-05-19 codex: Tightened the same PR D fixture/compiler guard so a static indirect eval-created global function body resolves caller-name collisions through `globalThis` instead of capturing the caller local.
- 2026-05-19 codex: Added PR D lexical-boundary coverage for static indirect eval. `indirect-eval-static-lexical-local.ts` verifies `let` / `const` declarations inside static indirect eval are eval-local, can shadow caller locals, do not leak to `globalThis`, and still require no host eval capability.
- 2026-05-19 codex: Added PR D global `var` hoist support for static indirect eval. AOT expansion now prepends global `var` hoist steps with `undefined` before executable completion steps, so branch-skipped `var` declarations still create global object bindings; `indirect-eval-static-var-hoist-global.ts` guards Node parity and host-deny standalone behavior.
- 2026-05-19 codex: Added PR D global function hoist coverage for static indirect eval. `indirect-eval-static-function-hoist-global.ts` verifies an eval-created global function can be called before its declaration and remains host-deny standalone.
- 2026-05-19 codex: Added PR E static `new Function(...)` prototype identity coverage. `function-constructor-new-static-prototype.ts` verifies the generated function object's `.prototype.constructor` points back to the function and constructed instances satisfy `instanceof`, while remaining host-deny standalone.
- 2026-05-19 codex: Tightened PR E static Function constructor host-deny coverage by adding the existing call-static, static construct, and zero-argument static Function fixtures to the standalone no-host manifest guard.
- 2026-05-19 codex: Extended PR E static Function constructor source classification beyond string literals to compile-time primitive literals. `Function(null)` and `new Function(true)` now apply plan-owned ToString source coercion, expand through the AOT lane, match Node output including `toString()`, and remain host-deny standalone.
- 2026-05-19 codex: Added PR B/D static non-string eval classification. `eval(1)`, `(0, eval)(true)`, `eval(null)`, and `eval(undefined)` now use `EvalSource::NonStringStatic` and return the argument value without host eval imports, matching Node's non-string eval semantics under Node differential and host-deny guards.
- 2026-05-19 codex: Extended the same non-string eval classification to optional eval. `eval?.(1)` now resolves as indirect/global AOT eval with `EvalSource::NonStringStatic`, matches Node's value-return behavior, and remains host-deny standalone.
- 2026-05-19 codex: Extended the static non-string eval fixtures to no-argument calls. `eval()`, `(0, eval)()`, and `eval?.()` now have Node differential and host-deny guards for the `undefined` result path.
- 2026-05-19 codex: Guarded the PR E Function constructor primitive-parameter path. `Function(1, "return 1")` now stays on the AOT parse-goal path and reports a static Function-constructor syntax diagnostic instead of falling back to `host.function.compile`.
- 2026-05-19 codex: Added the existing `new-function-constructor-static.ts` fixture to the standalone host-deny guard so static `new Function("return 1")` remains covered as AOT/no-host in both Node differential and manifest checks.
- 2026-05-19 codex: Added PR F host function handle propagation coverage for array elements returned by dynamic `Function(...)`. `function-constructor-dynamic-array-function-element-node-shim.ts` verifies callbacks loaded from a host external array remain callable through `host.function.call` with exact `host.function.compile` + `host.function.call` manifest imports.
- 2026-05-19 codex: Added PR F/G coverage for host function handles returned inside arrays from dynamic direct eval. `direct-eval-dynamic-array-function-element-node-shim.ts` verifies callbacks loaded from a host external eval result remain callable through `host.function.call`, with exact `host.eval.direct` + `host.function.call` manifest imports. The direct eval env descriptor guard is now source-aware for statically known runtime-source strings: it still rejects sources that reference a later TDZ binding, but no longer rejects merely because unrelated caller bindings are declared after the eval call.
- 2026-05-19 codex: Added a direct PR G regression for the source-aware guard. `direct-eval-dynamic-let-initializer-node-shim.ts` verifies `let result = eval(source)` compiles and runs when the statically known runtime source does not reference later caller bindings, while requiring only `host.eval.direct`.
- 2026-05-19 codex: Tightened the source-aware direct eval TDZ guard so identifier names inside single/double quoted source strings and comments do not count as caller binding references. `direct-eval-dynamic-tdz-name-in-string-node-shim.ts` guards the false-positive case while the existing TDZ conflict fixture still rejects a real later binding reference.
- 2026-05-19 codex: Extended the same source-aware TDZ scan to template literals. Raw template text such as `` `later` `` no longer triggers a false TDZ rejection, while template expressions such as `` `${later}` `` still reject as an UnsupportedEval descriptor-v2 boundary.
- 2026-05-19 codex: Advanced PR G normal-code visibility for runtime-source direct eval-created `var` bindings when the source value is statically known. Resolver/lowering now predeclare simple eval-created `var`/function names as caller env cells, so `let source = "var created = 7; created"; eval(source); created` reads through the direct-eval mutation ledger; `direct-eval-dynamic-new-var-normal-code-node-shim.ts` guards the focused path with host-deny coverage.
- 2026-05-19 codex: Extended the same PR G normal-code visibility path to statically known runtime-source direct eval function declarations. Eval-created function names now retain host function handle facts on the predeclared env cell, so `created()` lowers through exact `host.eval.direct` + `host.function.call`; `direct-eval-dynamic-new-function-normal-code-node-shim.ts` guards Node-shim stdout, host-deny rejection, and exact manifest imports.
- 2026-05-19 codex: Extended PR G normal-code visibility to statically known runtime-source direct eval `var` destructuring declarations. Resolver/lowering now extract object and array binding names from eval source `var` patterns, predeclare them as caller env cells, and guard normal-code reads with focused node-shim and host-deny fixtures.
- 2026-05-19 codex: Centralized the lightweight statically known eval-source declaration scanner in `ts2wasm-resolve::direct_eval_source`. Resolver predeclaration and IR lowering now consume the same scanner for eval-created `var` destructuring and function declaration names, reducing descriptor-v2 drift while the canonical `EvalDeclarationPlan` migration remains open.
- Static direct eval("expr") is implemented via compile-time expansion (8fe6e97fa) and has since gained caller-local reads, guarded side-effect completion, static indirect eval AOT slices, dynamic host-lane slices, and `new eval(...)` TypeError parity guards.
