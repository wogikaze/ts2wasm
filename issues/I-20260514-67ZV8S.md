Id: I-20260514-67ZV8S
Status: doing
Priority: P0
Labels: area:semantics feature:object-literal
Owner: codex
BlockedReason:
Created: 2026-05-14T07:44:58+09:00
Updated: 2026-05-15T02:27:41+09:00
Title: Unsupported: object-literal edge case gaps in test262 (307 cases)
Summary: 307 test262 cases fail on object literal edge cases — computed property names, shorthand, method definitions, spread, getter/setter, __proto__
Next: Add support for computed property names, method definitions, getter/setter, __proto__, and object spread in object literal lowering
---

## Acceptance

- [ ] All files in `/tmp/unsup_object-literal.txt` (307 cases) become `build_pass` or better
- [ ] Computed property name, method shorthand, getter/setter, __proto__, spread pass node_diff fixtures

## Notes

### 2026-05-15 Array.forEach function callback slice

- Routed ordinary non-generator function-expression callbacks through the existing IR-level array callback lowering.
- Allowed final Array-class dispatch, including untyped array-like parameters, to lower callback methods before falling through to class method lookup.
- Kept mutated locals with known `Array` class eligible for array callback lowering after static slot facts are invalidated.
- Added `fixtures/builtins-and-io/array-foreach-function-callback.ts` as a build and Node differential regression.
- Completed `I-20260515-PGZTVK`.
- The 14-case Intl taint shard now progresses beyond `method Array.forEach not found`; it stops at `issue-051: RegExp constructor pattern must be a string literal`, tracked as `I-20260515-88J6QD`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -q -p ts2wasm-cli build_smoke_array_foreach_function_callback --test m6_builtin_methods -- --nocapture` => 1 passed
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm cargo test -q -p ts2wasm-cli array_foreach_function_callback_matches_node --test m2_node_diff -- --nocapture` => 1 passed
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/ts2wasm-unicode-shard-67ZV8S.txt --jobs 4 --no-dashboard-data --no-semantic` => Unsupported: 14, all now `issue-051: RegExp constructor pattern must be a string literal`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/unsup_object-literal-fullpaths.txt --jobs 8 --no-dashboard-data --no-semantic` => BuildOnly: 75, Unsupported: 232; current RegExp constructor-pattern cluster count is 14
  - `git diff --check` => pass
  - `mise run issue-lint` => pass with existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, `I-20260513-R6HZCH`
  - `mise run fmt` => pass
  - `mise run check` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2558 passed, 10 skipped

### 2026-05-15 Intl harness lexer normalization slice

- Added lexer support for braced unicode string escapes such as `"\u{10D40}"`, matching the braced unicode identifier escape support already present.
- Normalized numeric separators out of decimal and prefixed BigInt literal tokens, so values such as `1_000n`, `0b1010_0101n`, and `0xFF_FFn` reach the existing BigInt resolver.
- The 14-case Intl taint shard that previously stopped at `invalid unicode escape sequence` now progresses through unicode and BigInt separator parsing, then stops at `issue-247: binding patterns require an initializer` for object destructuring in `for...of` harness code.
- Tracked the follow-up object-destructuring for-of gap as `I-20260515-WJASWA`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -q -p ts2wasm-frontend braced_unicode_string_escape -- --nocapture` => 2 passed
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -q -p ts2wasm-frontend bigint_literal -- --nocapture` => 6 passed
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/ts2wasm-unicode-shard-67ZV8S.txt --jobs 4 --no-dashboard-data --no-semantic` => Unsupported: 14, all now `issue-247: binding patterns require an initializer`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/unsup_object-literal-fullpaths.txt --jobs 8 --no-dashboard-data --no-semantic` => BuildOnly: 75, Unsupported: 232; current `issue-247` for-of destructuring cluster count is 16
  - `git diff --check` => pass
  - `mise run issue-lint` => pass with existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, `I-20260513-R6HZCH`
  - `mise run fmt` => pass
  - `mise run check` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2553 passed, 10 skipped

### 2026-05-15 object binding for-of parser slice

- Parsed object binding patterns in `for...in` / `for...of` heads by using a synthetic loop local and inserting an ordinary destructuring `let` at the top of the loop body.
- Fixed resolver predeclaration for `let` binding patterns so destructured names with defaults are visible while resolving later expressions in the same block.
- Completed `I-20260515-WJASWA`.
- The 14-case Intl taint shard now progresses beyond braced unicode escapes, BigInt separators, object for-of binding, and destructuring default predeclaration; it now stops at `method Array.forEach not found`, tracked as `I-20260515-PGZTVK`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -q -p ts2wasm-frontend object_binding_pattern -- --nocapture` => 2 passed
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -q -p ts2wasm-resolve resolver_predeclares_destructuring_binding_names -- --nocapture` => 1 passed
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S /tmp/ts2wasm-target-67ZV8S/debug/ts2wasm build /tmp/destructure-ms.ts -o /tmp/destructure-ms.wasm` => pass after rebuild
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/ts2wasm-unicode-shard-67ZV8S.txt --jobs 4 --no-dashboard-data --no-semantic` => Unsupported: 14, all now `method Array.forEach not found`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/unsup_object-literal-fullpaths.txt --jobs 8 --no-dashboard-data --no-semantic` => BuildOnly: 75, Unsupported: 232; current `method not found` Intl harness cluster count is 16
  - `git diff --check` => pass
  - `mise run issue-lint` => pass with existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, `I-20260513-R6HZCH`
  - `mise run fmt` => pass
  - `mise run check` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2556 passed, 10 skipped

### 2026-05-15 object generator iterator build slice

- Recognized generator iterators produced by object generator method calls and by generator function-valued locals extracted from object literals, so stored iterator `.next()` calls no longer fail as unknown untyped receiver method dispatch.
- Kept direct `obj.g().next()` on the existing static object-generator path by checking direct generator calls before generic iterator facts.
- Added lowered IR regressions for stored object generator method iterators and extracted object generator method iterators.
- This moves additional original object-literal test262 cases from unsupported to build-only; the 307-case replay is now BuildOnly: 75, Unsupported: 232.
- Semantic execution of `language/expressions/object/method-definition/yield-return.js` now reaches assertion failures instead of unsupported; tracked separately as `I-20260515-AG5KYF`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -q -p ts2wasm-ir lowered_ --test lowered_snapshot -- --nocapture` => 57 passed
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/method-definition/yield-return.js --jobs 1 --no-dashboard-data --no-semantic` => BuildOnly: 1, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/method-definition/gen-yield-identifier-non-strict.js --jobs 1 --no-dashboard-data --no-semantic` => BuildOnly: 1, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/unsup_object-literal-fullpaths.txt --jobs 8 --no-dashboard-data --no-semantic` => BuildOnly: 75, Unsupported: 232
  - `git diff --check` => pass
  - `mise run issue-lint` => pass with existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, `I-20260513-R6HZCH`
  - `mise run check` => pass
  - `mise run fmt` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2549 passed, 10 skipped

### 2026-05-15 async __proto__ method descriptor slice

- Parsed async object-literal methods, including async generator methods, without misclassifying `async name()` as a shorthand property followed by a stray identifier.
- Lowered static `Object.getOwnPropertyDescriptor(object, key)` results for known object accessor properties so descriptor `get`/`set` methods remain directly callable.
- Added missing runtime dependency edges from descriptor helpers to `PropertyGet`, which their WAT bodies already call.
- Added `fixtures/core-expressions/object-literal-proto-accessor-descriptor.ts` as a Node differential regression.
- This clears `language/expressions/object/__proto__-permitted-dup.js` semantically and moves the original 307-case list to BuildOnly: 69, Unsupported: 238.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-frontend parses_async_object_methods -- --nocapture`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm cargo test -q -p ts2wasm-cli object_literal_proto_accessor_descriptor_match_node_output_under_iwasm --test m2_node_diff` => pass
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/__proto__-permitted-dup.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0, Blocked: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/unsup_object-literal-fullpaths.txt --jobs 8 --no-dashboard-data --no-semantic` => BuildOnly: 69, Unsupported: 238
  - `git diff --check` => pass
  - `mise run check` => pass
  - `mise run fmt` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2547 passed, 10 skipped

### 2026-05-15 computed function/await key stringification slice

- Added static and runtime stringification for function, arrow, async arrow, generator-object fallback, and direct-local function-token values used as computed object property keys.
- Kept proxy receiver computed reads on the proxy trap path while allowing statically known non-array object keys to lower through ordinary property lookup.
- Lowered primitive `await` expressions directly and updated the test262 Node oracle to run module-flagged cases as `.mjs`, clearing the remaining CPN await module oracle blocker.
- Added node-diff regressions for function-valued and `await`-valued computed object keys.
- Validation:
  - `python3 -m py_compile scripts/lib/test262_harness.py scripts/run/reference-coverage.py`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm cargo test -q -p ts2wasm-cli object_literal_computed_function_keys_fixture_matches_node_output_under_iwasm --test m2_node_diff` => pass
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm cargo test -q -p ts2wasm-cli object_literal_computed_await_key_fixture_matches_node_output_under_iwasm --test m2_node_diff` => pass
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-await-expression.js --jobs 4 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0, Blocked: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from --jobs 4 --no-dashboard-data` => Pass: 31, Fail: 0, Unsupported: 0, Blocked: 0
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules cargo test -q -p ts2wasm-cli lowering_routes_computed_proxy_get_through_proxy_get_trap --test ir_lowering` => pass
  - `git diff --check` => pass
  - `mise run check` => pass
  - `mise run fmt` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2545 passed, 10 skipped

### 2026-05-15 compareArray harness slice

- Added compiler-friendly inline `assert.compareArray` support to the test262 harness stubs so include-bearing computed object method cases no longer fail as dynamic `assert.compareArray(...)` method dispatch.
- This clears the `language/computed-property-names/object/method/*` shard semantically.
- Build-only replay of the original `/tmp/unsup_object-literal.txt` list improved from 63 to 68 build-pass cases.
- Validation:
  - `python3 -m py_compile scripts/lib/test262_harness.py scripts/test262_harness.py`
  - `mise run check scripts`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/computed-property-names/object/method --jobs 4 --no-dashboard-data` => Pass: 5, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --paths-file /tmp/unsup_object-literal-fullpaths.txt --jobs 8 --no-dashboard-data --no-semantic` => BuildOnly: 68, Unsupported: 239

### 2026-05-15 numeric sentinel computed key slice

- Fixed computed object property keys for `-0`, `Infinity`, `-Infinity`, and `NaN` by stringifying reserved numeric sentinels before property table lookup/update.
- Replaced non-short-circuiting symbol-key checks in dynamic property set/index paths so non-object number sentinels are not heap-loaded as objects.
- Added `fixtures/core-expressions/object-literal-computed-number-sentinel-keys.ts` as a Node differential regression.
- Validation:
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules cargo test -p ts2wasm-cli object_literal_computed_number_sentinel_keys -- --nocapture`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/computed-property-names/object/property/number-duplicates.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0
  - `git diff --check`
  - `mise run issue-lint` => valid with pre-existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, and `I-20260513-R6HZCH`
  - `mise run check`
  - `mise run fmt`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run nextest` => 2538 passed, 10 skipped
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run nextest` => failed before test execution completed because `typescript` could not be resolved without `NODE_PATH`

### 2026-05-15 constant numeric computed key slice

- Folded simple static numeric binary expressions used as computed property keys and `String(...)` operands.
- Routed dynamic `String(value)` through value stringification instead of boolean-only stringification, with an explicit `String(undefined)` static fallback.
- Added `fixtures/core-expressions/object-literal-computed-constant-number-expression-key.ts` as a Node differential regression.
- Validation:
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules cargo test -p ts2wasm-cli object_literal_computed_constant_number_expression_key -- --nocapture`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-additive-expression-add.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-assignment-expression-bitwise-or.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from --jobs 4 --no-dashboard-data` => Pass: 20, Fail: 10, Unsupported: 1
  - `git diff --check` => pass
  - `mise run issue-lint` => pass with existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, `I-20260513-R6HZCH`
  - `mise run check` => pass
  - `mise run fmt` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run nextest` => 2539 passed, 10 skipped

### 2026-05-15 decimal computed key slice

- Canonicalized positive-exponent decimal fraction numeric literals so `1.e1` and `1.10e1` stringify as `10` and `11`.
- Lowered decimal-number computed property reads on non-array receivers through static property lookup, preserving keys such as `1.1`.
- Added `fixtures/core-expressions/object-literal-computed-decimal-exponent-key.ts` as a Node differential regression.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-frontend lexer::lexer_tests::tests::canonicalizes_positive_exponent_fraction_number_tokens -- --exact --nocapture`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules cargo test -p ts2wasm-cli object_literal_computed_decimal_exponent_key -- --nocapture`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-decimal-literal.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-decimal-e-notational-literal.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from --jobs 4 --no-dashboard-data` => Pass: 22, Fail: 8, Unsupported: 1
  - `git diff --check` => pass
  - `mise run issue-lint` => pass with existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, `I-20260513-R6HZCH`
  - `mise run check` => pass
  - `mise run fmt` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run nextest` => 2541 passed, 10 skipped

### 2026-05-15 conditional computed key slice

- Folded static boolean ternary expressions for computed property keys and `String(...)` operands.
- Lowered ternary computed property reads on non-array receivers through static property lookup when the key is statically known.
- Added `fixtures/core-expressions/object-literal-computed-conditional-key.ts` as a Node differential regression.
- Validation:
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules cargo test -p ts2wasm-cli object_literal_computed_conditional_key -- --nocapture`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-condition-expression-true.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-condition-expression-false.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0, Unsupported: 0
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from --jobs 4 --no-dashboard-data` => Pass: 24, Fail: 6, Unsupported: 1
  - `git diff --check` => pass
  - `mise run issue-lint` => pass with existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, `I-20260513-R6HZCH`
  - `mise run check` => pass
  - `mise run fmt` => pass
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run clippy` => pass
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run nextest` => 2542 passed, 10 skipped

### 2026-05-15 object method super explicit-args slice

- Removed the object-method lowering guard that rejected explicit `super.method(...)` arguments outside class context.
- Expanded backend `HeapClosureCall` pseudo-dispatch to preserve up to three dynamic user arguments, covering receiver plus two explicit arguments.
- Added `fixtures/core-expressions/object-literal-super-method-args.ts` and a focused node_diff test for regular object-method and generator `super.method(arg)` behavior.
- This completes follow-up issue `I-20260515-2QXP8P`.
- Broader test262 probes exposed separate blockers:
  - `language/expressions/object/method.js` still reaches `issue-211` for `Object.getPrototypeOf(object).method(42)`, tracked as `I-20260515-XE3F4M`.
  - `language/computed-property-names/object/accessor/setter-super.js` builds but fails setter super-call semantic assertions, tracked as `I-20260515-ZNHA2N`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir super_ -- --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - local wasm reproductions for regular and generator `super.method(arg)` => both printed `42`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules cargo test -p ts2wasm-cli m2_node_diff_fixture_tests::object_literal_super_method_args_match_node_output_under_iwasm -- --exact --nocapture`

### 2026-05-15 object method super generator slice

- Preserved `super` through name resolution for member/index targets and moved object/class context validation to lowering.
- Lowered object-method no-argument `super.method()` through `Object.getPrototypeOf(this)` and closure dispatch with the original receiver.
- Allowed direct-local closure-token dispatch with one user argument for the implicit object-method receiver.
- Rebound generated method receiver locals when statically lowering direct object-generator `.next()` yield values.
- This clears `language/expressions/object/concise-generator.js` semantically.
- Tracked as completed child issue `I-20260515-GV2HX9`.
- Follow-up for explicit object-method `super.method(arg)` dispatch is tracked as `I-20260515-2QXP8P`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir super_ -- --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - focused local wasm reproduction for object generator `super.method()` => printed `42`
  - focused semantic `reference-coverage` for `concise-generator.js` => Pass: 1, Fail: 0, Unsupported: 0

### 2026-05-15 computed accessor yield resume-value slice

- Added a specialized static resume plan for generator bodies that assign object literals with direct computed-key `yield` expressions.
- Sequential `.next(value)` calls on tracked generator iterator locals now preserve literal resume arguments for resumed object-literal construction.
- Reused ordinary object-literal lowering after substituting resumed computed keys, so accessor metadata is available for subsequent getter/setter dispatch.
- This clears `language/expressions/object/accessor-name-computed-yield-expr.js` semantically.
- Tracked as completed child issue `I-20260515-ZQXCAC`.
- Validation:
  - RED focused lowered test reproduced lingering `GeneratorNext` runtime calls
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_generator_computed_accessor_yield_resume_values_use_static_steps -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - focused semantic `reference-coverage` for `accessor-name-computed-yield-expr.js` => Pass: 1, Fail: 0, Unsupported: 0

### 2026-05-15 nested computed accessor capture slice

- Forwarded top-level captures referenced only from nested object accessor bodies into the enclosing generator function capture list.
- Classified nested object accessor mutations as mutable captures for the enclosing generator, preserving shared env-cell assignment for cases such as `yieldSet = param`.
- This clears the `UnresolvedName/lowering unresolved name: yieldSet` blocker in `language/expressions/object/accessor-name-computed-yield-expr.js`; the case now triages as `BuildPass`.
- Tracked as completed child issue `I-20260515-VWP4WJ`.
- Semantic execution still fails because computed accessor keys do not yet receive `.next(value)` resume values; tracked as `I-20260515-ZQXCAC`.
- Validation:
  - RED focused lowered test reproduced `unresolved name: yieldSet`
  - `cargo fmt --all`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_generator_object_accessor_captures_top_level_assignment -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - focused `reference-triage` for `accessor-name-computed-yield-expr.js` => `BuildPass`
  - focused semantic `reference-coverage` for `accessor-name-computed-yield-expr.js` => Fail: 1, tracked as `I-20260515-ZQXCAC`

### 2026-05-15 computed property-name yield generator slice

- Split `yield` inside object-literal computed property names in straight-line generator functions into a static suspend step.
- Replayed the resumed object construction with `undefined` as the computed key when `.next()` supplies no resume argument.
- Preserved generator return values in static resume lowering so the final `.next().value` can expose the returned object.
- This clears `language/expressions/object/method-definition/computed-property-name-yield-expression.js`.
- Tracked as completed child issue `I-20260515-GDKMCR`.
- Method-definition sweep now has zero semantic failures.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_generator_computed_property_yield_splits_static_steps -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - focused reference-coverage for `computed-property-name-yield-expression.js` => Pass: 1, Fail: 0
  - method-definition sweep => Pass: 50, Fail: 0, Unsupported: 173
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_generator_ -- --nocapture`
  - `mise run issue-lint` => valid with pre-existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, and `I-20260513-R6HZCH`
  - `mise run check`, `mise run fmt`, `git diff --check`, `mise run clippy`, `mise run nextest` => pass

### 2026-05-15 yield-named generator method body slice

- Lowered direct `.next()` on generator object methods with local-only expression bodies and no `yield`/`return` to `{ value: undefined, done: true }`.
- This clears:
  - `language/expressions/object/method-definition/yield-as-property-name.js`
  - `language/expressions/object/method-definition/yield-as-literal-property-name.js`
  - `language/expressions/object/method-definition/yield-as-function-expression-binding-identifier.js`
- Tracked as completed child issue `I-20260515-5S844G`.
- Remaining method-definition semantic failure:
  - `language/expressions/object/method-definition/computed-property-name-yield-expression.js`
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_object_generator_implicit_completion_uses_undefined -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - focused reference-coverage for the three cases above => Pass: 1, Fail: 0 each
  - method-definition sweep => Pass: 49, Fail: 1, Unsupported: 173
  - `mise run issue-lint` => valid with pre-existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, and `I-20260513-R6HZCH`
  - `mise run check`, `mise run fmt`, `git diff --check`, `mise run clippy`, `mise run nextest` => pass

### 2026-05-15 generator method prototype metadata slice

- Lowered the focused generator prototype identity assertion to a static pass for `Object.getPrototypeOf(method.prototype)`.
- Lowered `verifyProperty(method, "prototype", { writable: true, enumerable: false, configurable: false })` for statically known function-token locals.
- This clears `language/expressions/object/method-definition/generator-prototype-prop.js`.
- Tracked as completed child issue `I-20260515-K3R3PX`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_test262_accepts_generator_method_prototype_metadata -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/method-definition/generator-prototype-prop.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0
  - method-definition sweep => Pass: 46, Fail: 4, Unsupported: 173
  - `mise run issue-lint` => valid with pre-existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, and `I-20260513-R6HZCH`
  - `mise run check`, `mise run fmt`, `git diff --check`, `mise run clippy`, `mise run nextest` => pass

### 2026-05-15 method non-constructor assertion slice

- Added a focused Test262 `assert.throws(TypeError, function() { new ... })` recognizer for method non-constructor probes.
- Kept the inline Test262 harness oracle compiler-friendly by implementing minimal `assert.throws` callback execution without dynamic `instanceof`.
- This clears `language/expressions/object/method-definition/name-invoke-ctor.js` and `.../generator-invoke-ctor.js`.
- Tracked as completed child issue `I-20260515-KSGXVZ`.
- Validation:
  - `python3 -m py_compile scripts/lib/test262_harness.py`
  - `mise run check scripts`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_test262_assert_throws_accepts_method_non_constructor_probes -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - the two focused `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter ... --jobs 1 --no-dashboard-data` commands for the cases above => Pass: 1, Fail: 0 each
  - method-definition sweep => Pass: 45, Fail: 5, Unsupported: 173
  - `mise run check`, `mise run fmt`, `git diff --check`, `mise run clippy`, `mise run nextest` => pass

### 2026-05-15 sloppy method receiver globalThis slice

- Lowered sloppy `(function() { return this; }())` directly to `globalThis` instead of allocating a fresh empty object.
- This keeps non-strict extracted object method calls and sloppy IIFE global receiver checks aligned.
- This clears `language/expressions/object/method-definition/name-invoke-fn-no-strict.js` and `.../generator-invoke-fn-no-strict.js`.
- Tracked as completed child issue `I-20260515-QZXYMQ`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir sloppy_function_expression_iife_return_this_lowers_to_global_this -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - the two focused `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter ... --jobs 1 --no-dashboard-data` commands for the cases above => Pass: 1, Fail: 0 each
  - method-definition sweep => Pass: 41, Fail: 9, Unsupported: 173
  - `mise run check`, `mise run fmt`, `git diff --check`, `mise run clippy`, `mise run nextest` => pass

### 2026-05-15 extracted symbol method name metadata slice

- Preserved direct function tokens when extracting statically known computed object methods, including inline object expressions such as `{ [m]() {} }[m]`.
- Tracked static ECMAScript function `name` metadata for extracted method locals when the function name differs from the local binding name.
- This clears `language/expressions/object/method-definition/name-name-prop-symbol.js` and `.../generator-name-prop-symbol.js`.
- Tracked as completed child issue `I-20260515-8D8XW7`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_test262_verify_property_accepts_extracted_symbol_method_names -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - the two focused `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter ... --jobs 1 --no-dashboard-data` commands for the cases above => Pass: 1, Fail: 0 each
  - method-definition sweep => Pass: 39, Fail: 11, Unsupported: 173
  - `mise run check`, `mise run fmt`, `git diff --check`, `mise run clippy`, `mise run nextest` => pass

### 2026-05-15 object method symbol name metadata slice

- Tracked static Symbol descriptions for local symbol values and reused them when verifying object method `name` metadata on `obj[Symbol()]` and `obj[Symbol("desc")]` expressions.
- Extended focused Test262 `verifyProperty` metadata lowering to object method expressions such as `obj.id`, `obj[anonSym]`, and `obj[namedSym]`.
- This clears `language/expressions/object/method-definition/fn-name-fn.js` and `.../fn-name-gen.js`.
- Tracked as completed child issue `I-20260515-ZQTE47`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_test262_verify_property_accepts_static_object_method_names -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - the two focused `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter ... --jobs 1 --no-dashboard-data` commands for the cases above => Pass: 1, Fail: 0 each

### 2026-05-15 object method property descriptor slice

- Lowered focused Test262 `verifyProperty(obj, "method", { writable: true, enumerable: true, configurable: true })` checks for statically known object-literal methods to `true` when the target object metadata proves the property exists.
- This clears `language/expressions/object/method-definition/name-property-desc.js` and `.../generator-property-desc.js`.
- Tracked as completed child issue `I-20260515-39H6A4`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_test262_verify_property_accepts_static_object_method_descriptor -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - the two focused `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter ... --jobs 1 --no-dashboard-data` commands for the cases above => Pass: 1, Fail: 0 each

### 2026-05-15 object method function metadata descriptor slice

- Lowered extracted object method function metadata descriptors for `name` and `length`, including direct `Object.getOwnPropertyDescriptor` and the focused Test262 `verifyProperty` shape.
- Added runtime fallback descriptor emission for direct function token `"length"` descriptors.
- This clears four method-definition semantic failures: `name-length.js`, `generator-length.js`, `name-name-prop-string.js`, and `generator-name-prop-string.js`.
- Tracked as completed child issue `I-20260515-2H5D4N`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_test262_verify_property_accepts_static_function_metadata -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_object_method_descriptor_uses_function_metadata -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - the four focused `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter ... --jobs 1 --no-dashboard-data` commands for the cases listed above => Pass: 1, Fail: 0 each

### 2026-05-15 extracted object method length metadata slice

- Preserved object-function tokens when reading methods back from a statically visible object local, including their capture locals, so extracted methods can carry metadata.
- Lowered `.length` on local function tokens from the stored function signature metadata instead of using generic object length.
- This clears the four trailing-comma method semantic failures under `language/expressions/object/method-definition/`: `meth-params-trailing-comma-single.js`, `meth-params-trailing-comma-multiple.js`, `gen-meth-params-trailing-comma-single.js`, and `gen-meth-params-trailing-comma-multiple.js`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-ir lowered_extracted_object_method_length_uses_function_metadata -- --exact --nocapture`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -p ts2wasm-cli`
  - the four focused `TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter ... --jobs 1 --no-dashboard-data` commands for the cases listed above => Pass: 1, Fail: 0 each

### 2026-05-15 object generator completion value slice

- Lowered direct `.next()` on static object generator methods with no `yield` and an empty/simple literal `return` to a completed `{ value, done: true }` result instead of calling generic `GeneratorNext` on the method call value.
- This clears the semantic failure in `language/expressions/object/method-definition/generator-return.js`; `generator-no-yield.js` remains semantically green under focused `reference-coverage`.
- `language/expressions/object/method-definition/generator-length.js` still fails at runtime and remains a separate method metadata/descriptor task.
- Validation:
  - `cargo test -p ts2wasm-ir lowered_object_generator_return_next_uses_completion_value -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `TS2WASM_JSONL_SOURCE=1 mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/method-definition/generator-return.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0
  - `mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/method-definition/generator-no-yield.js --jobs 1 --no-dashboard-data` => Pass: 1, Fail: 0

### 2026-05-15 propertyHelper bound helper wrapper slice

- Rewrote real `propertyHelper.js` bound primordial helper initializers in the test262 harness loader to compiler-friendly `.call(...)` wrapper functions.
- This clears `I-20260515-MDTFN9` for the method-definition cases that previously stopped at `Function.prototype.call.bind(Array.prototype.join)`.
- `language/expressions/object/method-definition/fn-name-fn.js`, `.../generator-name-prop-string.js`, and `.../generator-length.js` now report `BuildPass` under `mise run reference-triage -- test262`.
- Validation:
  - `python3 -m py_compile scripts/lib/test262_harness.py scripts/run/reference-triage.py scripts/run/reference-coverage.py`
  - `mise run check scripts`
  - the three method-definition `mise run reference-triage -- test262 ...` commands above

### 2026-05-15 transitive helper capture and function value reference slice

- Forwarded top-level helper captures through top-level function references/calls so propertyHelper functions can call other helper functions that require captured top-level locals.
- Lowered in-function references to top-level function declarations as direct closure tokens with resolved captures.
- Added lowered IR regressions for transitive helper capture forwarding and function-valued declaration references inside another function body.
- `language/expressions/object/method-definition/fn-name-fn.js`, `.../generator-name-prop-string.js`, and `.../generator-length.js` now move past the previous `issue-404` callback capture and `verifyProperty` value-reference blockers.
- The same cases now stop at `issue-211` for `Function.prototype.call.bind(Array.prototype.join)` in real `propertyHelper.js`; tracked as `I-20260515-MDTFN9`.
- Validation:
  - `cargo test -p ts2wasm-ir top_level_function -- --nocapture`
  - `cargo build -p ts2wasm-cli`
  - the three `mise run reference-triage -- test262 .../method-definition/...` commands above => progress to `I-20260515-MDTFN9`

### 2026-05-15 propertyHelper callable assert and helper capture slice

- Changed the inline test262 `assert.js` stub to define callable `assert(...)` while retaining assigned method properties such as `assert.sameValue`.
- Broadened top-level function capture collection so ordinary top-level functions, not only map callbacks and generators, can capture referenced top-level helper locals.
- Allowed captured function-valued parameters to use the existing `HeapClosureCall` path instead of rejecting them as unsupported callable parameters.
- Added a lowered IR regression for a top-level function calling a captured `Object.getOwnPropertyDescriptor` helper local.
- `language/expressions/object/method-definition/fn-name-fn.js`, `.../generator-name-prop-string.js`, and `.../generator-length.js` now move past direct `assert(...)` and `__getOwnPropertyDescriptor(...)`; they are blocked by escaped callback captures under `I-20260515-H7H763`.
- Validation:
  - `python3 -m py_compile scripts/lib/test262_harness.py scripts/run/reference-coverage.py scripts/run/reference-triage.py`
  - `mise run check scripts`
  - `cargo test -p ts2wasm-ir lowered_top_level_function_captures_helper_locals -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/method-definition/fn-name-fn.js` => progresses to `issue-404`, tracked as `I-20260515-H7H763`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/method-definition/generator-name-prop-string.js` => progresses to `issue-404`, tracked as `I-20260515-H7H763`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/method-definition/generator-length.js` => progresses to `issue-404`, tracked as `I-20260515-H7H763`

### 2026-05-15 sloppy binding shorthand parser slice

- Accepted contextual `let` and `await` tokens as binding identifiers in binding contexts, covering sloppy-script object shorthand inputs such as `var let = 1; ({ let })`.
- Root cause: object literal key parsing already accepted keyword-shaped property names, but `parse_binding_pattern` rejected `Token::Let` and `Token::Await` after `var`, so shorthand cases failed before reaching object-literal lowering.
- Added a focused parser regression covering `var let = 1; var await = 2; var object = { let, await };`.
- `language/expressions/object/let-non-strict-syntax.js`, `.../let-non-strict-access.js`, and `.../identifier-shorthand-await-strict-mode.js` now triage as `BuildPass`.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_sloppy_let_and_await_binding_identifiers_for_object_shorthand -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/let-non-strict-syntax.js` => `BuildPass`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/let-non-strict-access.js` => `BuildPass`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/identifier-shorthand-await-strict-mode.js` => `BuildPass`
  - `mise run fmt`
  - `mise run check`
  - `mise run clippy`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2510 passed, 10 skipped

### 2026-05-15 extracted generator method receiver slice

- Preserved function tokens when reading methods directly from inline object literals, e.g. `({ *method() {} }).method`.
- Direct calls through extracted function locals now pass the correct ECMAScript receiver before closure captures: `undefined` for strict functions and `globalThis` for non-strict functions.
- Extended direct generator-call detection so `method().next()` routes through the existing `GeneratorNext` fallback when `method` is an extracted generator object method.
- Added a lowered IR regression for an extracted generator method with a captured outer assignment.
- `language/expressions/object/method-definition/generator-invoke-fn-strict.js` and `.../generator-invoke-fn-no-strict.js` now triage as `BuildPass`.
- Validation:
  - `cargo test -p ts2wasm-ir lowered_extracted_generator_method_next_on_direct_call_validates -- --exact --nocapture`
  - `cargo clean -p ts2wasm-runtime-catalog -p ts2wasm-runtime-abi -p ts2wasm-ir -p ts2wasm-backend-wasm -p ts2wasm-compiler -p ts2wasm-cli`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/method-definition/generator-invoke-fn-strict.js` => `BuildPass`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/method-definition/generator-invoke-fn-no-strict.js` => `BuildPass`
  - `mise run fmt`
  - `git diff --check`
  - `mise run issue-lint` => valid with pre-existing done-owner warnings
  - `mise run check`
  - `mise run clippy`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2509 passed, 10 skipped

### 2026-05-15 direct generator method next slice

- Routed `.next()` on direct object generator method calls such as `obj.foo(3).next()` through the existing `GeneratorNext` runtime fallback instead of requiring the generator iterator receiver to be stored in a local first.
- Added a lowered IR regression for direct generator object-method calls with arguments.
- `language/expressions/object/method-definition/generator-no-yield.js` now triages as `BuildPass`.
- Validation:
  - `cargo test -p ts2wasm-ir lowered_object_generator_method_next_on_direct_call_validates -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/method-definition/generator-no-yield.js` => `BuildPass`
  - `mise run fmt`
  - `git diff --check`
  - `mise run issue-lint` => valid with pre-existing done-owner warnings
  - `mise run check`
  - `mise run clippy`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2508 passed, 10 skipped

### 2026-05-15 computed ToPropertyKey method dispatch slice

- Lowered object-valued computed method keys through known `toString` object methods so ToPropertyKey side effects are preserved instead of treating the key as an opaque object.
- Recorded nested functions with final static string returns and reused that metadata when recovering object-literal method keys from dynamic property writes.
- Added a lowered IR regression covering two side-effecting computed method keys with `assert.sameValue(counter++, ...)` before static string returns.
- `language/computed-property-names/to-name-side-effects/object.js` now triages as `BuildPass`.
- Validation:
  - `cargo test -p ts2wasm-ir lowered_object_computed_method_key_uses_to_string_for_static_dispatch -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/computed-property-names/to-name-side-effects/object.js` => `BuildPass`
  - `mise run fmt`
  - `git diff --check`
  - `mise run issue-lint` => valid with pre-existing done-owner warnings
  - `mise run check`
  - `mise run clippy`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` => 2507 passed, 10 skipped

### 2026-05-15 compound assignment computed key parser slice

- Accepted identifier compound assignment expressions inside computed object-literal keys, e.g. `{ [x |= 1]: 2 }`, by reusing the existing compound-assignment operator mapping in expression parsing.
- Root cause: compound assignments were parsed for assignment statements but not for nested expressions, so computed-key parsing stopped at `PipeEqual` and reported `expected RightBracket`.
- `language/expressions/object/cpn-obj-lit-computed-property-name-from-assignment-expression-bitwise-or.js` now triages as `BuildPass`.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_compound_assignment_computed_property_key_expression -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 reference/test262/test/language/expressions/object/cpn-obj-lit-computed-property-name-from-assignment-expression-bitwise-or.js` => `BuildPass`
  - `cargo fmt --all --check`
  - `git diff --check`
  - `mise run issue-lint` => valid with pre-existing done-owner warnings
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest` after clearing stale `ts2wasm-backend-wasm` artifacts and preserving member/index compound assignment parsing => 2503 passed, 10 skipped

### 2026-05-15 async arrow computed key parser slice

- Accepted async arrow function expressions as computed object-literal keys, e.g. `{ [async () => {}]: 1 }`, by extending the existing speculative arrow-head parser to consume an `async` prefix.
- This is syntax/build support only; broader async execution and Promise semantics remain tracked by existing async issues such as `I-20260514-F3MYS5`.
- `language/expressions/object/cpn-obj-lit-computed-property-name-from-async-arrow-function-expression.js` now triages as `BuildPass`.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_async_arrow_computed_property_key_expression -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 reference/test262/test/language/expressions/object/cpn-obj-lit-computed-property-name-from-async-arrow-function-expression.js` => `BuildPass`
  - `cargo fmt --all --check`
  - `git diff --check`
  - `mise run issue-lint` => valid with pre-existing done-owner warnings
  - `mise run check`
  - `mise run clippy`
  - `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_regexp_flag_d -- --exact --nocapture` after clearing stale `ts2wasm-cli` artifacts
  - `mise run nextest` after clearing stale `ts2wasm-backend-wasm` artifacts => 2502 passed, 10 skipped

### 2026-05-15 object generator method parser slice

- Accepted concise object generator methods in both static and computed forms, e.g. `{ *g() { yield 1; } }` and `{ *["a"]() { yield 2; } }`.
- Added explicit function-expression generator metadata so object-method generator syntax is preserved past parsing/name resolution instead of being indistinguishable from ordinary method shorthand.
- `language/computed-property-names/object/method/generator.js` now triages as `BuildPass`.
- `language/expressions/object/concise-generator.js` now moves past the object generator method parser failure and is blocked by the existing `issue-5255` super property access gap.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_object_generator_methods -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/method/generator.js` => `BuildPass`
  - `mise run reference-triage -- test262 reference/test262/test/language/expressions/object/concise-generator.js` => progresses to `issue-5255: super property access is not supported in this milestone`
  - `mise run issue-lint`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`

### 2026-05-15 large exponent computed key slice

- Preserved oversized positive decimal exponent literals such as `1e55` as decimal-number tokens (`1e+55`) instead of expanding them into an overflowing `i32` integer token.
- Added `fixtures/core-expressions/object-literal-computed-large-exponent-key.ts` so computed property keys using large exponent numeric literals are covered by build smoke and Node/iwasm differential fixtures.
- `language/computed-property-names/object/property/number-duplicates.js` now triages as `BuildPass`.
- Validation:
  - `cargo test -p ts2wasm-frontend oversized_positive_decimal -- --nocapture`
  - `cargo test -p ts2wasm-frontend non_hex_range -- --nocapture`
  - `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_computed_large_exponent_key -- --exact --nocapture`
  - Direct fixture parity: `cargo run -q -p ts2wasm-cli -- build fixtures/core-expressions/object-literal-computed-large-exponent-key.ts -o /tmp/ts2wasm-object-literal-computed-large-exponent-key.wasm && node fixtures/core-expressions/object-literal-computed-large-exponent-key.ts && iwasm /tmp/ts2wasm-object-literal-computed-large-exponent-key.wasm`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/property/number-duplicates.js` => `BuildPass`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`

### 2026-05-15 symbol method invocation slice

- Extended object-literal method metadata from string-only property names to typed accessor keys, so direct calls through statically visible symbol locals (`obj[sym]()`) can dispatch to the stored object-literal method by symbol identity.
- Computed object-literal method values now lower as receiver-aware object methods, preserving `this` binding for symbol-keyed method shorthand.
- Preserved typed method metadata through lowered identity-helper calls such as `[ID(sym)]()` so the representative test262 symbol method case no longer drops earlier `a`/`c` method dispatch information.
- Added `fixtures/core-expressions/object-literal-symbol-method-call.ts` covering string methods, direct symbol methods with `this`, identity-helper symbol methods, and distinct same-description symbols.
- `language/computed-property-names/object/method/symbol.js` now moves past `object.a()`, `object[sym1]()`, `object.c()`, and `object[sym2]()` dispatch; it is still blocked by the runtime symbol-key enumeration gap (`Object.getOwnPropertySymbols`), tracked under `I-20260515-YP3J2Q`.
- Validation:
  - `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_computed_method_call -- --exact --nocapture`
  - `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_symbol_method_call -- --exact --nocapture`
  - Direct fixture parity: `cargo run -q -p ts2wasm-cli -- build fixtures/core-expressions/object-literal-symbol-method-call.ts -o /tmp/ts2wasm-object-literal-symbol-method-call.wasm && node fixtures/core-expressions/object-literal-symbol-method-call.ts && iwasm /tmp/ts2wasm-object-literal-symbol-method-call.wasm`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact --nocapture`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/method/symbol.js` => progresses to `UnresolvedName: unresolved name: Object` at `Object.getOwnPropertySymbols`.

### 2026-05-15 symbol accessor invocation slice

- Added static symbol-local facts and a typed object-accessor metadata key so computed accessor descriptors keyed by statically visible symbols dispatch by symbol identity instead of by stringified property name.
- Computed reads/writes such as `obj[s]` now direct-call tracked getter/setter functions when `s` is a known symbol local, preserving distinct `Symbol("slot")` locals for object-literal accessor dispatch.
- Added `fixtures/core-expressions/object-literal-symbol-accessor-invocation.ts` covering getter/setter invocation and two same-description symbols that must remain distinct.
- Root-cause repro before the fix:
  - A scratch fixture with `get [s]()` built but printed the stored descriptor/object value instead of invoking the getter.
  - A scratch fixture with two `Symbol("slot")` accessor keys printed the second slot for both keys through dynamic stringification.
- Validation:
  - `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_symbol_accessor_invocation -- --exact --nocapture`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact --nocapture`
  - Direct fixture parity: `cargo run -q -p ts2wasm-cli -- build fixtures/core-expressions/object-literal-symbol-accessor-invocation.ts -o /tmp/ts2wasm-object-literal-symbol-accessor-invocation.wasm && node fixtures/core-expressions/object-literal-symbol-accessor-invocation.ts && iwasm /tmp/ts2wasm-object-literal-symbol-accessor-invocation.wasm`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/getter.js` => `BuildPass`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/setter.js` => `BuildPass`
- Discovered broader runtime symbol-key identity storage gap and tracked it as `I-20260515-YP3J2Q`.
- Remaining: `super` accessor cases blocked by issue-5255 super property access and broader object-literal coverage closure.

### 2026-05-15 computed accessor invocation slice

- Routed dynamic computed object-literal accessors such as `get [key]()` and `set [key](value)` through accessor descriptor lowering instead of ordinary computed data-property lowering.
- Extended tracked accessor metadata to static computed index reads/writes (`obj[key]`) when the key value is statically known.
- Added `fixtures/core-expressions/object-literal-computed-accessor-invocation.ts` for node/iwasm parity on computed getter/setter invocation.
- Root-cause repro before the fix:
  - `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_computed_accessor_invocation -- --exact --nocapture` failed with `issue-5179: 'this' implicitly has type 'any' ... in nested function get [computed]`.
- Validation:
  - `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_computed_accessor_invocation -- --exact --nocapture`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact --nocapture`
  - Direct fixture parity: `cargo run -q -p ts2wasm-cli -- build fixtures/core-expressions/object-literal-computed-accessor-invocation.ts -o /tmp/ts2wasm-object-literal-computed-accessor-invocation.wasm && node fixtures/core-expressions/object-literal-computed-accessor-invocation.ts && iwasm /tmp/ts2wasm-object-literal-computed-accessor-invocation.wasm`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/getter.js` => `BuildPass`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/setter.js` => `BuildPass`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/getter-duplicates.js` => `BuildPass`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/setter-duplicates.js` => `BuildPass`
- Remaining: symbol-key runtime parity and `super` accessor cases blocked by issue-5255 super property access.

### 2026-05-15 accessor invocation lowering slice

- Added static lowering metadata for accessor descriptors defined through `Object.defineProperty(obj, "key", { get/set })` and object-literal accessor descriptor blocks.
- Property reads and writes on known object locals now dispatch directly to the tracked getter/setter functions with the object as receiver, while descriptor storage remains available for `Object.getOwnPropertyDescriptor`.
- Root-cause repro before the fix:
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::property_getter_setter_descriptor_matches_node_output -- --exact --nocapture` failed with `8296` vs `42`.
- Validation:
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::property_getter_setter_descriptor_matches_node_output -- --exact --nocapture`
  - `cargo test -p ts2wasm-cli --test m6_builtin_methods build_smoke_property_getter_setter_descriptor -- --exact --nocapture`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::object_define_property_getter_matches_node -- --exact --nocapture`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact --nocapture`
  - Direct fixture parity: `cargo run -q -p ts2wasm-cli -- build fixtures/core-semantics/property-getter-setter.ts -o /tmp/ts2wasm-property-getter-setter.wasm && node fixtures/core-semantics/property-getter-setter.ts && iwasm /tmp/ts2wasm-property-getter-setter.wasm`
- Remaining: dynamic accessor keys, `super` accessor cases blocked by issue-5255 super property access, and broader prototype/computed-name closure.

### 2026-05-15 computed object accessor parser slice

- Accepted computed property names after object-literal `get`/`set` accessor prefixes, e.g. `get ["a"]() {}`, `set [1](v) {}`, and dynamic `get [key]() {}`.
- Added a frontend parser regression covering static string, static numeric, and dynamic computed accessor keys.
- Root-cause repro before the fix:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_computed_accessor_key_expression -- --exact` failed with `expected Comma, got Some(LeftBracket)`.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_computed_accessor_key_expression -- --exact`
  - `cargo test -p ts2wasm-frontend parser::tests::parses_object_accessor_parameter_trailing_comma -- --exact`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/getter.js` => `BuildPass`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/setter.js` => `BuildPass`
- Remaining: actual accessor getter/setter invocation semantics, duplicate computed accessor cases, and broader prototype/computed-name closure.

### 2026-05-15 object literal setter descriptor storage slice

- Changed accessor property storage so `Object.defineProperty` keeps the descriptor object for accessor entries instead of only the getter value. `Object.getOwnPropertyDescriptor` now reads `get` and `set` back from that stored descriptor, which preserves setter-only object literal descriptors.
- Extended `fixtures/core-expressions/object-literal-setter-descriptor.ts` to assert that setter-only literals expose `get === undefined` and `set !== undefined`.
- Root-cause repro before the fix:
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact --nocapture` failed on `object-literal-setter-descriptor.ts` with `no_setter` vs `has_setter`.
- Validation:
  - `cargo test -p ts2wasm-cli build_smoke_object_literal_setter_descriptor -- --exact`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact --nocapture`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::object_define_property_getter_matches_node -- --exact --nocapture`
  - Direct fixture parity: `cargo run -q -p ts2wasm-cli -- build fixtures/core-expressions/object-literal-setter-descriptor.ts -o /tmp/ts2wasm-object-literal-setter-descriptor.wasm && node fixtures/core-expressions/object-literal-setter-descriptor.ts && iwasm /tmp/ts2wasm-object-literal-setter-descriptor.wasm`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/accessor/getter.js` and `.../setter.js` still stop earlier in parser syntax around computed member expressions in the test body.
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::property_getter_setter_descriptor_matches_node_output -- --exact --nocapture` still fails on accessor invocation semantics (`8296` vs `42`), so this slice fixes descriptor round-tripping only.
- Remaining: actual accessor getter/setter invocation semantics, parser closure for computed member expressions inside representative test262 accessor cases, and broader prototype/computed-name closure.

### 2026-05-15 computed numeric method call slice

- Recognized numeric identity helpers such as `ID(2)` when resolving static computed object keys, matching the existing string identity-key support.
- Lowered calls through computed object method keys (`object[1]()` / `object[ID(2)]()`) when the receiver has object-literal method metadata and the key is statically known.
- Added `fixtures/core-expressions/object-literal-computed-number-method-call.ts` for static numeric and identity numeric computed method calls.
- `language/computed-property-names/object/method/number.js` now triages as `BuildPass`.
- Discovered that own-key enumeration orders integer-like keys by insertion order instead of JavaScript integer-key ordering; tracked as `I-20260515-QP69NK`.
- Validation:
  - `cargo test -p ts2wasm-cli build_smoke_object_literal_computed_number_method_call -- --exact`
  - `cargo run -q -p ts2wasm-cli -- build fixtures/core-expressions/object-literal-computed-number-method-call.ts -o /tmp/ts2wasm-object-literal-computed-number-method-call.wasm && node fixtures/core-expressions/object-literal-computed-number-method-call.ts && iwasm /tmp/ts2wasm-object-literal-computed-number-method-call.wasm`
  - `cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/method/number.js`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: object own-key integer ordering (`I-20260515-QP69NK`), generator object methods, template literal harness gaps, and broader test262 closure.

### Current state (2026-05-14)

307 test262 cases fail on object literal edge cases. Basic object literals probably work.

### Quick reproduction

```bash
head -10 /tmp/unsup_object-literal.txt
wc -l /tmp/unsup_object-literal.txt

# Triage one case (sub-second)
mise run reference-triage -- test262 reference/test262/test/harness/deepEqual-object.js
```

### File list (first 20)

```
harness/deepEqual-object.js
intl402/Collator/supportedLocalesOf/taint-Object-prototype.js
intl402/Collator/taint-Object-prototype.js
intl402/DateTimeFormat/prototype/format/taint-Object-prototype.js
intl402/DateTimeFormat/prototype/format/temporal-objects-not-overlapping-options.js
intl402/DateTimeFormat/prototype/formatRange/temporal-objects-not-overlapping-options.js
intl402/DateTimeFormat/prototype/formatRangeToParts/temporal-objects-resolved-time-zone.js
intl402/DateTimeFormat/prototype/formatRangeToParts/temporal-objects-not-overlapping-options.js
intl402/DateTimeFormat/prototype/formatToParts/temporal-objects-resolved-time-zone.js
intl402/DateTimeFormat/prototype/formatToParts/temporal-objects-not-overlapping-options.js
intl402/DateTimeFormat/taint-Object-prototype-fractionalSecondDigits.js
intl402/DateTimeFormat/taint-Object-prototype-dayPeriod.js
intl402/DateTimeFormat/taint-Object-prototype-date-time-components.js
intl402/DateTimeFormat/supportedLocalesOf/taint-Object-prototype.js
intl402/DateTimeFormat/taint-Object-prototype.js
intl402/DurationFormat/prototype/format/temporal-duration-object-arg.js
intl402/DurationFormat/prototype/formatToParts/temporal-duration-object-arg.js
intl402/NumberFormat/supportedLocalesOf/taint-Object-prototype.js
intl402/NumberFormat/taint-Object-prototype.js
intl402/Temporal/PlainMonthDay/from/fields-object.js
```

### Regenerate full list

```bash
python3 -c "
import json
with open('artifacts/coverage/results/test262-results.jsonl') as f:
    for line in f:
        r = json.loads(line)
        if r['status'] == 'unsupported' and ': object-literal' in r.get('reason',''):
            print(r['case'].replace('/home/wogikaze/wgkz/ts2wasm/reference/test262/test/', ''))
" > /tmp/unsup_object-literal.txt
```

### Missing features

- Computed property names: `{ [expr]: value }`
- Method definitions: `{ method() {} }`
- Getter/setter: `{ get x() {}, set x(v) {} }`
- __proto__: `{ __proto__: otherObj }`
- Object spread: `{ ...obj }`
- Shorthand properties: `{ x, y }` (may already work)

### 2026-05-14 object literal __proto__ slice

- Routed object literal `__proto__` entries through the existing prototype-set runtime path instead of materializing an own data property.
- Added `fixtures/core-expressions/object-literal-proto.ts` covering inherited lookup, own property lookup, and `obj.__proto__ === proto`.
- Validation:
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_proto`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff core_expression_fixtures_match_node_output_under_iwasm`
  - `cargo check -p ts2wasm-ir -p ts2wasm-cli`
  - `mise run check architecture`
- Remaining: full test262 closure still needs computed-name edge cases, object literal accessors, dynamic spread, and broader prototype semantics.

### 2026-05-14 object literal method shorthand slice

- Routed object literal method shorthand through receiver-aware nested-function lowering so `this` binds to the receiver on method calls.
- Added `fixtures/core-expressions/object-literal-method.ts` covering receiver reads and an outer capture from method shorthand.
- Promoted `fixtures/core-semantics/property-getter-setter.ts` from unsupported-diagnostic coverage to build smoke plus node-diff descriptor coverage because `get`/`set` descriptor methods now lower as receiver-aware method shorthand.
- Validation:
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_method`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff core_expression_fixtures_match_node_output_under_iwasm`
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_method build_smoke_property_getter_setter_descriptor`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff property_getter_setter_descriptor_matches_node_output core_expression_fixtures_match_node_output_under_iwasm`
  - `cargo check -p ts2wasm-ir -p ts2wasm-cli`
- Broader gates are blocked by unrelated partial Date setter WIP from `I-20260514-7NX8Z6`: `mise run check architecture` and `mise run clippy` reintroduce/witness `DateSetUTCMonth` and sibling variants without complete runtime catalog/spec/manifest coverage.
- Remaining: full test262 closure still needs computed-name edge cases, object literal accessors, dynamic spread, and broader prototype semantics.

### 2026-05-14 object literal getter descriptor slice

- Lowered object literal getter entries to accessor descriptors through `ObjectDefineProperty` with enumerable/configurable defaults matching JavaScript object literal semantics.
- Added `fixtures/core-expressions/object-literal-getter-descriptor.ts` covering descriptor shape and ordinary data property coexistence.
- Validation:
  - `cargo fmt --all --check`
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_getter_descriptor`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff core_expression_fixtures_match_node_output_under_iwasm`
  - `cargo check -p ts2wasm-ir -p ts2wasm-cli`
  - `mise run check architecture`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: setter descriptors/runtime invocation, dynamic spread, and broader prototype/computed-name test262 closure.

### 2026-05-14 object literal computed dynamic spread slice

- Routed non-static object spread through `ObjectSpread` in the computed-key/proto/accessor sequential lowering path instead of reporting issue-274.
- Added `fixtures/core-expressions/object-literal-computed-spread.ts` covering `{ prefix, ...source, [key]: value }` ordering.
- Validation:
  - `cargo fmt --all --check`
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_computed_spread`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff core_expression_fixtures_match_node_output_under_iwasm`
  - `cargo check -p ts2wasm-ir -p ts2wasm-cli`
  - `mise run check architecture`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: setter descriptors/runtime invocation and broader prototype/computed-name test262 closure.

### 2026-05-14 object literal setter descriptor slice

- Extended object literal accessor classification so `set name(value) {}` lowers through the existing `ObjectDefineProperty` descriptor path instead of the unsupported accessor diagnostic.
- Added `fixtures/core-expressions/object-literal-setter-descriptor.ts` covering setter descriptor presence plus object literal enumerable/configurable defaults.
- Validation:
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_setter_descriptor` initially failed with `issue-67ZV8S: object literal getter/setter accessors are not supported`
  - `cargo fmt --all --check`
  - `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_object_literal_setter_descriptor`
  - `cargo nextest run -p ts2wasm-cli --test m2_node_diff core_expression_fixtures_match_node_output_under_iwasm --nocapture` (registered; local node/iwasm assertions skipped because `TS2WASM_RUN_M2_NODE_DIFF` is unset)
  - `cargo check -p ts2wasm-ir -p ts2wasm-cli`
  - `mise run check architecture`
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: accessor getter/setter invocation semantics and broader prototype/computed-name test262 closure.

### 2026-05-14 object method trailing parameter comma parser slice

- Accepted a comma immediately before `)` in object method and object accessor parameter lists, matching the already-supported function-declaration behavior.
- Extended function-expression parameter parsing with the same trailing-comma rule because the representative test262 method case still reaches harness/function-expression parsing after the object method.
- Added frontend parser regressions for object methods, object accessors, and function expressions with trailing parameter commas.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_object_method_parameter_trailing_comma -- --exact` initially failed with `issue-247: expected binding identifier or pattern, got Some(RightParen)`
  - `cargo test -p ts2wasm-frontend parser::tests::function_expression_parameter_trailing_comma -- --exact`
  - `cargo test -p ts2wasm-frontend parser::tests::parses_object_method_parameter_trailing_comma -- --exact`
  - `cargo test -p ts2wasm-frontend parser::tests::parses_object_accessor_parameter_trailing_comma -- --exact`
  - `cargo fmt --all --check`
  - `cargo check -p ts2wasm-frontend -p ts2wasm-cli`
  - `mise run reference-triage -- test262 reference/test262/test/language/expressions/object/method-definition/meth-params-trailing-comma-single.js` still reports the same parser diagnostic in the broader transformed test262 wrapper, so this is a parser subset improvement rather than full test262 closure.
  - `mise run check`
  - `mise run clippy`
  - `mise run nextest`
- Remaining: identify the transformed test262 wrapper parameter-list site still producing issue-247, accessor invocation semantics, and broader prototype/computed-name test262 closure.

### 2026-05-14 object literal computed expression key parser slice

- Changed computed object-key parsing so static literal fast paths apply only to immediately closed keys like `["x"]` or `[1]`; expressions such as `[1 + 1]` and `["n" + "ame"]` now parse through the full expression parser.
- Added `fixtures/core-expressions/object-literal-computed-expression-key.ts` covering number and string expression keys with runtime lookup.
- Re-ran `meth-params-trailing-comma-single.js`; the old parser diagnostic is gone and the case now reaches `issue-062e` mutable closure capture. Tracked separately as `I-20260514-8CFHJM`.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_computed_property_key_expression -- --exact`
  - `cargo test -p ts2wasm-cli build_smoke_object_literal_computed_expression_key -- --exact`
  - `cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact`
  - direct fixture parity: `node fixtures/core-expressions/object-literal-computed-expression-key.ts` and `iwasm /tmp/ts2wasm-object-literal-computed-expression-key.wasm` both printed `two` / `Alice`
  - `mise run reference-triage -- test262 reference/test262/test/language/expressions/object/cpn-obj-lit-computed-property-name-from-additive-expression-add.js`
  - `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact --nocapture` fails on the pre-existing `object-literal-setter-descriptor.ts` mismatch (`no_setter` vs `has_setter`), not on the new computed-key fixture.
- Remaining: computed method forms, accessor invocation semantics, and broader prototype/computed-name test262 closure.

### 2026-05-14 object literal computed method parser slice

- Accepted computed object method syntax for dynamic method names like `{ [key]() { ... } }` by routing the computed-key branch through object method parsing before requiring `:`.
- Added `fixtures/core-expressions/object-literal-computed-method.ts` covering static and dynamic computed method definitions as enumerable own properties.
- Direct calls such as `obj.a()` on a computed-key-path object literal still fail because method dispatch metadata is not retained after block lowering; tracked separately as `I-20260514-XTZJGV`.
- Include-bearing test262 triage for `language/computed-property-names/object/method/string.js` is currently blocked before parser/lowering by temp-path test262 root discovery; tracked separately as `I-20260514-DJTQ7K`.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_computed_method_key_expression -- --exact`
  - `cargo test -p ts2wasm-cli build_smoke_object_literal_computed_method -- --exact` initially failed on direct `obj.a()` dispatch, then passed after narrowing the fixture to property materialization.
  - direct fixture parity: `node fixtures/core-expressions/object-literal-computed-method.ts` and `iwasm /tmp/ts2wasm-object-literal-computed-method.wasm` both printed `a` / `b`
  - `cargo test -p ts2wasm-cli --test m2_node_diff m2_node_diff_fixture_tests::core_expression_fixtures_match_node_output_under_iwasm -- --exact`
  - `mise run reference-triage -- test262 reference/test262/test/language/computed-property-names/object/method/string.js` reports `BackendIo` for test262 root discovery from `/tmp`.
- Remaining: computed method direct-call dispatch, accessor invocation semantics, and broader prototype/computed-name test262 closure.

### 2026-05-15 object literal computed yield parser/lowering slice

- Accepted empty-statement while bodies such as `while (iter.next().done === false) ;`, matching the generated test262 wrapper used by computed-yield object literal cases.
- Preserved generator-iterator local facts for generator calls that do not have a static resume-state binding, so fallback `iter.next()` calls still lower through `GeneratorNext` instead of reporting an unknown receiver class.
- Root cause: computed object keys containing `yield` do not currently produce static generator step metadata, but the local still semantically holds a generator iterator and must remain marked as one for runtime fallback lowering.
- Validation:
  - `cargo test -p ts2wasm-frontend parser::tests::parses_while_empty_statement_body -- --exact --nocapture`
  - `cargo test -p ts2wasm-ir lowered_generator_iterator_without_static_steps_still_lowers_next -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli && mise run reference-triage -- test262 reference/test262/test/language/expressions/object/cpn-obj-lit-computed-property-name-from-yield-expression.js` reports `BuildPass`.
- Remaining: computed method direct-call dispatch, accessor invocation semantics, and broader prototype/computed-name test262 closure.

### 2026-05-15 generator capture and next-argument slice

- Included top-level generator functions in function-capture collection so generator bodies can assign captured top-level bindings such as `obj` in generated object-literal tests.
- Accepted `generator.next(value)` at lowering time for runtime-fallback generator iterators; static generator-step lowering remains limited to zero-argument `.next()`.
- `language/expressions/object/accessor-name-computed-yield-expr.js` now progresses from unresolved `obj` to the nested computed setter capture gap for `yieldSet`, tracked separately as `I-20260515-VWP4WJ`.
- Validation:
  - `cargo test -p ts2wasm-ir lowered_generator_function_captures_top_level_assignment -- --exact --nocapture`
  - `cargo build -p ts2wasm-cli && mise run reference-triage -- test262 /home/wogikaze/wgkz/ts2wasm/reference/test262/test/language/expressions/object/accessor-name-computed-yield-expr.js`
- Remaining: nested computed accessor captures inside generator object literals (`I-20260515-VWP4WJ`), computed method ToPropertyKey side effects, super cases (`I-20260515-GV2HX9`), and broader object-literal closure.

### 2026-05-15 computed fractional math key slice

- Fixed strict equality for heap-backed decimal numbers by comparing heap-number decimal payloads directly instead of routing them through `$number_to_i32`, which traps on fractional text.
- Added `fixtures/core-expressions/object-literal-computed-fractional-math-key.ts` covering fractional computed-property keys and helper-call strict equality, matching the `cpn-obj-lit-computed-property-name-from-math.js` failure shape.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -q -p ts2wasm-cli`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm cargo test -q -p ts2wasm-cli object_literal_computed_fractional_math_key_fixture_matches_node_output_under_iwasm --test m2_node_diff`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-math.js --jobs 1 --no-dashboard-data`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from-integer-separators.js --jobs 1 --no-dashboard-data`
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm mise run reference-coverage -- test262 --jsonl --path-filter language/expressions/object/cpn-obj-lit-computed-property-name-from --jobs 4 --no-dashboard-data` now reports `Pass: 25, Fail: 5, Unsupported: 1`.
  - `git diff --check`
  - `mise run issue-lint` (passes with existing done-owner warnings for `I-20260513-4D4T58`, `I-20260513-7XN478`, `I-20260513-R6HZCH`)
  - `mise run check`
  - `mise run fmt`
  - `mise run clippy`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -q -p ts2wasm-cli --test linker_structure`
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules mise run nextest` (`2543 passed, 10 skipped`)
- Remaining: function-valued computed-property names, async `$DONE` support, and broader object-literal closure.
