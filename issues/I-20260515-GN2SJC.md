Id: I-20260515-GN2SJC
Status: done
Priority: P1
Labels: area:frontend feature:for-in feature:object-literal
DependsOn: I-20260514-67ZV8S
Owner:
BlockedReason:
Created: 2026-05-15T13:48:51+09:00
Updated: 2026-05-15T13:57:55+09:00
Title: Frontend gap: braceless for-in loop bodies
Summary: Template-literal blocker replay now stops on braceless for-in loop bodies in deepEqual.js.
Next:
---

## Notes

### 2026-05-15 completion

- Routed `for`, `for-in`, `for-of`, and `for await` loop bodies through the parser's existing single-statement control-body helper.
- Treated `;` as an empty control-statement body in that helper.
- Added parser regression tests for single-statement classic `for`, `for-in`, and `for-of` bodies.
- Focused tagged-template shard now clears the previous `expected LeftBrace, got Some(Ident("aKeys"))` parser blocker.
- Remaining focused-shard blocker is `issue-211: unknown receiver class for method join (receiver arr)` in the prepared `deepEqual` harness, tracked as `I-20260515-SA2YJD`.
- Validation:
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-frontend for_in` => 4 passed
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo test -p ts2wasm-frontend single_statement_body` => 3 passed
  - `CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S cargo build -q -p ts2wasm-cli` => pass
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run reference-coverage -- test262 --jsonl --detail --paths-file /tmp/tagged-template-function-relpaths-67ZV8S.txt --jobs 5 --no-dashboard-data --no-semantic` => Unsupported: 5, no `aKeys` parse blocker
  - `NODE_PATH=/home/wogikaze/wgkz/ts2wasm/node_modules TS2WASM_BINARY=/tmp/ts2wasm-target-67ZV8S/debug/ts2wasm CARGO_TARGET_DIR=/tmp/ts2wasm-target-67ZV8S mise run reference-coverage -- test262 --detail --paths-file /tmp/tagged-template-function-relpaths-67ZV8S.txt --jobs 5 --no-dashboard-data --no-semantic` => UnsupportedSyntax: 5 (`feature-unsupported`: 4, `object-literal`: 1)

## Evidence
- focused parser tests pass; tagged-template five-case replay no longer reports expected LeftBrace/aKeys parser blocker
