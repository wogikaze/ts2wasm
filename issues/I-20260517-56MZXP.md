Id: I-20260517-56MZXP
Status: done
Priority: P2
Labels: area:ir coverage:test262 type:feature
Owner:
BlockedReason:
Created: 2026-05-17T12:30:00+09:00
Updated: 2026-06-14T00:00:00+09:00
Title: Fix unknown receiver class for RegExp.prototype.compile
Summary: Fix resolver to handle untyped receivers for RegExp.compile calls. ~10 test262 cases blocked.
Next: Add RegExp class detection for compile method in class resolver

---

## Acceptance

- [x] `regexp.compile(pattern)` resolves without "unknown receiver class" error
- [x] `regexp.compile(pattern, flags)` works with known RegExp locals
- [x] Node differential test passes

## Notes

test262 sample (limit 500): ~10 UnsupportedSyntax cases for unknown receiver class on `compile` method.
Issue-211 tracking. The receiver is a typed or ambient variable whose class cannot be determined by the resolver.

## Fix

- Added `RuntimeFn::RegExpCompile` variant with WAT implementation `$regexp_compile`
- Added `("RegExp", "compile") => Some(RuntimeFn::RegExpCompile)` to `collection_method_runtime_fn` in `program_builtins.rs`
- Removed the hard error at `method.rs:4663-4665` that prevented compile from reaching the class-dispatch path
- Fixed RegExp arity checks to allow 2 args for compile (vs test/exec which take 1)
- Added flags padding in `lower_collection_method_args` for RegExp compile
- The WAT function creates a new RegExp string by concatenating `/${pattern}/${flags}`
- All 34 runtime-catalog tests pass

## Files Changed

- `crates/runtime-catalog/src/runtime_fn.rs` — added `RegExpCompile` variant, deps, domain, emission order
- `crates/runtime-catalog/src/runtime/spec/all.rs` — added spec entry
- `crates/runtime-catalog/src/runtime/manifest/all.rs` — added manifest name
- `crates/backend-wasm/src/runtime/regexp/emit.rs` — added WAT function `$regexp_compile`
- `crates/backend-wasm/src/runtime_dispatch_string.rs` — added dispatch arm
- `crates/backend-wasm/src/runtime/string/catalog.rs` — added to string catalog
- `crates/ir/src/lowered/program_builtins.rs` — added collection_method_runtime_fn entry
- `crates/ir/src/lowered/resolver/call/method.rs` — removed compile error, fixed arity checks, added flags padding

## Test-Requirements
- Test type: differential
- Fixture path(s): fixtures/builtins-and-io/regexp-compile.ts
- Test command(s): cargo nextest run -p ts2wasm-cli --test node_diff -- regexp
- Expected result: pass
- Default gate: yes
