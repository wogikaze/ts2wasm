Id: I-20260513-MWJWKV
Status: open
Priority: P1
Labels: type:feature area:ir area:runtime feature:generator
Owner:
BlockedReason:
Created: 2026-05-13T21:32:28+09:00
Updated: 2026-05-13T21:32:28+09:00
Title: IR: generator branch and loop resume labels
Summary: Lower generator yield inside branches and loops with runtime resume labels instead of static straight-line step collection.
Next: Add Node diff fixtures for if/else yields and loop yields, then transform generator bodies into resumable control-flow blocks driven by runtime state.

---

## Notes

- Split from `I-20260513-HAY3Q2`: that slice replaces compile-time `next_index` with runtime state locals for straight-line generators, multiple iterator instances, aliasing, direct `gen().next()`, and trailing completion side effects.
- Remaining control-flow work needs generator body lowering to preserve branch/loop execution across suspend/resume rather than collecting only straight-line resume chunks.

## Acceptance

- Add a Node diff fixture where a generator yields from `if`/`else` branches and resumes correctly for both branch outcomes.
- Add a Node diff fixture where a generator yields from a loop across multiple `next()` calls.
- Replace the straight-line-only generator step collector for branch/loop bodies with runtime resume labels or equivalent control-flow state.
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff <branch-loop-generator-test-names>`
- `cargo test -p ts2wasm-ir -p ts2wasm-compiler`
