Id: I-20260514-D6YC6Z
Status: done
Priority: P1
Labels: area:runtime feature:number-formatting type:compat
Owner:
BlockedReason:
Created: 2026-05-14T09:31:50+09:00
Updated: 2026-05-14T09:41:10+09:00
Title: Runtime: Number formatting decimal rounding parity
Summary: Number.prototype formatting methods need decimal rounding and full precision/range parity beyond the small-integer runtime path.
Next:
---

## Notes

## Evidence

- decimal number formatting literal/local Node/iwasm fixtures pass

Implemented exact decimal-string formatting for statically known Number receivers:

- Decimal literals and straight-line locals initialized from decimal literals now fold
  `toFixed`, `toExponential`, and `toPrecision` to string literals during lowering.
- Rounding uses source decimal digits rather than binary floating-point formatting.
- Added `fixtures/builtins-and-io/number-formatting-decimal.ts` covering direct
  literals, negative decimal literals, precision padding, and local decimal
  receivers.

Validation:

- `env TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli number_formatting_matches_node number_formatting_decimal_matches_node` => pass
- `cargo check -p ts2wasm-ir` => pass
- `cargo nextest run -p ts2wasm-runtime-catalog` => pass
- `cargo fmt --all --check` => pass
- `python3 scripts/check/fixture-catalog.py` => pass
