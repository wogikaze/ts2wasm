Id: I-20260513-P5PH9E
Status: open
Priority: P1
Labels: type:feature area:ir area:runtime feature:generator
Owner:
BlockedReason:
Created: 2026-05-13T21:12:13+09:00
Updated: 2026-05-13T21:12:13+09:00
Title: IR: lazy generator resume side effects
Summary: Implement fully lazy generator resume semantics for side-effectful yield expressions and statement execution between next() calls.
Next: Add a Node diff fixture where generator creation precedes side-effectful yield execution, then lower resume states so GeneratorNext executes body steps lazily.

---

## Notes

- Split from `I-20260513-VSV8AF`: generator metadata, straight-line local yield materialization, and generator-specific `GeneratorNext` state object are now implemented. Remaining work is full lazy execution for side-effectful yield expressions and statements between `next()` calls.

## Acceptance

- Add a Node diff fixture where `const g = gen(); console.log("created"); g.next();` proves generator body side effects do not run at creation time.
- Add a Node diff fixture with statement side effects between two yields and verify each side effect runs on the corresponding `next()`.
- Lower `SuspendPoint` entries into executable resume states instead of precomputing all yield values during generator creation.
- `cargo test -p ts2wasm-ir -p ts2wasm-compiler`
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff <lazy-generator-test-name>`
