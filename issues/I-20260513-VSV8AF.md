Id: I-20260513-VSV8AF
LegacyId: 446
Status: doing
Priority: P1
Labels: type:feature area:ir
Owner: codex
BlockedReason:
Created: 2026-05-13T02:35:42+09:00
Updated: 2026-05-13T21:00:16+09:00
Title: IR: generator state-machine lowering
Summary: Add type:Generator to function flags. Transform function* body into state machine with yield points: save/restore locals, suspend/resume via Generator
Next: Lower SuspendPoint entries into a GeneratorNext runtime protocol and replace backend yield no-op emission.
---

## Acceptance
- cargo test -p ts2wasm-ir -p ts2wasm-compiler
- grep -R 'SuspendPoint\|GeneratorState' crates/ir/src/lowered
- mise run check architecture

## Notes
- 2026-05-13 codex: Added the first IR metadata slice: `LoweredFunction`/`MirFunction` now carry `is_generator` plus optional `GeneratorState`, and `GeneratorState` contains `SuspendPoint` entries. `function* gen() {}` now lowers with empty generator state metadata and MIR round-trips preserve it.
- Validation: `cargo test -p ts2wasm-ir -p ts2wasm-compiler`, `grep -R 'SuspendPoint\|GeneratorState' crates/ir/src/lowered`, `mise run fmt`, `mise run clippy`, `mise run check`, and `mise run nextest` passed.
- Blocker: `mise run check architecture` still fails on pre-existing architecture limits (`fixtures/catalog.yaml` > 1200 lines, `crates/ir/src/lowered/mir.rs` file/function size, `lower_mcall_array_runtime` > 200 lines). Do not mark this issue done until architecture is either fixed or the acceptance is split to avoid relying on that baseline blocker.
- 2026-05-13 codex: Preserved declaration generator bodies through parser, builtin resolution, lowering, MIR bridge, dumps, validation, and backend traversal. `yield` statements now lower as explicit `LoweredStmt::Yield` nodes, and generator metadata now records one `SuspendPoint` per lowered yield with deterministic resume states.
- Validation: `cargo test -p ts2wasm-frontend`, `cargo test -p ts2wasm-ir -p ts2wasm-compiler`, `grep -R 'SuspendPoint\|GeneratorState' crates/ir/src/lowered`, `mise run fmt`, `mise run clippy`, and `mise run check` passed.
- Remaining blocker: `mise run check architecture` still fails on baseline architecture limits (`fixtures/catalog.yaml`, `crates/ir/src/lowered/mir.rs`, and `lower_mcall_array_runtime`). The temporary backend lowering for `Yield` evaluates and drops the yield value so codegen traversals stay exhaustive, but observable generator protocol semantics remain for the next runtime slice.
- 2026-05-13 codex: Added a `generator-local-state` Node diff fixture and taught straight-line generator yield collection to evaluate local `let`/assignment state before materializing the generator iterator values. This covers the smallest save/restore-local case (`let value = 1; yield value; value = value + 1; yield value`) without weakening the existing `GeneratorState` metadata.
- Validation: `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff generator_local_state_matches_node_output`, `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff generator_basic_matches_node_output generator_local_state_matches_node_output`, `cargo test -p ts2wasm-ir -p ts2wasm-compiler`, and `cargo fmt --all --check` passed.
- Remaining blocker: full lazy resume through `GeneratorNext` is still not implemented, and `mise run check architecture` still fails on baseline architecture size/public-API limits. Keep this issue open until both are handled or formally split.
- 2026-05-13 codex: Replaced the runtime `GeneratorNext` array-iterator proxy with a generator-specific state object. `GeneratorYield` now creates `{values, state}` runtime state and `GeneratorNext` reads/updates the state slot while returning `{value, done}`. Added a backend WAT assertion that `generator_next` calls `$array_get` and no longer calls `$array_iterator_next`.
- Validation: `cargo test -p ts2wasm-backend-wasm generator_next_uses_generator_state_object` and `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff generator_basic_matches_node_output generator_local_state_matches_node_output` passed.
