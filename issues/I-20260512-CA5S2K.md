Id: I-20260512-CA5S2K
Status: open
Priority: P1
Labels: coverage class P16-semantic-correctness
Owner:
BlockedReason:
Created: 2026-05-12T12:00:00+09:00
Updated: 2026-05-12T12:00:00+09:00
Title: Class implementation completion
Summary: Burn down class-related unsupported records while keeping class fixtures differential.
Next: Identify the smallest class semantic gap with a focused failing differential fixture.
---

## Acceptance
- cargo test -p ts2wasm-cli --test m2_node_diff
- mise run reference-coverage -- test262 --jsonl --sample 50 --jobs 4 --no-dashboard-data

## Evidence
- Static field initializers: Implemented by emitting static field initializer assignments as a synthetic `static { ... }` block.
  Previously discarded (guarded by `if !is_static` in parser) -- fixed. Test passes: `class_static_fields_fixture_matches_node_output`.
- Diagnostic format: Updated 6 test expectations from `[Code]` to `[Code/phase]` prefix format (`
  [UnsupportedSyntax]` -> `[UnsupportedSyntax`, `[UnsupportedBuiltin]` -> `[UnsupportedBuiltin]`)
  in m2_node_diff.rs and m2_node_diff_fixture_tests.rs.
- Remaining: `class-extends-builtin` (extends Array) -- requires architectural changes: synthetic
  `super(...args)` call in implicit constructor, wiring builtin prototype chain in backend WAT emission.
  Not a small gap -- deferred to follow-up issue.

Validation: `TS2WASM_RUN_M2_NODE_DIFF=1 cargo test -p ts2wasm-cli --test m2_node_diff "class"` => 27 pass, 1 fail (extends-builtin)
Commit: TODO

## Notes
- Wave plan: `docs/27-coverage-expansion-epics.md`.
