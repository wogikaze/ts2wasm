Id: I-20260513-85ZGJE
LegacyId: 439
Status: done
Priority: P1
Labels: type:feature area:runtime
Owner: 
BlockedReason: 
Created: 2026-05-13T02:35:42+09:00
Updated: 2026-05-13T06:54:26+09:00
Title: Runtime: RegExp.exec and RegExp.test basic implementation
Summary: Implement RegExp.prototype.exec and test with basic pattern matching (literal chars, ., *, +, ?, [], (), |, ^, $, d, w, s, escape sequences). Track la
Next:
---

## Acceptance
- cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_regexp_exec_test
- cargo nextest run -p ts2wasm-cli --test m2_node_diff regexp_basic_matches_node_output

## Notes
- Existing RegExp lowering/runtime already routes literal-backed `test`, `exec`, and `String.prototype.match` through the shared RegExp runtime subset.
- Added acceptance-name tests that exercise `fixtures/core-semantics/regexp-test.ts`, which covers literal RegExp, `new RegExp`, `test`, `exec`, `match`, null results, and `g` flag construction.
- No new `regexp_exec_data` helper was added in this slice because the current acceptance is satisfied by the existing string-returning match runtime; capture arrays and indices remain a future extension.

## Evidence
- `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods build_smoke_regexp_exec_test` => pass
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff regexp_basic_matches_node_output` => pass
- `mise run fmt` => pass
- `mise run clippy` => pass
- `mise run nextest` => pass, 2014 passed, 10 skipped
- `mise run issue-lint` => pass
