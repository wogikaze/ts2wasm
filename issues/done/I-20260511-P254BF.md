Id: I-20260511-P254BF
LegacyId: 347
Status: done
Priority: P1
Labels: tooling cross P13-final-gate
DependsOn: 
Owner: 
BlockedReason: 
Created: 2026-05-11T12:00:00+09:00
Updated: 2026-05-12T12:00:00+09:00
Title: Add final architecture completion gate
Summary: Final roll-up gate for architecture decoupling, semantic coverage unblock, runtime catalog extraction, Validated contracts, LLM-friendly sizing, capab
Next: 
---

## Acceptance
- cargo test --workspace
- mise run check architecture
- python3 scripts/manager.py reference-coverage test262 --jobs 1 --path-filter language/statements
- grep -R '"semantic_pass":[1-9]' site/docs/coverage/web-ui/public/data web-ui/public/data site/docs/public/dashboard/data
- ! grep -R "ts2wasm_frontend" crates/backend-wasm/src crates/backend-wasm/Cargo.toml
- ! grep -R "RuntimeIntrinsic" crates/ir/src crates/backend-wasm/src
- ! grep -R "pub fn emit.*&LoweredProgram" crates/backend-wasm/src
- ! grep -R "RuntimeCall.*String\|runtime_fn: String" crates/ir/src
- ! grep -R "include!" crates/ir/src/lowered.rs
- grep -R "Validated<LoweredProgram>" crates/backend-wasm/src crates/compiler/src
- grep -R "Validated<RuntimeLinkPlan>" crates/runtime-catalog crates/backend-wasm crates/compiler
- test -f crates/runtime-catalog/Cargo.toml
- test -f crates/source/Cargo.toml
- test -f crates/diagnostic/Cargo.toml
- test -f crates/syntax/Cargo.toml
- test -f crates/resolve/Cargo.toml
- test -f crates/semantics/Cargo.toml
- test -f crates/runtime-abi/Cargo.toml
- test -f crates/backend-core/Cargo.toml

## Non-goals
- No requirement that the compiler is full ECMAScript compliant.
- No requirement that every test262 test passes.
- No requirement that wasm-encoder is the default backend.

## Plan files
- `scripts/check/architecture-rules.py`
- `docs/current-state.md`
- `docs/24-architecture-decoupling.md`
- `crates/**`
- `scripts/**`
- `tests/**`

## Notes
Closed after final gate validation; architecture warnings remain advisory and issue 276 tracks reducing oversized functions.

## Evidence
- Commit: `6db961e26`
- `cargo test --workspace` → exit 0
- `mise run check architecture` → exit 0
- `python3 scripts/manager.py reference-coverage test262 --jobs 1 --path-filter language/statements` → exit 0
- `grep -R '"semantic_pass":[1-9]' site/docs/coverage/web-ui/public/data web-ui/public/data site/docs/public/dashboard/data` → exit 0
- `! grep -R "ts2wasm_frontend" crates/backend-wasm/src crates/backend-wasm/Cargo.toml` → exit 0
- `! grep -R "RuntimeIntrinsic" crates/ir/src crates/backend-wasm/src` → exit 0
- `! grep -R "pub fn emit.*&LoweredProgram" crates/backend-wasm/src` → exit 0
- `! grep -R "RuntimeCall.*String\|runtime_fn: String" crates/ir/src` → exit 0
- `! grep -R "include!" crates/ir/src/lowered.rs` → exit 0
- `grep -R "Validated<LoweredProgram>" crates/backend-wasm/src crates/compiler/src` → exit 0
- `grep -R "Validated<RuntimeLinkPlan>" crates/runtime-catalog crates/backend-wasm crates/compiler` → exit 0
- `test -f crates/runtime-catalog/Cargo.toml && test -f crates/source/Cargo.toml && test -f crates/diagnostic/Cargo.toml && test -f crates/syntax/Cargo.toml && test -f crates/resolve/Cargo.toml && test -f crates/semantics/Cargo.toml && test -f crates/runtime-abi/Cargo.toml && test -f crates/backend-core/Cargo.toml` → exit 0
- `git diff --check` → exit 0
