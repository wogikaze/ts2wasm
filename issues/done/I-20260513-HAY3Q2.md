Id: I-20260513-HAY3Q2
Status: done
Priority: P1
Labels: type:feature area:ir area:runtime feature:generator
Owner:
BlockedReason:
Created: 2026-05-13T21:25:36+09:00
Updated: 2026-05-13T21:33:04+09:00
Title: IR: generator runtime next state for visible iterators
Summary: Replace compile-time generator next tracking with runtime state for statically visible generator iterators.
Next:
---

## Notes

- Split from `I-20260513-P5PH9E`: straight-line generator body statements before each yield now execute lazily for statically visible generator iterator locals, but the implementation tracks `next()` index at compile time.
- Remaining semantic gaps:
  - two iterator objects from the same generator function need independent runtime state;
  - statements after the final yield should run when the generator is resumed to completion;
  - direct `gen().next()` and aliasing should not depend on compile-time call ordering.
- 2026-05-13 codex: Split branch/loop yield lowering to `I-20260513-MWJWKV`; that requires control-flow resume labels rather than straight-line step collection.
- 2026-05-13 codex: Replaced compile-time iterator `next_index` with per-iterator runtime state locals for statically visible generator calls and aliases. Direct `gen().next()` uses a one-shot state local inside the lowered block expression. Completion statements after the final yield now run once when the iterator resumes to `done`.

## Acceptance

- Add Node diff fixtures for two independent generator iterator instances from the same function.
- Add Node diff fixtures for aliasing, direct `gen().next()`, and trailing side effects after the final yield.
- Replace compile-time `next_index` tracking with runtime resume-state execution driven by generator state.
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff <general-generator-test-names>`
- `cargo test -p ts2wasm-ir -p ts2wasm-compiler`

## Evidence
- TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff generator_multiple_instances_matches_node_output generator_alias_state_matches_node_output generator_direct_next_matches_node_output generator_trailing_completion_matches_node_output generator_lazy_creation_matches_node_output generator_lazy_between_yields_matches_node_output generator_basic_matches_node_output generator_local_state_matches_node_output; cargo test -p ts2wasm-ir -p ts2wasm-compiler; cargo fmt --all; rg -n 'next_index' crates/ir/src; mise run issue-lint; git diff --check
