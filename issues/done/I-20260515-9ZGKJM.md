Id: I-20260515-9ZGKJM
Status: done
Priority: P0
Labels: type:architecture area:capability area:gate
DependsOn: I-20260515-8JD3YA
Owner: child-agent
BlockedReason: 
Created: 2026-05-15T05:55:46+09:00
Updated: 2026-05-15T07:58:46+09:00
Title: Architecture wave: strict standalone host-deny compile gate
Summary: Convert host-deny from info-only to strict compile/manifest/wasm gate for standalone fixtures
Next: 
---

## Acceptance
- Add `--strict` to `scripts/check/host-deny.py` that compiles fixtures, emits manifests, and inspects wasm import sections.
- Extend `fixtures/catalog.yaml` host policy values: `host_imports: deny`, `host_imports: allow-wasi`, `host_imports: allow-node-host`.
- In strict mode, compile all fixtures with category not in `negative`, `parser`, `test-infrastructure` and host policy `deny` or `allow-wasi`.
- For each compiled fixture, validate: wasm import module `host` absent; manifest `node_host.required == false`; manifest `standalone == true`.
- Source regex remains advisory but must not be final verdict in strict mode.
- Add `--fixture PATH`, `--limit N` for local debugging; CI strict command has all-fixture mode.
- Add manager alias `check host` to pass through strict options; do not make `--strict --compile` part of default smoke.
- Update `crates/cli/tests/m11_host_deny.rs` to assert CLI build with `--host-deny deny` rejects known host-required fixture.
- Add script self-test with fake manifest/wasm import pairs.
- Add catalog validation that every compilable fixture has host policy inherited from directory or explicit fixture entry.
- Run: `python scripts/check/host-deny.py --self-test`
- Run: `python scripts/manager.py check host -- --strict --compile`
- Run: `cargo nextest run -p ts2wasm-cli --test m11_host_deny`

## Notes
- Source: `plans/next-architecture-design (4).md` REQ-CAP-002 / TASK-CAP-002
- Depends on REQ-CAP-001 (I-20260515-8JD3YA): manifest/import equivalence must be hard gate before strict host-deny can rely on manifest correctness.
- Suggested order: after manifest import parity gate (I-20260515-8JD3YA) and RuntimeLinkPlan validation (I-20260515-BX2KBJ).

## Evidence
- mise run check-host-deny -- --self-test => pass, manifest-imports deterministic check => pass, cargo test -p ts2wasm-runtime-catalog => pass
