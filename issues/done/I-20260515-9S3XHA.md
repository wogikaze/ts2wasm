Id: I-20260515-9S3XHA
Status: done
Priority: P1
Labels: type:architecture area:frontend area:infrastructure
DependsOn: I-20260515-YKACKF
Owner: 
BlockedReason: 
Created: 2026-05-15T05:55:46+09:00
Updated: 2026-05-15T14:35:44+09:00
Title: Architecture wave: TypeScript oracle boundary isolation
Summary: Mechanically enforce that production build path does not call TypeScript oracle
Next: 
---

## Acceptance
- Add `scripts/check/typescript-oracle-boundary.py` that scans production build path files:
  - `crates/compiler/src/pipeline.rs`, `crates/compiler/src/stages/*.rs`, `crates/backend-wasm/src/**/*.rs`, `crates/ir/src/**/*.rs`
  - Fails if they call `collect_typescript_diagnostics`, `check_typescript_file`, `typescript-oracle.js`, or `Command::new("node")`.
- Allow oracle usage in: `crates/frontend/src/typescript_oracle.rs`, CLI `check` command path, tests, and scripts/check and scripts/run triage files.
- Add manager alias `check typescript-oracle-boundary` and include it in `check frontend-boundary` composite.
- Optionally stop re-exporting oracle from `crates/frontend/src/lib.rs` and require explicit module path if tests can migrate safely.
- Script self-test with temporary allowed/forbidden files.
- CLI `ts2wasm check` test remains allowed.
- Build fixture test with `PATH` hiding `node` should still compile a simple file if cargo binary is already built.
- Docs state oracle is non-production and used only for reference triage.
- Run: `python scripts/check/typescript-oracle-boundary.py --self-test`
- Run: `python scripts/manager.py check typescript-oracle-boundary`
- Run: `cargo nextest run -p ts2wasm-cli --test type_reference_directives`

## Notes
- Source: `plans/next-architecture-design (4).md` REQ-FE-004 / TASK-FE-001
- Depends on REQ-FE-001 (I-20260515-YKACKF): frontend syntax ownership contract must be established before oracle boundary can be enforced mechanically.
- Suggested order: after frontend syntax ownership contract (I-20260515-YKACKF), alongside parser smoke (I-20260515-95D6A2).

## Evidence
- scripts/check/typescript-oracle-boundary.py created (self-test 6/6 pass, --check OK); scans production build path (pipeline.rs, stages/*, backend-wasm/src, ir/src); allows oracle in typescript_oracle.rs, CLI check, tests, scripts
