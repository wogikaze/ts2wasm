Id: I-20260519-5JNH3P
Status: done
Priority: P1
Labels: type:bug area:runtime
Owner: wogikaze
BlockedReason: 
Created: 2026-05-19T21:34:27+09:00
Updated: 2026-05-21T00:00:00+09:00
Title: Runtime: Number/Date/Math built-in runtime failures (656 combined)
Summary: 656 test262 fails: Number(238)+Date(213)+Math(205). Static and prototype methods need WAT fixes.
Next: 

---

## Notes

## Fixes Applied

### Math.round negative rounding (commit `e5df6152f`)
Fix for Math.round rounding up when fractional digit > 5 for negative numbers.

### Math.f16round routing (commit `338680146`)
Fixed dispatch in `resolve_method_to_runtime_fn` so Math.f16round routes to the correct WAT implementation.

### Number.prototype.toString(radix) (commit `e7086dcd5`)
Fixed dispatch so Number.prototype.toString with a radix argument routes correctly.

### Number.toString routing + ParseInt/ParseFloat rename (commit `b6692b2a7`)
Fixed Number.toString method routing and renamed ParseInt/ParseFloat for consistency.

### Math.atan2 / Math.hypot host shims (commit `d1f986d73`)
Added two-argument host shims for Math.atan2 and Math.hypot (the only Math trig functions lacking host shim support). These cross-cutting changes touched 7 files:
- `crates/runtime-catalog/src/host_import.rs` — Added `MathAtan2`/`MathHypot` variants
- `crates/runtime-catalog/src/capability.rs` — Added `HostMathAtan2`/`HostMathHypot` capabilities
- `crates/runtime-catalog/src/runtime_fn.rs` — Added import/capability constants
- `crates/runtime-catalog/src/runtime/spec/all.rs` — Updated specs with imports/capabilities
- `crates/runtime-catalog/src/link_plan.rs` — Added host import→capability mappings
- `crates/backend-wasm/src/runtime/host/emit.rs` — Replaced broken local WAT impls with host shim calls
- `crates/backend-wasm/src/capability_manifest.rs` — Added new capabilities to Node host match arm

### Test Status
All 3239 tests pass (0 failures) after fixes.