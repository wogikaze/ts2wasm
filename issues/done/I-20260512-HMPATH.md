Id: I-20260512-HMPATH
Status: done
Priority: P1
Labels: compiler ir pipeline gate
DependsOn: I-20260512-P6GATE I-20260512-MREWAT
Owner:
BlockedReason:
Created: 2026-05-12T15:00:00+09:00
Updated: 2026-05-13T22:50:03+09:00
Title: Add feature-gated HIR to MIR to emit_mir pipeline and default switch rehearsal
Summary: Wire compiler stages through validated HIR and MIR under an opt-in flag before considering default switch.
Next:
---

## Notes

### Goal

Make the target pipeline executable without making it the default too early.

### Acceptance

- Compiler has an opt-in path for `BuiltinResolved AST -> Validated<HirProgram> -> Validated<MirProgram> -> emit_mir`.
- Unsupported HIR lowering cases fall back only when the mode explicitly permits compatibility fallback.
- The opt-in path records or exposes comparison results against the legacy path.
- Semantic canary and selected reference fixtures pass in opt-in mode before any default switch proposal.
- Docs/current-state or roadmap notes the switch status.

### Non-goals

- No broad feature expansion while wiring the path.
- No removal of `LoweredProgram` compatibility bridge in this issue.

## Evidence
- cargo test -p ts2wasm-ir --test hir_to_mir; cargo test -p ts2wasm-cli --test command_contract hir_mir; cargo run -p ts2wasm-cli -- build fixtures/hir-support/supported-function-calls.ts -o /tmp/ts2wasm-hmpath-supported-function-calls.wasm --experimental-hir-mir; cargo run -p ts2wasm-cli -- build fixtures/hir-support/supported-statements-and-expressions.ts -o /tmp/ts2wasm-hmpath-supported-statements.wasm --experimental-hir-mir; cargo run -p ts2wasm-cli -- build fixtures/hir-support/unsupported-ternary-expression.ts -o /tmp/ts2wasm-hmpath-ternary-fallback.wasm --experimental-hir-mir-compat-fallback; mise run fmt; mise run check; mise run clippy; mise run nextest
