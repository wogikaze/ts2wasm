Id: I-20260515-TWGRZQ
Status: done
Priority: P2
Labels: type:architecture area:process
Owner: 
BlockedReason: 
Created: 2026-05-15T05:32:53+09:00
Updated: 2026-05-15T14:52:21+09:00
Title: Architecture wave: open design questions
Summary: Track 5 unresolved architecture decisions from next-architecture-design that require resolution during implementation.
Next: 
---

## Acceptance
- All 5 open questions are resolved with documented decisions linked from this issue.
- Each resolution is recorded as a note in the relevant implementation issue or as an update to affected docs.
- No P0/P1 implementation is blocked waiting for open question resolution unless explicitly noted.

## Open Questions

### Q1: What exact commits should seed `reference/lock.json`?
- **Resolution path**: Run `scripts/reference_lock.py print-current --suite all` on the canonical machine that produced the current `artifacts/coverage/reference-coverage-matrix.md`.
- **Commit** the resulting lock with the matrix evidence.
- **Owner**: TASK-REF-001 (I-20260515-RPMVQN).

### Q2: Should `CapabilityManifest` schema version remain 1 after adding ABI metadata and `target_profile`?
- **Resolution path**: If validation can accept missing fields during migration, keep `schema_version=1` and document optional fields; otherwise follow `docs/11` migration procedure and bump to 2.
- **Owner**: TASK-CAP-002 / TASK-ABI-002 (I-20260515-RH7945 / I-20260515-E24P8A).

### Q3: When does `--harness-mode real` become the default?
- **Resolution path**: After `I-20260513-HDW7PQ` acceptance passes with unresolved harness globals not aggregated as unknown.
- **Owner**: I-20260513-HDW7PQ.

### Q4: Should noncanonical unlocked coverage rows appear in dashboard data?
- **Resolution path**: Allow them only as evidence rows with `status="noncanonical"`, never as the canonical suite row.
- **Owner**: TASK-REF-002 (I-20260515-ZA94X3).

### Q5: How should unavailable `wasm-encoder` feature/tools be represented?
- **Resolution path**: Tests and summaries must record `blocked` with reason `tool-unavailable:<tool-or-feature>`, not `pass`.
- **Owner**: TASK-ABI-002 (I-20260515-VC2Z7R / I-20260515-E24P8A).

## Notes
- Source: plans/next-architecture-design (2).md Open Questions section.
- These are architectural decisions requiring resolution during task implementation, not standalone implementation tasks.
- Each question has a designated owner issue that should resolve it as part of its implementation.
- This issue exists to ensure the questions are not forgotten; close it once all 5 are resolved.

## Evidence
- Closed in architecture batch wave
