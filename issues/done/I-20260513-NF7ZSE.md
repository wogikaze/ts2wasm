Id: I-20260513-NF7ZSE
Status: done
Priority: P1
Labels: area:ir area:frontend type:bug coverage:builtin-api
Owner:
BlockedReason:
Created: 2026-05-13T13:09:20+09:00
Updated: 2026-05-13T13:33:07+09:00
Title: IR: Preserve decimal numeric literals for runtime semantics
Summary: Decimal numeric literals such as 3.14 are lowered as integer values, blocking Number.isInteger base fixture coverage.
Next:
---

## Notes

- 2026-05-13: Preserved fractional decimal literals through syntax, resolved IR, HIR/MIR, and lowered IR as `DecimalNumber`.
- 2026-05-13: Backend now emits fractional decimal literals as heap-number decimal spellings instead of truncating to small integers.
- 2026-05-13: `Number.isInteger` / `Number.isSafeInteger` now inspect heap-number decimal spelling and return false when the fractional part has a non-zero digit.
- 2026-05-13: Unary minus now preserves fractional decimal literals as negative decimal spellings instead of routing through integer negation.
- 2026-05-13: Promoted `number-is-integer.ts` and `number-is-safe-integer.ts` into number static integer differential coverage.

## Evidence
- clean worktree: cargo check; TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest number_static_integer_fixtures; m6 number smoke; fmt/issue-lint/diff-check pass
- RED: `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff number_static_integer_fixtures_match_node_output_under_iwasm` => failed before this fix because `3.14` lowered as integer `3`.
- Probe: `Number.isInteger(3.14)`, `Number.isInteger(0.5)`, `Number.isInteger(-0.5)`, `Number.isInteger(1.0)`, and `Number.isSafeInteger(3.14)` match Node after preserving decimal spellings.
- `TS2WASM_RUN_M2_NODE_DIFF=1 cargo nextest run -p ts2wasm-cli --test m2_node_diff number_static_integer_fixtures_match_node_output_under_iwasm` => pass with `number-is-integer.ts` and `number-is-safe-integer.ts`
- clean worktree `/tmp/ts2wasm-nf7zse-decimal-validate`: `cargo check -p ts2wasm-frontend -p ts2wasm-ir -p ts2wasm-backend-wasm -p ts2wasm-cli` => pass
- clean worktree `/tmp/ts2wasm-nf7zse-decimal-validate`: `cargo nextest run -p ts2wasm-cli --test m6_builtin_methods number_is_integer_method_emits number_is_safe_integer_method_emits` => pass
- clean worktree `/tmp/ts2wasm-nf7zse-decimal-validate`: `cargo fmt --all --check`, `python scripts/issue-lint.py`, and `git diff --check` => pass
